---
name: character-physics-plan
description: Plan 008 — first character spawn with bitECS + Rapier physics, collision, follow camera
metadata:
  type: project
---

Plan: `.claude/plans/008-character-physics/readme.md`. Spawn the first **player** (TEMP cube, see [[player-cube-placeholder]]) on CJ's parking lot in Ganton (`GANTON_CJ_HOME = [2495,-1687,13]`, Z-up), with gravity + collision against the real map COL (from [[col-collision-plan]]'s `CollisionWorld`), a **follow camera**, and keyboard control in **play** mode when grounded. First real test of the COL collision and first slice of dynamics — via **bitECS** (entities/components/systems) + **Rapier** (`@dimforge/rapier3d-compat`, needs `await init()`).

**Key decisions:** physics + ECS run in **GTA Z-up** (Rapier gravity `(0,0,-9.81)`); the `−90°X` is display-only — `CollisionWorld` colliders go to Rapier as-is (model-space verts + Z-up placement per fixed body, no baking). Player `Object3D` sits under a new `Game.entityRoot` (`−90°X`), sibling of the region group; camera follows `player.getWorldPosition()` (Y-up). New `Config`: `gameState: 'play'|'pause'` + remappable `controls` keymap. New deps: `@dimforge/rapier3d-compat`, `bitecs`. `Game` gains `addSystem()` + `entityRoot`. Player model loaded via `three/addons` `TDSLoader` behind a `loadPlayerMesh` seam (→ DFF later).

**Module layout (generic `game` layer):** `game/ecs/` (world, components, queries), `game/physics/` (rapier init, physics-world, physics.system), `game/character/` (load-player, character-controller.system, render-sync.system), `game/input/keyboard.ts`, `core/camera-controller` gains follow mode.

**Iterations (each green):** 0 deps+seams (`addSystem`, `entityRoot`, `initRapier`) → 1 cube on lot (static) → 2 bitECS Transform + render-sync → 3 Rapier gravity + temp ground (falls/rests) → 4 real map colliders from `CollisionWorld` → 5 play/pause + keyboard + grounded control → 6 follow camera → 7 PLAY/PAUSE UI + polish.

**Status: Iterations 0–1 DONE.** Installed `@dimforge/rapier3d-compat@0.19.3` + `bitecs@0.4.0` (⚠️ bitecs 0.4 = new **functional** API, not classic 0.3). `game/physics/rapier.ts` `initRapier()` (cached) — **Rapier WASM works in vitest/node**, so physics is unit-testable headless (no skipIf). `Game.addSystem()` + `Game.entityRoot` (−π/2 X, display-only) + `getEntityRoot()`. **Iter 1:** `game/character/load-player.ts` `loadPlayerMesh(url)` (TDSLoader, bright placeholder material); `CameraController.focus`; `Game.frameEntity`; `PLAYER_SPAWN=[2495,-1687,16]`; bootstrap spawns the cube under entityRoot + frames it. 129 tests green; **cube-on-lot confirmed in browser** (tuned: `frameEntity` distance 20, player scale 0.8; cube floats until physics lands it — expected). Fixed a StrictMode bug in debug-overlay (region-reload effect's "skip first" ref mis-fired → camera clobbered; now `appliedModesRef` compare). **Iter 2:** `game/ecs/{world,components}.ts` (`Transform` SoA + `PlayerControlled` tag, bitECS 0.4 — components are plain SoA objects, data managed by hand), `game/character/render-sync.system.ts` (`Transform`→Object3D per frame) + `setup-character.ts` (`setupCharacter`→`CharacterContext {world, playerEid, renderRefs}`); bootstrap drives the cube via ECS. 131 tests green; visually unchanged (plumbing). **Iter 3:** `game/physics/physics-world.ts` (`PhysicsWorld` over `RAPIER.World`, gravity Z −9.81; `createBox`/`createStaticBox`/`step`/`readBody`/`dispose`), `physics.system.ts` (`PhysicsSystem.fixedUpdate`: step → body→`Transform` for `[RigidBody, Transform]`), `RigidBody{handle}` component. `setupCharacter` now **async** — builds Rapier world + temp static ground (z 12.5) + dynamic player box **sized from mesh bbox** + registers physics/render-sync; cube falls on load. 134 tests green (headless Rapier: drop & settle). Loop: PhysicsSystem.fixedUpdate → Transform → RenderSync.update → mesh. **Iter 4:** `PhysicsWorld.createStaticColliders(models)` — fixed body per placement (translation+rotation via `Matrix4.decompose`, Z-up) + trimesh/cuboid/ball shapes (degenerate skipped); `setupCharacter` swaps temp ground for `await game.loadColliders()` + `createStaticColliders`. Cube lands on real lot COL. 138 tests green. Spawn `[2495,-1675,16]`, camera frame distance 12. Watch: fall-through if no COL under spawn; load time (colliders for whole Ganton radius). **Iter 5:** Config `gameState`(default pause)+`controls`(WASD+Space); `game/input/keyboard.ts` (`Keyboard`+`KeyboardInput` stub seam); `Game.setGameState`(+`game-state` event)+`getConfig()`; `PhysicsSystem` steps only in play; `CharacterControllerSystem` (play+grounded via `castRay` → WASD planar velocity, Space jump; air keeps momentum); player uses `createCharacterBody` (locked rotations+friction); systems order controller→physics→render-sync; debug overlay **GAME: Play/Pause**. 143 tests green. Default changed to **gameState 'play'** (loads running; pause is a toggle) — pause-default confused the user. **Iter 6:** `CameraController.follow(target)` (translates camera by player movement, OrbitControls re-centred on it → mouse orbits/zooms around player); `Game.getCamera()`/`setFollowTarget()`; loop runs follow in play else free orbit. **Movement now camera-relative** (`camera.getWorldDirection`→ground→Z-up `(x,y,z)→(x,−z,0)`, right=fwd×up; W goes where camera looks). 143 tests green. **Iter 7 (camera rework):** `CameraController` is two-mode — **follow** (play): custom orbit on plain `pointermove` (no button), polar clamped to a hemisphere above the player (`Config.camera.followMin/MaxPolar`, never below floor), optional wheel zoom (`followZoom`), distance from config; **debug** (Ctrl+D): detaches top-down over district (`DEBUG_HEIGHT=250`), OrbitControls rotate-off + drag-pan X/Y (`LEFT=PAN`) + wheel dolly-down (`screenSpacePanning=false`). New `Config.camera`. `Game.setDebugMode`→camera `setMode`; `setFollowTarget`→`setTarget`; loop = `cameraController.update()`. **Plan 008 functionally complete (iters 0–7).** Optional leftovers: dispose Rapier/keyboard/ECS on Game.dispose (singleton = page life, low pri); respawn button; debug region-frame buttons are angled not top-down. Tunables: `LOOK_SENSITIVITY`, `DEBUG_HEIGHT`, `Config.camera` defaults in canvas-host.
