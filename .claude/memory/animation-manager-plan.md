---
name: animation-manager-plan
description: Plan 012 — animation manager (walk/run/jump/idle) driving Tommy's skeleton from ped.ifp (ANP3)
metadata:
  type: project
---

Plan: `.claude/plans/012-animation-manager/readme.md`. Drive Tommy's skeleton ([[character-model-plan]], `CharacterContext.skeleton`/`bonesByName`) with GTA SA animations from `static/anim/ped.ifp` — an animation manager that plays **idle/walk/run/jump** and crossfades by movement state.

**Verified everything is present:** `ped.ifp` is **ANP3** (294 anims, parses end-to-end). Locomotion clips exist: `WALK_civi, run_civi, sprint_civi, IDLE_stance, JUMP_launch/glide/land`. **IFP bone names match the skeleton exactly** (32/32 for WALK_civi: `Root, Pelvis, … R Toe0` after trim) → map tracks→bones by name. Root bone = frame type 4 (rot+translation), others type 3 (rot only). `static/data/animgrp.dat` = ped gait groups (hardcode `man` for now). `static/anim/anim.img/` = 133 activity IFPs (NOT needed for walk/run/jump; a WIMG packer is a secondary deliverable).

**ANP3 layout:** `"ANP3", u32 size; char[24] internalName, i32 numAnims; per anim {char[24] name, i32 numBones, i32, i32; per bone {char[24] name, i32 frameType, i32 frameCount, i32 boneId; per frame: i16 rot xyzw /4096, i16 time, if type==4: i16 pos xyz /1024}}`. Names have a trailing 3ds-max path after the NUL (read to first NUL).

**Iterations:** 1) `renderware/parsers/binary/ifp.ts` `parseIfp` (ANP3 → `IfpAnimation[]`, no three); 2) `renderware/three/build-anim-clip.ts` (→ `THREE.AnimationClip`, bone-name tracks, **strip root X/Y translation** since physics owns position); 3) `WorldAdapter.loadAnimations(ifpUrl)` + `animation-controller.ts` (AnimationMixer on the player wrapper) — browser-check forcing WALK to validate quaternion convention + time scale; 4) `character-animation.system.ts` state machine (planar speed via `physics.getLinvel` + `isGrounded`, no controller changes) → crossfade idle/walk/run/sprint/jump; 5) jump launch→glide→land sequence + polish; 6) secondary `scripts/pack-anim-img.mjs`. **Open (tune in browser, isolated in build-anim-clip):** IFP quaternion sign (conjugate?) + i16 time→seconds scaling.

**Iter 1 DONE:** `renderware/parsers/binary/ifp.ts` `parseIfp(buffer) → IfpAnimation[]` (ANP3; `rotation` i16/4096 file-order x,y,z,w, `translation` i16/1024 for type-4 root, `time` raw i16). Barrel-exported. Verified on real `ped.ifp`: 294 anims, WALK_civi 32 bones, **all quats norm≈1** (order/scale right), times monotonic 0,2,…,68 → seconds scale likely `/60` (≈1.13 s; tune in clip builder). 189 tests green.

**Iter 2 DONE:** `renderware/three/build-anim-clip.ts` `buildAnimationClip(anim, {timeScale=1/60, includeTranslation=false})` → `AnimationClip` of `QuaternionKeyframeTrack`s named `"<trimmedBone>.quaternion"`, times×timeScale. **Translation stripped by default** (WALK/run bake forward motion into root +Y: WALK 0→1.72, run 0→3.16, accumulating; X/Z flat — physics owns position → rotation-only/in-place). Quaternion file-order x,y,z,w (flip convention here if mirrored). Real WALK_civi → 32 tracks, 1.133 s. 191 tests green. **Retargeting deferred to iter 3** (needs skeleton): sprint/idle/jump root = `Normal` (not `Root`); `JUMP_launch` has `Spine 1`/`Spine 2` (space) vs `Spine1`/`Spine2`. Iter 3 retargets via `bonesByName` + normalized key `trim().toLowerCase().replace(/\s+/g,'')` with alias `normal→root`.

**Iter 3 DONE (code; browser pending):** `WorldAdapter.loadAnimations(ifpUrl)` (adapter: fetch→parseIfp→buildAnimationClip, keyed by lower name). `game/character/animation-controller.ts` `AnimationController(root, clips, bonesByName)` — `AnimationMixer` on the player wrapper; retargets every clip via exported `retargetClip(clip, bonesByName)` (normalized key + `normal→root`, clones, drops unmatched); `play(name, fade=0.2)` crossfades looping clips, `update(delta)` ticks. `character-animation.system.ts` ticks it. Bootstrap forces `walk_civi`. 193 tests green. **Browser checkpoint:** WALK in place upright? if mirrored/twisted → flip quaternion convention in `build-anim-clip` (negate xyz / reorder); if speed off → `timeScale` (default 1/60). **Iter-3 browser result:** walk plays correctly; orientation fix needed — the IFP clips already orient the skeleton to GTA Z-up (animated up = model +Z), so `TOMMY_PLACEMENT.rotation=[0,0,0]` (the old +90°X laid him on his back); `orientCharacter` simplified to rotation+scale+`offset` ([0,0,0.04]).

**Iter 4 DONE (code; browser pending):** `character-animation.system.ts` `CharacterAnimationSystem(controller, physics, body, halfHeight, character, config)` — per `update` (frozen unless `gameState==='play'`): `getLinvel` planar speed + `isGrounded` → `clipFor` (air→`jump_glide`; `<1`→`idle_stance`; `<18`→`walk_civi`; else `run_civi`) → `controller.play` (crossfade 0.2); then **face movement** `character.rotation.z=atan2(-vx,vy)` (default fwd +Y; runs after render-sync so it sticks). Walk-vs-run from one input: controller got `WALK_SPEED=10`/`RUN_SPEED=26` + `ControlsConfig.run?`=`ShiftLeft` (default walk, Shift=run). `CharacterContext` += `bodyHandle`, `halfExtents`. 193 tests green. **Browser ✅ — camera behind, walks facing forward, turns right, idle/walk/run/jump switch.** Facing fix: store `facing` (default `π` = face away from the free-orbit follow camera's −Z start side) and **apply `character.rotation.z=facing` every frame** (render-sync overwrites the wrapper rotation each frame from the body), updating it to `atan2(-vx,vy)` while moving. Foot offset `[0,0,0.04]`.

**Iter 5 DONE (code; browser pending):** jump sequence. `AnimationController.play(name, fade, loop)` (loop=false → `LoopOnce`+`clampWhenFinished`) + `duration(name)`. `CharacterAnimationSystem` state machine `ground→launch→glide→land→ground`: launch `JUMP_launch`(0.2s once)→ glide `JUMP_glide`(0.5s loop) after its duration or `vz<0`; grounded→ land `JUMP_land`(0.233s once) → ground after its duration. FADE 0.12. Durations: launch 0.2 / glide 0.5 / land 0.233 / idle 1.5 / walk 1.133 / run 0.733 s. 193 tests green. Iter-5 accepted (walk 3 / run 7 / jump 3.5 in `Config.movement`).

**Iter 6 DONE — PLAN 012 COMPLETE.** `scripts/pack-anim-img.mjs` (`npm run pack:anim`) packs the 133 `static/anim/anim.img/` IFPs **+ loose `ped.ifp`** (locomotion isn't in the folder) into a WIMG archive → **`static/anim/animations.img`** (folder is named `anim.img`, so output can't reuse that name; env `ANIM_SRC`/`ANIM_PED`/`ANIM_OUT`). 134 IFPs, 11.8 MB. `WorldAdapter.loadAnimations(archiveUrl, ifpName)` = `loadArchive`(cached, WIMG)+`archive.get(name)`+`parseIfp`; bootstrap reads `…/anim/animations.img` `'ped.ifp'`. Other IFPs load free from the cached archive later. 193 tests green. **Player speeds/jump in `Config.movement` {walkSpeed 3, runSpeed 7, jumpSpeed 3.5}**; anim thresholds derive from them (idle `<walk*0.35`, run `>(walk+run)/2`).
