---
name: world-streaming-plan
description: Plan 009 — GTA SA sectioned world streaming + LOD rendering (grid cells, dual draw distance)
metadata:
  type: project
---

Plan: `.claude/plans/009-world-streaming-lod/readme.md`. Render the map like GTA SA: full-detail (HD) models around the view, **LODs** ringing them, world split into **grid cells (squares)** that stream in/out as the view moves; **configurable draw distance** for HD models and for LODs. This is the **StreamingSystem** scaffolded in plan 006 (rides the `System` registry + player/camera view).

**SA reference:** per-instance `drawDist` (IDE, we parse `IdeObjectDef.drawDistance`) + **LOD links** (`IplInstance.lod` = index of the LOD stand-in instance; LOD models are `lod`-prefixed = `isLodModel`, with large drawDist). HD culled when far → its LOD draws → far = nothing. `CWorld` buckets the world into a sector grid. (Memory streaming N/A — our WIMG archive is fully in RAM.)

**Our design (grid streaming):** bucket exterior (`interior===0`) instances into `cellSize` cells, split HD vs LOD per cell; `buildCell(...)` builds/caches per-cell `InstancedMesh[]` (reuse build-region grouping/conjugated-quat). Each cell-crossing: cells within `hdDrawDistance` → HD, within `lodDrawDistance` (beyond HD) → LOD, else unloaded; StreamingSystem diffs desired vs loaded → `adapter.loadCell`/unload under one −90°X streaming root. Generic `game/streaming/{streaming.system,grid}.ts` (cell/ring math, no renderware) + `renderware/map/{world-grid,build-cell}.ts` behind the adapter (`loadCell(CellRequest)`+`cellSize`). New `Config.streaming` = `{ cellSize, hdDrawDistance, lodDrawDistance }` (defaults ~250/300/1500, tunable). Status: **planned, not started.**

**Debug UX (decided):** entering debug mode **suspends streaming** → StreamingSystem `manual` mode rendering an explicit cell set; debug overlay shows an **N×N checkbox grid** of cells around the current section (centre/current checked by default; tick extra sections to view alongside) + a single **"Show LODs"** toggle (HD vs LOD for checked cells). Leaving debug → resume streaming flow. Replaces the old `geometry: lods|map` + `camera: ganton/full-map` toggles. So StreamingSystem has two modes (`stream`/`manual`, follows `debugMode`) and exposes view cell + cellSize + `setManualCells(cells, lod)`.

**Iterations:** 1 world-grid (pure) → 2 build-cell → 3 grid ring math → 4 adapter loadCell seam → 5 StreamingSystem (stream+manual modes) + Config.streaming → 6 wire into loadGame + debug section inspector (checkbox grid + Show LODs) → 7 refinements (per-instance drawDist cull, precise HD↔LOD via lod index, hysteresis, per-frame build budget).

**Status: Iterations 1–2 DONE.** Iter 1: `renderware/map/world-grid.ts` — `buildWorldGrid(defs, cellSize)` → `WorldGrid = Map<cellKey, GridCell{cx,cy,hd[],lod[]}>` (exterior only, HD/LOD split via isLodModel); `cellKey`, `instanceCell`. Iter 2: factored `addToGroup` + `buildInstancedMeshes` out of `build-region.ts` (buildRegion reuses them, unchanged); `build-cell.ts` — `cellGroups(defs,cell,lod)` (pure select+group) + `buildCell(archive,defs,grid,cx,cy,lod)→InstancedMesh[]`. 153 tests green; no behaviour change. Iter 3: `game/streaming/grid.ts` (generic, no renderware) — `cellOf(pos,size)`, `cellsWithin(pos,radius,size)` (circular — nearest cell point ≤ radius), `cellKey`, `CellCoord`. 157 tests green. Iter 4: `WorldAdapter` gained `readonly cellSize` + `loadCell(CellRequest{cx,cy,lod})`; `GtaSaWorldAdapter` builds grid in `prepare`, `loadCell`→`buildCell` (native Z-up, no per-cell −90°X — streaming root applies it), cached by (cx,cy,lod); `GtaSaWorldConfig.cellSize` (bootstrap=250). 160 tests green; no behaviour change (loadCell unused). Iter 5: `game/streaming/streaming.system.ts` `StreamingSystem` (System) — **stream** mode (view cell → HD ring `hdDrawDistance` + LOD ring `lodDrawDistance` via `cellsWithin` → diff loaded → async `adapter.loadCell` add under streaming root / unload removes, no dispose since adapter caches) + **manual** mode (`setManualCells(cells,lod)`, active while `debugMode`). `Config.streaming{cellSize,hdDrawDistance,lodDrawDistance}`; `Game.getStreamingRoot()` (−90°X). Bootstrap: streaming defaults hd 300/lod 1500, shared `CELL_SIZE 250`. 164 tests green; **not registered yet** (no render change). **Iter 6 DONE (code):** **static path removed entirely** (`WorldAdapter.loadRegion`+impl, `buildRegion`+`RegionOptions`, `Game.worldObjects`) — kept `addToGroup`/`buildInstancedMeshes`/`RegionMeshData`/`RegionRequest`. Bootstrap creates+registers `StreamingSystem` (view = player `Transform` via `setupCharacter` `ctx.viewOf`); `Game`: `setStreamingSystem`/`setManualCells`/`getViewCell`, `pick` raycasts streaming root; `loadGame` = prepare + collision zone only. Debug overlay: **SECTIONS** 5×5 checkbox grid (around current cell) + **Show LODs** → `setManualCells`; enter debug = manual mode, leave = stream. Defaults `Config.streaming` hd 300/lod 1500/cellSize 250 (canvas-host). 164 tests green; **browser acceptance ✅ confirmed — streaming works, no lag.** **Plan 009 functionally complete (iters 1–6).** Iteration 7 (refinements: per-instance drawDist cull, precise HD↔LOD via lod index, hysteresis, per-frame cell-build budget, LRU cache) **DEFERRED** — not needed at current scale per the user. Out of scope for now: collision streaming (pairs later; collision stays fixed area around spawn from [[character-physics-plan]]), occlusion, DFF character swap ([[player-cube-placeholder]]).

**Cell fade-in (DONE, roadmap "Streaming fade objects"):** `game/streaming/fade.ts` `CellFader` ramps a
freshly-streamed cell's material opacity 0→original over 0.4s so sections don't pop. Wired in
`StreamingSystem`: `update(delta=0)` advances fades; on cell add → `fader.start(key, objects)` (skipped
when `config.mapViewer` — inspector shows instantly); on unload → `fader.cancel(key)` restores materials
before the cell mesh returns to the adapter cache. Safe because materials are **per-cell** (built fresh
in `buildClumpParts`, only Textures/clumps are shared) — verified. Translucent materials keep their
original opacity/`transparent`; opaque ones toggle `transparent` only for the fade (with `needsUpdate`),
restored on completion. `fade.test.ts` (5 tests).
