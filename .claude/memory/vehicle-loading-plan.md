---
name: vehicle-loading-plan
description: Plan 015 (DONE) — parse GTA SA vehicle data + render static painted cars (admiral, camper); formats + paint markers
metadata:
  type: project
---

Plan: `.claude/plans/015-vehicle-loading/readme.md`. Parse vehicle data + place two cars statically near Tommy's spawn — **admiral** (2-colour), **camper** (4-colour). No driving yet. Deferred phases (user): **dummy** (component system / wheel spin/steer / doors / lights / seats), **collision**, **physic** (driving from handling), **vehicle_vlo** (LOD), **damage** (ok/dam).

**Files (all verified present + parse):** `static/vehicles/{admiral,camper}.dff`+`.txd` (DXT1), `static/models/generic/vehicle.txd` (19 tex rgba8888, shared); `static/data/vehicles.ide`, `carcols.dat`, `handling.cfg`.

**Formats:**
- `vehicles.ide` `cars`: `id, model, txd, type, handlingId, gameName, anims, class, frq, flags, comprules, wheelModelId, wheelScaleFront, wheelScaleRear, upgradeClass`. **admiral 0.68/0.68, camper 0.66/0.66.**
- `carcols.dat`: `col`…`end` = **palette** (`R,G,B  # idx name` per line, index by order); `car`…`end` = per-car **2-colour** combos (`name, p,s, p,s,…` palette-index pairs); `car4`…`end` = **4-colour** (`name, c1,c2,c3,c4,…`). admiral=`34,34 35,35…`; camper=`1,31,1,0…`. Names lowercased; strip `#`.
- `handling.cfg`: one line per `handlingId` = ~30 space-separated physics fields (mass, drag, dims, centre-of-mass, %submerged, traction, gears, max speed…). **Parse into a dict now (`parseHandling`); physics uses it later.**

**Vehicle DFF structure (key):** frames = body root + **dummies** (`wheel_lf/rf/lb/rb_dummy`, `chassis_dummy`, `door_*_dummy`, `bonnet/boot/bump/windscreen_dummy`, `engine`, `exhaust`, `ped_*seat`) + component atomics with **`_ok`/`_dam`** variants + **`chassis_vlo`** (LOD) + a **single `wheel` atomic** instanced at the 4 wheel dummies. **Paint markers in material colour: `(60,255,0)` = PRIMARY, `(255,0,175)` = SECONDARY** → replace with the carcol palette colour (tinting the texture). Note: generic `buildMaterial` forces white when textured → need a **vehicle-specific material builder** so paint tints show. The car DFF references textures from BOTH generic `vehicle.txd` AND its own `.txd` → **merge both texture maps**.

**Iter 1 DONE:** `parseHandling`/`parseVehicleDefs`/`parseCarcols` in `src/renderware/parsers/text/` (+ barrel + tests, all green on real files). handling fields kept **raw `string[]`** keyed by id (col→meaning mapping deferred to the physics phase); `parseHandling` reads only the standard car table (skips `;`/`!`/`$`/`%` lines). `parseVehicleDefs` keyed by lowercased model name (wheel cols 11/12/13). `parseCarcols` strips inline `#`, lowercases names.

**Iter 2 DONE:** `src/renderware/three/build-vehicle.ts` `buildVehicle(clump, textures, {primary, secondary, wheelScale})` → Group (native Z-up, no Y-up flip — caller's streamingRoot rotates). Body = chassis + `*_ok`, **skips `*_dam`/`*_vlo` + bare `wheel`** atomic; each body atomic placed by its **frame world transform** (parent-chain compose, cached). Wheel instanced at the 4 `wheel_*_dummy` frames (regex `^wheel_(lf|rf|lb|rb)_dummy$`), scaled front/rear, right side rotated π about Z to mirror. Paint via `buildVehicleMaterial` = reuse `buildMaterial` then override `material.color` for markers (primary `(60,255,0)`/secondary `(255,0,175)`) so the texture is tinted not whited. Exported `buildGeometry`+`frameMatrix` from build-clump for reuse. Verified real admiral/camper have all 4 dummies + bare `wheel` + `_ok`/`_dam`/`_vlo`. 8 tests green. **Mirror rotation (Z, π) is a guess — verify rim orientation in browser at iter 3.**

**Iter 3 DONE (browser pending):** `GtaSaWorldAdapter.loadVehicle(modelName)` added (+ to WorldAdapter interface). Lazy-caches `ensureVehicleData()` (vehicles.ide/carcols.dat/handling.cfg), `loadGenericVehicleTextures()` (shared `vehicle.txd`), `resolveVehicleColours()` (first `car` combo, else first `car4` combo's c1/c2 → palette RGB, white fallback). Merges generic + car TXD, `buildVehicle`. `canvas-host.tsx` `VEHICLE_PLACEMENTS` loads admiral+camper, sets `position`/`rotation.z`, adds under `getStreamingRoot()`. Verified headless on real files: admiral 11 body+4 wheels (paint grey [100,100,100] — its first combo uses one palette idx for both), camper 12 body+4 wheels (primary [245,245,245], secondary [95,39,43]); markers applied to ~12/~11 mats. Real marker colours confirmed: admiral has (60,255,0)×23 only (no secondary); camper has (60,255,0)×11 + (255,0,175)×10. **Extra single-count marker-ish colours exist ((255,175,0),(185,255,0),(0,255,200),(255,60,0); camper also (0,255,255)×8,(124,124,124)×9) — likely colour3/4 + special parts; deferred.** All 222 tests + vite build green. **TO VERIFY IN BROWSER:** car z-height (13.4 guess), heading, wheel right-side mirror (Z,π guess), positions clear of garbage/poles.

**Iter 3 browser fixes:** (1) **Glass transparency** — vehicle glass = material with **colour alpha 128** + texture `vehiclegeneric256` (admiral on `chassis` frame, camper on `windscreen_ok`). Shared `buildMaterial` derives transparency from `map.userData.hasAlpha` (false for vehiclegeneric256) and ignores colour alpha → glass was opaque black over the headlights. `buildVehicleMaterial` now: if `rw.color[3] < 255` → `transparent=true, opacity=alpha/255, alphaTest=0, depthWrite=false, side=DoubleSide`. (2) **Wheel size** — `WHEEL_SCALE_BOOST = 1.15` multiplier in `addWheels` (vehicles.ide scale read a bit small; can't edit the .ide). (3) **Textures were fine all along** — every DFF texture ref resolves (admiral92*/remapcamperbody256 from car txd, vehicle* + carp*/plate* from generic vehicle.txd); `map:Y` confirmed on headlights/tread/disc. The black headlights were the opaque glass panel in front. **`wheels.txd` NOT needed** — it parses to 0 textures; tread `vehicletyres128` is in generic vehicle.txd, disc `admiral92wheel64` in car txd. **Still to verify in browser:** ~~right-wheel mirror~~ (fixed), car z/heading. WHEEL_SCALE_BOOST=1.2 (accepted). **Wheel mirror:** all dummy frames are identity rotation, +X=right/−X=left; the wheel atomic is modelled facing out on the **right (+X)** side, so the **LEFT** copies get the π-about-Z spin (not the right — that put all 4 facing inward).

**Custom/modded DFF fix (BinMeshPLG material recovery):** some exporters (custom mods) write **materialIndex=0 on every face** in the triangle list and store the real per-material split only in **BinMeshPLG (0x50e)** → without it the whole model renders with material 0's texture ("all the textures got scrambled"; e.g. a custom admiral showed the lights texture on 57% of faces). `parseDff` now, when a multi-material geometry's triangle list is **all-zero**, parses the geometry-extension BinMeshPLG and reassigns each face's material by keying triangles on their order-independent vertex triple (`applyBinMeshMaterials`/`forEachSplitTriangle`, handles trilist + tristrip). Stock DFFs (varied indices, e.g. admiral2) are untouched. **General parser improvement — helps the map too.** Tests: 3 cases in dff.test.ts (`buildBinMeshClump`).

**Vehicle COLLISION DONE (browser-confirmed: aligns with the body):** vehicle collision is **embedded in the DFF** — a Collision plugin chunk `0x253f2fa` (`RwSection.COLLISION`) inside the Clump's Extension, wrapping one COL2/COL3 model (admiral2=`admiral_col` 16v/12f/18sph; camper=`camper_col`; custom admiral=`slamvan_col` 128v/104f/23sph — trimesh + spheres, no boxes). New `parseDffCollision(buffer)` in `col.ts` walks clump→extension→0x253f2fa→`parseColLibrary(slice)[0]`. `loadVehicle` now returns **`VehicleModel { object, colliders }`** (colliders = model-space `ModelColliders` via `toModelColliders({col, name, transforms:[]})`, or null). `canvas-host` builds a placement `Matrix4` (position + Z heading) and calls `character.physics.createStaticColliders([{...colliders, transforms:[matrix]}])` → fixed bodies (Z-up physics; display root's −90°X is visual-only so they align). Tests: 3 cases in col.test.ts (`dffWithCollision`). Cars are now solid obstacles. **dummy phase (wheel spin/steer/doors/lights) still deferred** — user chose collision first.

**DUMMY phase — wheel rig (API) DONE:** component framework + wheel spin/steer. `buildVehicle` now returns **`BuiltVehicle { root, wheels }`** (was Group); each wheel is wrapped **pivot (dummy pos/orient) → spinner (rig-rotated) → mesh (mirror+scale, centred)** so spin pivots on the axle. `BuiltWheel { front, radius, spinner }` (radius = geometry boundingSphere × scale). New `game/vehicle/vehicle-rig.ts` **`VehicleRig`**: `setSpeed`/`setSteer` + `update(dt)` — rolls all wheels about local **X (axle)** by `-distance/radius`, steers front wheels about **Z (up)** (`q = steer × spin`). New `game/vehicle/vehicle.system.ts` **`VehicleSystem`** (System) updates all rigs/frame. `loadVehicle` → `VehicleModel { object, colliders, rig }`; canvas-host registers a `VehicleSystem` + each rig. **Speed/steer stay 0 until vehicle physics drives them — wheels idle in-browser now (by design), API unit-tested** (build-vehicle: 4 wheels/front flags/scale nested; vehicle-rig: spin=distance/radius, front steer, rear no-steer, accumulation). Steer/spin sign may need a flip once driving lands. **Still deferred: doors/lights/seats (same pivot pattern), driving (physic).**

**Design:** `parseHandling`/`parseVehicleDefs`/`parseCarcols`; `build-vehicle.ts` `buildVehicle(clump, textures, {primary, secondary, wheelScale})` — body (chassis + `*_ok`, skip `*_dam`/`chassis_vlo`/bare `wheel`), wheels instanced at the 4 `wheel_*_dummy` frames (scaled front/rear), paint markers; `WorldAdapter.loadVehicle(modelName)` (resolve def+colours, merge txds, build); bootstrap places admiral+camper near `PLAYER_SPAWN` (Ganton lot, clear of garbage/poles, sit on ground) under `streamingRoot` (−90°X, static). Iterations: 1) parsers; 2) buildVehicle; 3) adapter+placement. Wheels placed NOW (recognisable car); full dummy/component system + first carcol combo only + 4th-colour deferred.

**Initial paint override:** `loadVehicle(modelName, colour?)` accepts carcols palette indices as a string
(e.g. admiral `'34,34'`, camper `'1,31,1,0'`) — first two indices = primary/secondary (build-vehicle has
only 2 paint markers, so extra indices are ignored). `resolveVehicleColours(name, indices?)` resolves them
via the carcols palette, else falls back to the car's default combo. `VehiclePlacement.colour` threads it
through `spawnVehicle`/LOD-respawn; canvas-host `CAR_COLORS` map sets parked + debug-spawned cars.

**4-colour paint:** `buildVehicle` now supports all four GTA SA carcol paint markers — primary
`(60,255,0)`, secondary `(255,0,175)`, tertiary `(0,255,255)`, quaternary `(255,255,0)` (3rd/4th
fall back to 1st/2nd if not supplied). `VehicleOptions` gained optional `tertiary`/`quaternary`;
`resolveVehicleColours` returns all four (`VehiclePaint`) from a car4 combo or explicit indices
(e.g. camper `'0,6,3,0'`). Also fixed: paint is set with `color.setHex(...)` (sRGB, like buildMaterial),
not `setRGB(.../255)` (linear) — the old way rendered paint washed-out/too light.
