---
name: vehicle-physics-plan
description: Plan 018 — replace arcade drive with real Rapier dynamic-vehicle physics (COL collision, gravity, GTA-style)
metadata:
  type: project
---

Plan: `.claude/plans/018-vehicle-physics.md`. **User directive:** the car must be a real **dynamic physical body** whose collision **is the model's COL** (reused later for damage — hit hood → hood damaged; COL is unique per car, must not be replaced), with real **gravity** + suspension, "like GTA SA". The arcade-kinematic drive ([[vehicle-driving-plan]] iters 1–3) is the wrong base and gets replaced.

**Approach = Rapier `DynamicRayCastVehicleController`** (maps onto GTA's model): chassis = dynamic rigid body (mass from handling.cfg); **chassis collider = convex hull of the COL vertices** (dynamic bodies can't collide with the static world trimesh using a concave trimesh — two trimeshes generate no contacts in Rapier → car falls through; convex hull is the same model geometry, just convex; full COL kept for damage); 4 **raycast wheels** at `wheel_*_dummy` (suspension/radius from handling) → gravity rests the car on wheels (no hover/settle/snag hacks needed); `setWheelEngineForce`/brake/`setWheelSteering` from handling.cfg + WSAD; `updateVehicle(dt)` each fixed step; render copies chassis transform → car object + wheel transforms → rig. API verified present (world.createVehicleController, addWheel, etc.).

**Removes** the `drive`/`moveCar`/`stopping`/kinematic-box/sensor/`createKinematicVehicle`/`groundHeight`/settle machinery from EnterVehicleSystem. Get-in/out kept but the seated player rides a **dynamic** chassis (sync to a chassis seat point; player collider off while seated).

**Iterations:** 1) dynamic chassis + raycast wheels + gravity (cars rest on wheels, collide with world, no input). 2) engine/brake/steer from handling.cfg (per-car feel from real mass/power). 3) get-in/out on the dynamic car. 4) polish + damage contact hooks.

**Iter 1 DONE (awaiting browser check):** `PhysicsWorld.createDynamicVehicle(pos, heading, hullVertices, mass, wheels)` builds a dynamic chassis (convexHull of COL verts, box-hull fallback, `setMass(handling.mass)`, CCD, damping) + `world.createVehicleController` with 4 raycast wheels at the `wheel_*_dummy` hubs (suspension consts in physics-world; parking brake holds parked cars). `PhysicsWorld.step` now runs `updateVehicle(dt)` for all controllers before `world.step`. New `VehiclePhysicsSystem` syncs body→object transform + live `position`/`heading` on the shared `EnterableVehicle` and rolls wheels from `currentVehicleSpeed`. Removed kinematic `createKinematicVehicle`/`moveCar`/`groundHeight`/settle/box-sensor + arcade `drive`/coast from EnterVehicleSystem (it now reads live pos/heading and the seated player rides the dynamic body; get-in/out kept). `BuiltWheel.connection` added (hub pos). `VehicleModel`: dropped `colliderBox`/`rideHeight`, added `wheels`. Deleted `vehicle.system.ts` (folded into VehiclePhysicsSystem). All green: tsc + eslint + 250 tests + build.

**Iter 1 tuning gotchas (resolved, verified offline via a throwaway vitest that sims a car on a static box with admiral's real geometry):**

- The chassis hull bottom (COL minZ) sits only ~0.13 m (admiral) / 0.26 m (camper) above the wheel contact. If suspension equilibrium compression exceeds that margin, the hull sinks into the road / tips. → keep suspension stiff enough that compression is small. Final: stiffness 120, rest 0.15, travel 0.25, maxForce 40000, damping comp 4.4 / relax 2.3 (Bullet-standard), frictionSlip 10.5. Admiral then rests with hull ~0.12 m clear.
- **`getLinvel` on a raycast-vehicle body returns a PHANTOM velocity** (~0.95 vz on flat ground) that `updateVehicle` writes each frame — the body does NOT actually move (zOsc≈0). Don't use it (nor likely `currentVehicleSpeed`) for wheel roll: on a tilted trimesh the phantom has a horizontal component that spins parked wheels. → VehiclePhysicsSystem now rolls wheels from the car's **real planar displacement** frame-to-frame (≈0 at rest), deadzone 0.1 m/s.

**Iter 1 also: chassis collider = COL spheres+boxes (NOT a single convex hull).** A single hull bridges over the concave hood (Tommy floated ~25cm above it). admiral COL = 23 spheres + trimesh, 0 boxes; camper = 20 spheres. `addVehicleHull` builds ball/cuboid colliders from the COL spheres/boxes (spreading mass by density so body mass = handling.mass), skips the trimesh (kept for damage), convex-hull fallback only if no primitives. Spheres hug the contour → standing on hood/roof is correct. Confirmed in browser.

**Iter 2 DONE (awaiting browser feel-tuning):** driving via `EnterVehicleSystem.fixedUpdate` while seated. `PhysicsWorld.setVehicleControls(controller, wheels, engineForce, brake, steer)` (engine split across all wheels = 4WD, brake all, steer front) + `parkVehicle(controller)` (engine 0, brake PARKING_BRAKE, straight). `drive()`: WSAD → engine toward a handling-scaled top speed (governor via `vehicleSpeed`), S = brake when moving forward / reverse at rest, off-throttle = light coast brake; front-wheel steering slewed (STEER_RATE) with lock shrinking toward top speed; `rig.setSteer` for visuals. Constructor regained `config` (controls). On exit: `parkVehicle` + steer reset; on seat: steer reset. Tuning consts in enter-vehicle.system.ts (ENGINE_ACCEL_SCALE 0.35, BRAKE_SCALE 0.6, MAXVEL_SCALE 0.25, REVERSE_FRACTION 0.4, etc.) — expect in-browser tuning of feel + **steer sign** (guessed; may need flip). 7 driving unit tests added (physics stubbed). All green: tsc+eslint+257 tests+build.

**Iter 2 polish (browser-driven, all confirmed except camper start-pop pending):**

- Seated rider runs in `EnterVehicleSystem.fixedUpdate` (after physics step, before render-sync) reading the body fresh → no fore/aft jitter. Camera follows the **car object** while seated (player mesh aliases due to double Transform writes + variable fixed-step count). Rider gets the car's **full orientation** via `setScripted(clip, { orientation:[x,y,z,w] })` (new option in character-animation.system) + seat placed via full transform → tilts/flips with the car (was sitting level → clipping out).
- Steering sign flipped (`-steerInput`); eased slew `STEER_RATE=1.2` (turn-in) / `STEER_RECENTER_RATE=2.4` (return) — was snapping.
- **Space = brake/handbrake** (`controls.jump`).
- Brake was applied per-wheel (4× torque) → nose-dive; `setVehicleControls` now splits brake across wheels too.
- **Reverse from a dead stop didn't translate** (Rapier raycast quirk; forward works): use the body's **horizontal velocity** for the speed decision (not phantom `currentVehicleSpeed`≈0.95 at rest) AND `PhysicsWorld.seedReverse` nudges a small backward velocity until rolling.
- **Start hop**: was suspension rebound (~4.4cm); `SUSPENSION_COMPRESSION` 4.4→12 → ~2.7cm. Engine force also ramped (`ENGINE_RAMP_TIME`).
- **Camper tippy/sideways pop**: first tried explicit low-COM + box-inertia mass properties → REVERTED (box inertia too low → camper flipped, admiral brake-dive returned). Back to **volume-density** mass (good admiral feel). Instead tamed via **`CHASSIS_ANGULAR_DAMPING` 0.6→2.0** (resists pitch-dive, roll-flip, AND over-sharp yaw) + **`STEER_LOCK_SCALE=0.6`** (gentler turn radius). Offline turn rate ~halved (admiral 135→62°/1.5s, camper 86→37°), rest/accel preserved, flat-ground start tilt ~0. Camper sideways pop is environment-specific (NOT reproducible on flat ground in vitest) — if it persists in-browser, suspect the camper's parking spot overlapping world geometry (investigate placement/move it). Tommy seated FULL orientation works (tilts/flips correctly — user confirmed).
- **Brake nose-dive (37° admiral / 20° camper!) FIXED**: Rapier's `setWheelBrake` value is a SMALL-scale constraint value, NOT a Newton force like engine. My `mass*brakeDecel*0.6` gave ~2100/wheel which over-constrains and pitches the body ~37°. Sweep: ≤150/wheel → strong decel (≈12 m/s²) with <1° dive; ≳400/wheel → violent dive. Now `BRAKE_FORCE=480` total (≈120/wheel) scaled by `brakeDecel/8.5`. Verified: 13→0 m/s with 0.6° pitch. (The dive had become MORE visible because Tommy now tilts with the car.) Note PARKING_BRAKE=80/wheel was already in the good range. Engine force (~2500/wheel) is fine — only brake is scale-sensitive.
- Camper start sideways-pop STILL present (lighter, no flip now with angular damping 2.0). NOT reproducible offline on flat ground. Camper spawn raised to z=15 (admiral 13.4) to test spawn-penetration theory — pending browser.
- **Flipped-car re-entry FIXED**: door routing is yaw-only so an overturned car had no reachable door → couldn't get back in. Added `isUpright` (car local +Z · world up > `UPRIGHT_MIN` 0.6); if not upright, `beginApproach` walks to the car itself and `startSeatedDirect` climbs straight in (no door swing/slide). (Still KNOWN minor: EXITING a flipped car can place you on the wrong side — yaw-only doorway.)
- **Start-pop NOT solved**: confirmed NOT spawn penetration (raising camper to z=15 didn't help — reverted, camper now at [2496,-1678,13.4] beside admiral). NOT reproducible offline on flat OR a 3.4° slope (always ~2.6cm hop, no sideways), and game uses the same 1/60 step → it's tied to the **streamed lot trimesh** under the cars (a curb/seam; wheel rays crossing triangle edges on launch). admiral ~20cm in-place, camper big sideways in-browser. Softened launch (`ENGINE_RAMP_TIME` 0.25→0.6) made control feel "toy-like" (floaty throttle) → reverted to 0.2. Camper jumps the SAME at the new spot (beside admiral) → confirmed camper-SPECIFIC, not the exact spot. Switched chassis mass from volume-density to **EQUAL mass per COL shape** (so the camper's oversized front sphere r0.93 can't drag the COM high/forward → less wobble; each shape keeps real inertia, unlike the failed box-inertia attempt). Offline preserves admiral feel + turn 64°/40°. Player↔car collision ruled out as the cause (group-ignored from Enter-press + fully disabled at climb-in + still off while seated; verified offline disp 0.000). Tommy "rides back-and-forth in camper" = the camper's own wobble now visible because he inherits full orientation. STILL pending browser confirm; if equal-mass doesn't fix it, next is an in-browser debug log of the camper body angVel/tilt on launch (can't see it offline).

**Start-pop — strongest hypothesis = CCD (FIX APPLIED, pending confirm).** User clarified: the pop is "from the FRONT", on BOTH cars, EVERY surface, always at start (and camper sometimes on exit), camper stronger. Ruled out offline: car launch physics (per-step pitch 0.2–0.5°, body z stable), player→car push (controller-walk moves car 0.007m with ignore on/off; teleport-jam 0.000). The "leading-edge, any-surface, not-reproducible-on-simple-ground" signature = **`setCcdEnabled(true)` on the chassis** sweeping the front edge against the detailed streamed world trimesh → catches on triangle seams → pop. Removed CCD (chassis spheres are big + per-step motion small → no tunneling risk at these speeds). Also reverted ENGINE_RAMP 0.6→0.2 (the 0.6 felt "toy-like"). equal-mass distribution kept. If CCD removal fixes it → done; if not, the remaining in-game-only difference is the game loop / streamed collision and we add an in-browser angVel/z debug log.

- CCD removal: turned the start-pop from "sideways/from-front" into a smaller VERTICAL in-place hop (admiral small, camper still "quite strong"). So CCD was part of it; a residual suspension launch-hop remains (couldn't repro offline; per-step pitch 0.2–0.5°). LEFT as-is — user declared the current handling "the best ever, keep it", so NOT touching engine/brake/steer/damping/mass/suspension. Pursue the residual camper hop only via an in-browser angVel/z debug log if asked.
- **EXIT-at-speed pop CONFIRMED + FIXED**: user reproduced admiral flying sideways when exiting a moving car (player collider re-enabled inside the still-moving car) + Tommy lagging to cabin centre (door target computed once while car drives on). Fix = reinstated **coast-to-stop**: Enter while seated → phase `'stopping'` (full brake, rider stays seated) → when speed ≤ `STOP_THRESHOLD` 0.8 → `startExit`. Car is stationary at exit → no pop, door target stable.
- **Flipped re-entry FIXED properly**: previous version ran to the car centre (never arrived → Tommy stuck running). Now `beginApproach` on a non-upright car calls `startSeatedDirect` immediately (gate + seat, no walk). Removed `flippedEntry` flag + approaching branch.
- User confirmed: flipped seated orientation correct; handling best-ever (keep).
- **Flipped car: now REFUSE entry** (user wants: can enter only if on wheels / at least upright-ish; NOT if on side/flipped). `beginApproach` skips non-upright cars (`isUpright` filter in the nearest search); removed the direct-seat path.
- **Start-jump ROOT CAUSE FOUND + FIXED = chassis falling ASLEEP while parked.** Debug log (browser) showed camper launching at vz=3.52, |ang|=2.14 with wheels still in contact (then ballistic), only on the FIRST drive after entering / re-entering, never on stop-and-go. A dynamic body sleeps when parked a while; while asleep the raycast **suspension forces aren't applied → it slowly sinks into the collision**; the first throttle wakes it and the penetration **ejects it**. Fix: **`RigidBodyDesc.setCanSleep(false)`** on the chassis (verified offline: park 50s then launch → vz 0.00; was 0.60 on flat box, much bigger on the real trimesh). Debug log + `getAngvel` removed afterwards.
- **Gotcha:** `setColliderEnabled(false)` does NOT stop a kinematic body (player) from shoving a dynamic one (offline: still 11.39 vz) — only **collision groups** (`ignoreVehicles`) do (0.76). The seated player is already protected by `ignoreVehicles` (from beginApproach); the `setColliderEnabled` calls are belt-and-suspenders but ineffective for that purpose. **[2026-07-18: this note was RIGHT and the method survived anyway for months — plan 077's unit tests re-found it (it reports `isEnabled()===false` while still blocking) and it is now DELETED. Lesson: a 'gotcha' recorded in a log is not a fix.]**
- Flipped/on-side car: **entry refused** (can only enter if upright-ish; `isUpright` filter in beginApproach).

**ENTRY/EXIT POP — RESOLVED (user confirmed "excellent").** Long hunt. The pop was the **kinematic player body shoving the dynamic car while sliding into/out of the seat**. Critical finding: NO collider-state trick suppresses the kinematic→dynamic push IN-GAME — `setColliderEnabled(false)`, collision groups (`ignoreVehicles`), and `setColliderSensor(true)` ALL work in isolated vitest sims (push→0) but ALL failed in the running game (camper still popped). Root unknown (some game-loop/integration difference), do NOT trust these to stop a kinematic body pushing a dynamic one in-game. The body-stays-out approach (moveBody:false, never teleport the body into the car) fixed the pop but broke render/exit/controls (body↔render divergence) — reverted. **Final fix: PIN the car during the getin/getout slides** — `EnterVehicleSystem.storeHold` snapshots the parked car transform at startGetin/startGetout; `advanceGetin`/`advanceGetout` call `PhysicsWorld.holdBody(body, pos, quat)` each (fixed-step) frame, which sets translation+rotation and zeroes lin/ang velocity. A pinned car can't be shoved. Seated driving is NOT pinned (untouched). Also: coast-to-stop before exit (phase 'stopping'), seated rider + slides run on the fixed step, follow camera tracks the car object while seated, ENGINE_RAMP 0.2, BRAKE_FORCE 480 (Rapier brake is small-scale not Newtons!), setCanSleep(false) (sleeping chassis sank→launch pop), low engine-launch. Tommy inherits full car orientation (tilts/flips). Debug logs removed. 258 tests green.

**Driving (plan 018) is in a GOOD state — user: "best handling, excellent". Don't change handling consts without reason.**

**iter4 DAMAGE (implemented, pending in-browser calibration):**

- **Collision bounce**: chassis colliders got `setRestitution(0.35)` + `RestitutionCombineRule.Max` (car bounces off walls instead of freezing — user wanted "a little bounce back").
- **Impact events**: vehicle chassis colliders get `setActiveEvents(CONTACT_FORCE_EVENTS)` + `setContactForceEventThreshold(400)`; `PhysicsWorld.step(eventQueue)` drains contact-force events into `impacts[]`; `takeImpacts()` returns+clears `Impact { bodyA, bodyB, force, direction }`. `collider.parent()?.handle` maps collider→body.
- **build-vehicle**: now pairs `<prefix>_ok`/`<prefix>_dam` atomics → `BuiltPart { name, ok, dam(hidden), pivot, position }` (panels wrapped in a pivot at the world transform; doors keep their hinge pivot + get a dam mesh). New `parts: BuiltPart[]` on BuiltVehicle. Damageable parts on admiral/camper: bonnet, boot, bump_front/rear, windscreen, doors, plate_rear. Refactored to `collectDamGeometry` + `addBodyAtomic` (cognitive-complexity). build-vehicle.test updated (parts are pivots now; `bodyMaterials` helper reads `parts.find('chassis').ok`).
- **`VehiclePart`** game type (game/vehicle/vehicle-part.ts) mirrors BuiltPart; `VehicleModel.parts`; adapter passes `built.parts`.
- **`VehicleDamageSystem`** (game/vehicle/vehicle-damage.system.ts): each update drains impacts; for a strong impact (`force >= STRONG_HIT`) on a car, finds the part most in the hit direction (hit dir = −forceDir in car-local frame; `PART_HIT_MIN_DOT` 0.15) → swaps `_ok`→`_dam`; a 2nd strong hit on an already-damaged part DETACHES it (`car.object.parent.attach(pivot)` to keep world transform, falls under gravity + tumble, removed after `FALL_TTL` 1.5s). canvas-host wires `vehicleDamage.add({body, object, parts})`.
- **Plan written: `.claude/plans/019-vehicle-damage.md`** (iterations 1-3 done, 4 = calibration+mapping now, 5 = polish later).
- **Calibration (from user):** contact forces — curb 1.4k, super-light 76k, light 207k, strong 377k. Set `STRONG_HIT = 300000` (light touches don't deform; real crashes do).
- **Hit-mapping FIX:** force _direction_ was unreliable (rear hit deformed the FRONT). Now use the **contact point** — `PhysicsWorld.step` queries `world.contactPair` → `manifold.solverContactPoint(0)` (world space) into `Impact.point`; the damage system maps it to car-local and picks the **nearest** part by distance. Also **dedup**: one state change per part per frame (a multi-contact crash can't deform-then-detach the same panel instantly). **CONFIRMED working in-browser (user: side correct, threshold good); debug log removed.** `solverContactPoint` IS world-space. Iteration 4 done — damage system complete: strong hit deforms the right panel, second hit detaches it (falls + removed after 1.5s), walls bounce. Iteration 5 (polish: per-part thresholds, debris/smoke, deform feel) deferred. Constants in vehicle-damage.system.ts (STRONG_HIT 300000, FALL_TTL 1.5) + physics-world.ts (CHASSIS_RESTITUTION 0.35, CONTACT_FORCE_THRESHOLD 400).

Known deferred (user OK): exiting a FLIPPED car puts the player on the wrong side (yaw-only door routing).

- After setCanSleep fix: admiral fully clean. Camper now pops only at **exit** (to the right-front = player capsule on the left/driver side shoves the car away as collision re-enables). Cause: `finishExit` restored `ignoreVehicles(false)` while the player still overlapped the camper's body. Fix: **defer restoring car collision until the player has stepped clear** — `finishExit` sets `restoreWhenClear=true` (keeps ignoring vehicles); `update()` restores `ignoreVehicles(false)` once `playerClearOf(car)` (player outside footprint + `VEHICLE_CLEARANCE` 0.6). `beginApproach` resets the flag. (Entry should already be protected by ignoreVehicles-on; if the user still reports an entry pop, add a targeted entry log.) 258 tests. NEXT: to pin it, either probe the actual streamed collision Z under the car spot, or try resting the cars on a flat static pad, or sample wheel suspension lengths in-browser on launch.

handling.cfg note: admiral driveType F mass 1650 engAccel 22; camper R mass 1900 engAccel 16. (We drive 4WD regardless for traction.)

**Remaining:** confirm camper start-pop; iter3 polish get-in/out on the moving dynamic car; iter4 damage contact hooks. Tuning consts (suspension + driving forces, steer sign) expected to need in-browser passes.
