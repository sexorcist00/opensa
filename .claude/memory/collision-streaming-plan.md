---
name: collision-streaming-plan
description: Plan 010 — stream static COL collision per grid cell (same grid as render), around the player
metadata:
  type: project
---

Plan: `.claude/plans/010-collision-streaming/readme.md`. Make static map collision **stream per grid cell** (the render grid from [[world-streaming-plan]]) around the player, replacing the fixed Ganton zone built once at spawn (plan 008 / [[character-physics-plan]]). Cells gain Rapier static bodies when near, drop them when far → collision works everywhere the player drives.

**Reuses:** `buildCollisionIndex` (name→ColModel), `buildColliders` (bind by name, conjugated-quat, Z-up), `toModelColliders` (adapter DTO), `WorldGrid` (already built in the adapter for render), `cellsWithin` grid math. Physics stays GTA Z-up.

**Design:** (1) factor `bindColliders` out of `buildColliders`; add `buildCellColliders(index, defs, grid, cx, cy)` (cell HD instances only — LODs have no collision); (2) `WorldAdapter.loadCellColliders(cx,cy)` cached; (3) `PhysicsWorld.createStaticColliders` returns **body handles** (`number[]`, was count) + new `removeBodies(handles)` (`world.removeRigidBody`); (4) `CollisionStreamingSystem` (game/streaming) — view cell → desired = HD cells within `collisionDrawDistance` → diff `Map<cellKey,handles>` → create/remove Rapier bodies, async load with a margin radius (colliders ready before the player arrives); (5) `Config.streaming.collisionDrawDistance` (~150, small + margin); (6) `setupCharacter` registers it instead of the one-shot `createStaticColliders`, remove the fixed `Game.loadColliders`/`CollisionWorld` path.

**Iterations:** 1 `buildCellColliders` → 2 PhysicsWorld handles+`removeBodies` → 3 adapter `loadCellColliders` → 4 `CollisionStreamingSystem` (diff) → 5 wire-in + remove fixed zone + browser acceptance (drive far, cube lands everywhere). Decisions: separate system (not extend render StreamingSystem); separate small `collisionDrawDistance`; run on `update` with margin; Show Collision stays region-based for now; remove `CollisionWorld`/`loadColliders`.

**Status: Iteration 1 DONE.** `renderware/collision/build-cell-colliders.ts` `buildCellColliders(index, defs, grid, cx, cy)` (cell HD instances → `RegionColliders[]`); factored `groupInstanceByModel` + `bindColliders` out of `buildColliders` (unchanged). 168 tests green; no behaviour change. Iter 2: `PhysicsWorld.createStaticColliders` → returns body **handles** (`number[]`, one body per placement; empty placements create none); new `removeBodies(handles)` (`world.removeRigidBody`). Tests updated + removeBodies test. 168 tests green; no behaviour change (setupCharacter still builds fixed zone, return ignored). Iter 3: `WorldAdapter.loadCellColliders(cx,cy)` + `GtaSaWorldAdapter` impl (`buildCollisionIndex`+`buildCellColliders`→`toModelColliders`, cached by cell via `colliderCache`). 170 tests green; no behaviour change. Iter 4: `game/streaming/collision-streaming.system.ts` `CollisionStreamingSystem` (System) — view cell → desired cells within `collisionDrawDistance` (`cellsWithin`) → diff `Map<cellKey, handles>` → async `loadCellColliders`+`createStaticColliders` (track handles) / `removeBodies`. `Config.streaming.collisionDrawDistance` (default 150, bootstrap+test configs). 172 tests green; not wired yet. **Iter 5 DONE (code):** bootstrap registers `CollisionStreamingSystem(adapter, ctx.physics, ctx.viewOf, config)`; `setupCharacter` dropped the one-shot collider build. **Removed fixed-zone path entirely:** `Game.loadColliders`/`getCollisionWorld`/`collisionWorld`, `CollisionWorld` class+test+barrel export, `WorldAdapter.loadColliders`+impl (kept `buildColliders`/`loadCollisionDebug` for Show Collision, `toModelColliders` for cell colliders). 167 tests green; **browser acceptance ✅ confirmed — cube drives on the ground everywhere, collision streams with the player.** **Plan 010 COMPLETE (iters 1–5).** Known limitation: Show Collision wireframe still region-based at spawn (not streamed). `collisionDrawDistance 150` (cell + 4 edge neighbours).
