---
name: enter-vehicle-plan
description: Plan 016 — player enters a car (Enter key → align/open/getin/sit) with door dummy + anims
metadata:
  type: project
---

Plan: `.claude/plans/016-enter-vehicle/readme.md`. Press **Enter** near a car → CJ auto-runs to the driver door, opens it, climbs in, ends seated (hands on wheel). Driver side only (LHS/`door_lf`); exit deferred. Builds on [[vehicle-loading-plan]] (rig/dummy pattern).

**Assets verified:** `ped.ifp` has `CAR_align_LHS`/`CAR_open_LHS`/`CAR_getin_LHS`/`CAR_closedoor_LHS`/`CAR_sit` (+ `CAR_getout_LHS`, `Drive_L/R` later) — already loaded into the AnimationController clip map by `loadAnimations`. **Door:** `door_lf_ok` atomic's frame parent = `door_lf_dummy` (hinge) at [0,0,0]; hinge at [-1.05,0.88] rel body (left-front=driver); door opens by rotating the hinge about **Z**. **Seat:** `ped_frontseat` dummy (at +X≈0.48 on admiral2 — verify side in browser). CAR_* clips carry root translation (default `buildAnimationClip` strips it) → plan moves CJ manually (align→door→seat lerps), in-place clips first.

**Iterations:** 1) `buildVehicle` wraps `door_*_ok` in hinge pivots, exposes `doors`/`seatLocal`/`doorEntryLocal` + door-open helper (+tests). 2) vehicle registry + `findEnterable(pos,range)` + Enter key → open driver door (first visible step). 3) full `EnterVehicleSystem` state machine ALIGN→OPEN→GETIN→SEATED, gating the character controller + animation systems, CJ ends in `CAR_sit`. 4) polish (RHS/side pick, align tuning, exit, camera).

**Key integration points:** character-controller.system + character-animation.system need a **gate flag** (pause manual input/physics while scripted). Vehicles live under the −90°X streaming root but physics/world is native Z-up → compute entry/seat world points in native Z-up from the car's placement, not the display matrix.

**Iter 1 DONE:** `buildVehicle` now returns `BuiltVehicle { root, wheels, doors, seats }`. `door_*_ok` atomics wrapped in a **hinge pivot** (`door_<side>` group at the `door_*_dummy` world transform; mesh hinge-relative inside) — `BuiltDoor { side, pivot, closed }`; `*_dam` skipped. `seats = { frontseat, backseat }` = seat-dummy local Matrix4 (or null). `game/vehicle/vehicle-door.ts` `setDoorAngle(door, angle)` rotates `pivot.quaternion = closed × Rz(angle)` (hinge = Z). `VehicleModel` += `doors`, `seats`; adapter passes them through. Tests: build-vehicle (door pivot pos/mesh, _dam skipped, seats), vehicle-door (open/close). 241 green. **Confirmed on real cars:** admiral2 doors lr/lf/rf/rr; camper lf/rf/lr; `door_lf` hinge at −X (driver). **⚠ `ped_frontseat` is at +X (right) on both — driver is −X, so iter 3 must mirror/offset the seat target to the driver side.** Door open direction (sign) + angle TBD in browser.

**Iter 2 DONE (browser pending):** `game/vehicle/enter-vehicle.system.ts` `EnterVehicleSystem(keyboard, playerPosition)` (a System). `add({ doors, heading, position })` registers a parked car. `update`: edge-detects **Enter** (`KeyboardInput.isDown` has no edge → track `enterHeld`), finds nearest car within `ENTER_RANGE=4` (planar), **toggles** its `lf` door open/closed by lerping the angle (`DOOR_OPEN_ANGLE=-π/3`, `DOOR_SPEED=π` rad/s) via `setDoorAngle`. canvas-host creates it with `character.keyboard` + `character.viewOf` and `enterVehicle.add(...)` per placed car. No player movement yet. Tests: 5 (no-press, out-of-range, opens-in-range, second-press-closes, held-toggles-once). 246 green. **VERIFY IN BROWSER:** door open direction/angle (`DOOR_OPEN_ANGLE` sign), ENTER_RANGE feel.

**Iter 2b DONE (ALIGN+OPEN, browser pending):** user wanted "from any side ≤4 m, Enter → CJ runs to the driver door & opens it". Added **scripted auto-run** to `CharacterControllerSystem`: `runTo(target: Vec3|null)` + `get arrived` + private `moveToward` (reads ECS `Transform`, runs at `movement.runSpeed`, clears+flags arrival within `ARRIVE_DISTANCE=0.6`); `desiredMove(players)` picks auto vs keyboard (extracted to keep cognitive-complexity ≤20). `setupCharacter` now returns `controllerSystem` in CharacterContext. `EnterVehicleSystem` rewritten to a phase machine `idle→approaching→opening`: Enter (edge) near a car (center within ENTER_RANGE, symmetric → any side) → `controller.runTo(driverDoorEntry)` → on `controller.arrived` → swing the `lf` door open. `driverDoorEntry` = car pos + Rz(heading)·(lf hinge local − [DOOR_STANDOFF,0] on −X). canvas-host passes `character.controllerSystem`. Tests updated (stub controller). 247 green. **NOT YET: GETIN/SEATED (climb in, parent to `ped_frontseat`, CAR_sit), gating the animation system, the align/open/getin anims themselves (currently just runs+opens). Seat still +X (mirror in seated step).** **VERIFY IN BROWSER:** any-side approach works, CJ runs to driver door, door opens; tune DOOR_STANDOFF/ARRIVE/angle.

**Iter 2c DONE (path around the car):** bug — from the passenger side CJ ran straight into the car body (collision) and never went around. Fixed: controller `runTo`→**`runPath(points: Vec3[])`** (follows waypoints, advances on `ARRIVE_DISTANCE`, `arrived` after the last; `moveToward` recurses to next). `EnterableVehicle` gained `halfExtents:[hx,hy]` (canvas-host computes via `Box3.setFromObject(object)` before placement). `EnterVehicleSystem.driverDoorPath` builds the route in vehicle-local: if player local x>0 (passenger side) → `[[playerX, ±(hy+END_MARGIN)], [entryX, ±(hy+END_MARGIN)], entry]` (around the nearer bumper), else straight `[entry]`; mapped to world via `toWorld`/`playerLocal` (heading rotate). Tests: straight vs 3-point around. 248 green. **VERIFY IN BROWSER:** routing from passenger side actually clears the body (tune END_MARGIN/DOOR_STANDOFF); waypoints not stuck on collision.

**Soft-lock fix:** the phase machine got stuck in `'opening'` after the door opened → Enter ignored afterward (couldn't approach car 2). Now `swingDoor` resets `phase='idle'`, `active=null` once the door reaches `DOOR_OPEN_ANGLE`, so the next Enter starts a fresh approach. **Closing a door is NOT implemented yet** (no close action) — that comes with getin (door closes from inside) / a later toggle. 249 green.

**halfExtents bug fix:** the custom admiral mod has a **stray vertex (~1e25)** → `Box3.setFromObject` blew up → halfExtents ~1e25 → around-path waypoint flew "into a wall". Now `halfExtents` comes from the **COL bounds** (`parseDffCollision`'s `col.bounds`, authored clean) in the adapter (`max(|min|,|max|)` per planar axis; fallback [1.2,2.5]); `VehicleModel` gained `halfExtents`; canvas-host no longer uses Box3. Sane now: admiral [1.0,2.63], camper [1.02,3.09]. **Lesson: never bbox a modded mesh for gameplay extents — use COL.** 249 green.

**Iter 3 climb-in + seated DONE (browser pending):** full sequence now `idle→approaching→opening→getin→seated`. Added gating + scripted clips:
- `PhysicsWorld.teleport(handle, pos)` (kinematic `setTranslation`).
- `CharacterControllerSystem.setEnabled(false)` (gates fixedUpdate while scripted).
- `CharacterAnimationSystem.setScripted(clip|null, {loop, facing})` — overrides locomotion: holds `character.rotation.z=facing`, plays the clip, no bob. (Locomotion clips are CLIPS.*; car clips `car_getin_lhs`/`car_sit` are already in the controller's clip map from `loadAnimations`.)
- `EnterVehicleSystem` now takes `(keyboard, playerPosition, controller, physics, bodyHandle, animation)`. On door open → `startGetin`: gate controller, `setScripted('car_getin_lhs', {facing:heading, loop:false})`, lerp the body door→seat over `GETIN_DURATION=1.2`s via teleport; then `startSeated`: teleport to seat, `setScripted('car_sit', {facing:heading, loop:true})`, door closes (`doorTarget=0`). Seat = `ped_frontseat` mirrored to −X driver side (canvas-host: `[-|x|, y, z]`), raised `SEAT_RAISE=0.5` to capsule centre. canvas-host captures `animationSystem` + passes `character.physics`/`bodyHandle`. 248 green, build ok. **VERIFY/TUNE IN BROWSER:** seat Z (`SEAT_RAISE`) + facing sign (`facing=heading` — may need ±π/offset so CJ faces forward), GETIN_DURATION vs clip, getin slide looks ok, door closes. **Once seated you can't enter another car (no EXIT yet — deferred).** Camera follows the seated player.

**Tuning (browser-confirmed):** `SEAT_RAISE` 0.5→**0** (CJ was half a body too high; now sits right). Added a **`stepin`** phase: after the door opens, the player walks tight into the doorway (`runPath` to local `[-(hx+DOORWAY_CLEAR=0.35), seatY]`, aligned with the seat) **before** `getin` starts — so the climb-in clip plays at the door, not 1–2 m away. Phase machine: `idle→approaching→opening→stepin→getin→seated`. 248 green.

**EXIT DONE (browser-confirmed):** seated + Enter → `exitopen` (reopen door) → `exiting` (`car_getout_lhs` + slide seat→doorway over `GETOUT_DURATION=1.2`) → `finishExit` (teleport to doorway, `setScripted(null)` resume locomotion, `controller.setEnabled(true)`, door shuts, phase idle). Reuses `doorwayWorld`. Full cycle enter↔exit works. 249 green. **Plan 016 core complete (driver side).**

**Polish (browser-confirmed):** exit facing — gating now **zeroes ECS Velocity** (`CharacterControllerSystem.setEnabled(false)`) so stale stepin velocity doesn't override; `CharacterAnimationSystem.faceTo(yaw)` snaps facing; on exit faces outward `atan2(cos h, -sin h)` (−X). **Camera**: `CameraController.setAzimuth`→`Game.setFollowAzimuth`→`EnterVehicleSystem` ctor `aimCamera(yaw)`. follow camera-relative forward = `(-sin az, cos az)`. On **seated** → `aimCamera(heading)` (camera behind the rear, W drives forward); on **exit** → `aimCamera(outward)` (behind player, W walks away from car). 9 enter-system tests; all green. Remaining/deferred: RHS + passenger seats, real `CAR_open/align` anims during approach (currently just walk+swing), camera framing in-car, and — the original goal — **driving** (`physic`: throttle/steer from handling.cfg, wheels already rig-ready via VehicleRig/VehicleSystem).
