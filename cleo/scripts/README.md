# cleo/scripts — authored CLEO script sources

One folder per script: `<name>/script.ts` (the DSL source, plan cleo-sdk/004) plus
`<name>/story.test.ts` (the headless behaviour + budget test). SOURCES only — compiled `.cs`
artifacts are build outputs in `cleo/sdk/dist/`, never committed here.

Build: `npm run build:cleo-scripts`. The SDK: [`../sdk/README.md`](../sdk/README.md).

| Script              | Replaces                                                                                                                                                                                                                                                                                                               | Artifact |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `hello-conformance` | — (the SDK's own conformance sample)                                                                                                                                                                                                                                                                                   | 88 B     |
| `rhino-tracks`      | the GTA 5 Rhino mod's `rhino tracks.cs` (34 114 B)                                                                                                                                                                                                                                                                     | 2 628 B  |
| `cutscene-override` | — (a real-SA field instrument, plan 003: the scene named in `cleo\cutscene-override.ini` plays at session start — warp to the `.cut` site, world preload, traffic off; empty `scene =` = inert; ini generated by `scripts/debug/cutscene-override-ini.ts`). **`sa-only` target** — the bottle only, never a game build | 792 B    |

Plans for the authored scripts live in [`docs/plans/`](docs/plans/readme.md) (001 rhino tracks ·
002 no_lights — the corpus-replacement chain, user's call 2026-08-06). **002's script was withdrawn**
2026-08-07: the effect it produced now comes from the model itself (plan 098/11), so there is nothing
for a per-car script to do. Its engine half — light-damage state + the atlas rows — stayed.
