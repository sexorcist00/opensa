{
  "_comment": "Plan 023A: per-model crease-angle overrides (degrees) for smooth-normals. Curation loop: field report -> add entry -> rebuild -> eyeball. Organic/rounded models want a higher threshold than the 45-degree default (dihedrals between their facets exceed it, so shading bands appear under per-vertex N.L). First demanded entry: the LV sphinx (field 2026-07-15, 'looks angular')."
}
