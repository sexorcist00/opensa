/** Fetch loader (plan 049): manifest-driven, cached, on-demand chunk download. */
export { AssetFetchLoader, type AssetFetchLoaderConfig } from './asset-fetch-loader';
export { cacheStorageStatus } from './cache-store';
