# Autodetect text files and force the following filetypes
# to have unix eols, so Windows does not break them
* text=auto eol=lf

# The prebuilt app archive is BINARY — never try to diff or merge it.
prebuilt/*.tar.gz binary
