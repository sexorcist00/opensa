{
  "name": "@opensa/cutscene-converter",
  "version": "0.4.0",
  "private": true,
  "description": "Standalone Windows app that puts modded cars into GTA:SA's cutscenes: a facade over @opensa/vehicle-cutscene — pick a game, pick a folder of cars, pick an output. One portable exe, no prerequisites.",
  "main": "dist/main/main.cjs",
  "scripts": {
    "build": "npm run build:asi && npm run build:main && npm run build:renderer",
    "build:asi": "tsx scripts/embed-asi.ts",
    "build:main": "tsx scripts/build-main.ts",
    "build:renderer": "vite build",
    "dev": "tsx scripts/dev.ts",
    "pack:win": "npm run build && electron-builder --win --config electron-builder.yml"
  },
  "nx": {
    "tags": [
      "type:tool"
    ]
  },
  "devDependencies": {
    "electron": "43.4.0",
    "electron-builder": "26.15.3",
    "esbuild": "0.27.7"
  }
}
