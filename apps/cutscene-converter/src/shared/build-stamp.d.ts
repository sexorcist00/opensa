/** The commit the app was BUILT from: `<short sha>`, `<short sha>-dirty`, or `unknown` off a git tree.
 *  Injected into the renderer by vite and into the main process by esbuild (see scripts/build-stamp.ts). */
declare const __BUILD_COMMIT__: string;
