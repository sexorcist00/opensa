# 002 — The phone as a tool: MCP over the panel

**Opened 2026-08-28, on the user's call**: *"the current debug and testing approach is inefficient — let us
make an MCP server, so you connect to my phone and have full access to testing the whole project in the
browser and in Termux."*

The complaint is right and the arithmetic says so. Every device-gated number this project owes — the field
run, the residency ceiling, the mode-switch cost, the tile bake, the baked-3D comparison — currently travels
as: an agent writes code → a person taps a panel → a person reads a screen → a person types back what it
said. Each round trip is a message, several minutes, and **whatever detail nobody thought to copy**. The
panel already removed the worst of it (a run is a tap, a capture is a commit). What is left is that the agent
cannot see any of it.

## What this is, and what it deliberately is not

**It is a CLIENT of the running panel, never a second copy of it.** `JobRunner` allows one job at a time, and
two runners on one phone is two converts fighting over one folder — the exact failure the panel's own
one-at-a-time rule exists to prevent. So `mcp.mjs` speaks to `http://127.0.0.1:8787` and, when the panel is
not up, says so instead of starting anything.

**What it can do is what the panel's buttons can do.** The jobs are an allowlist (`jobs.mjs`), not a shell:
preflight and state, the three converts, the pull/setup rituals, the shareable build, the log, the stop, and
`commit & push` over what the map itself filed. An agent that can do those can drive the whole measurement
loop without a person relaying a screen.

| Tool | What it answers |
| --- | --- |
| `phone_state` | the preflight checks and their verdict, the pak and its recipe, the running job, the ports, the captures waiting to be committed |
| `phone_jobs` | the allowlist itself — read from the table `buildJob` validates against, never a list kept in step by hand |
| `phone_run` | start one, with its knobs; refused while another runs |
| `phone_log` | the tail of the same ring the page shows — one log, or a field report and the screen disagree |
| `phone_stop` | stop it |
| `phone_commit` | commit and push exactly what the panel says is pending |

## The shell tool, and why it is off

`phone_exec` runs a command in the repository, and it is **off unless `PANEL_MCP_EXEC=1`**. The user asked
for full access and this is how full access is given: as something switched on deliberately, on the day it is
wanted, rather than as a remote shell that arrives with an update. The allowlist covers the measurement loop;
`exec` covers what nobody anticipated, which is a real category and not one to pretend away.

## Two transports, one handler

- **stdio** (default) — a Claude running ON the phone, in Termux. Nothing is exposed to any network at all.
- **`--http --port 8788`** — JSON-RPC over POST, for a Claude that is somewhere else. Binds `127.0.0.1` like
  the panel, and **requires a bearer token** (`PANEL_MCP_TOKEN`, or one generated and printed at startup).

**Reaching it from off-device is a tunnel the operator sets up on purpose**, and the token is what makes that
survivable: a tunnel plus no token is a shell on the open internet, addressed by a URL that is guessable the
moment it is logged anywhere. This is the one part of the design that is a security decision rather than an
engineering one, and it belongs to whoever owns the phone.

## What is verified, and where

The protocol and the plumbing are testable without a phone, and were: `mcp.test.mjs` covers the handshake,
the tool list, the notification-has-no-reply rule, the difference between a JSON-RPC error and a tool
failure (an agent can act on the second and can only give up on the first), the shell tool's default-off, and
that a run goes THROUGH the panel. Beyond the unit tests, the whole path was exercised in a container on
2026-08-28: `initialize` → `tools/list` → `phone_state` (15 checks, verdict `fail` — correctly, there are no
game files there) → `phone_run districts` → `phone_log`, which returned the district table the job printed.

**What a container cannot verify** is the part that is the point: that a phone behind a tunnel answers, and
that Claude Code's own MCP client is happy with this handshake. MCP servers are read from `.mcp.json` at
session start, so the first session to use it is the first proof.

**Two things the first attempt got wrong, both found on a real phone the same day (2026-08-28):**

- **The tunnel.** cloudflared's own pre-check failed both ways — `QUIC connection failed` and `HTTP/2 is
  blocked` — while `api.cloudflare.com:443` passed: the carrier allows 443 and blocks **7844**, the only
  port cloudflared uses in either protocol. Nothing in its config reaches that, so `tunnel.mjs` now tries
  every installed provider in an order chosen by what survives a restrictive network — **ngrok** (443, needs
  a free account), **localhost.run**, **pinggy** and **serveo** (SSH, no account), **cloudflared** last —
  falling through by name. `TUNNEL=<name>` forces one. **The phone settled the order**: `localhost.run` came
  up (`https://<id>.lhr.life`, 17:50), serveo's host closed the connection, and pinggy fell through to a
  password prompt — so every ssh provider now carries `BatchMode=yes`, which turns an unanswerable prompt
  into the next provider instead of a spent timeout.
- **An address is not a tunnel.** The first version announced on the address line, and cloudflared prints
  `Your quick Tunnel has been created!` BEFORE it has dialled the edge: it printed one, failed every dial
  after it, and the paste block claimed a dead URL was ready — worse than printing nothing. A provider is
  now announced only once it says it is connected, and one whose own pre-check reports `hard_fail=true` is
  dropped on that line instead of after the timeout. Both paths were replayed through the runner with the
  phone's verbatim log lines.
- **The config.** `"url": "${OPENSA_PHONE_URL}"` is refused whole when the variable is unset — Claude Code
  leaves an unset `${VAR}` unexpanded and reports `INVALID_CONFIG: 'url' is not a valid URL`; `${VAR:-default}`
  did not help either. That failure is at **load**, so it costs a new session to clear. `.mcp.json` now runs
  `mcp-bridge.mjs`, a stdio server that reads the address and token at run time and forwards each call over
  HTTP. The session always loads: without the variables it lists no tools and says what to set, with them it
  is the phone's tool list. `mcp-bridge.test.mjs` covers both sides, plus a rejected token reported as itself
  rather than as a stale address.

## The browser half, and why it is not a browser protocol

**Landed 2026-08-27 with the rest.** The ask was explicit: not just the panel, but the map page — see it,
read its numbers, its realtime benchmarks, drive it. Two ways to do that, and the cheap one turned out to be
the better one:

| | Chrome DevTools Protocol | The page as the instrument |
| --- | --- | --- |
| needs | `adb`, developer options, a wireless-debugging pairing, a port forward | nothing |
| sees | any page, including one whose bundle failed to parse | our console, once it boots |
| reads | the DOM, the console, arbitrary JS | the inventory report, the readout, the error log, the board |
| pictures | a real screenshot of the device | the console's own `exportImage` composition |

The console already computes everything worth reading (that is what `?inventory=1` is) and already composes
its own PNG (that is what 201/7-07's share link is). So the page long-polls the panel for commands and
answers them: `snapshot`, `screenshot`, `pose`, `mode`, `ops`, `errors`. `MapBus` is the queue, one waiter
per command, and it says WHICH end went quiet — "no map is attached" and "the map took it and never
answered" are different problems and a caller must not have to guess.

**Opt-in by query.** `&agent=1` is on the panel's own links and nowhere else, so a shared link on somebody
else's phone never talks to a local port that happened to answer.

**What this cannot do, stated rather than discovered:** a page that fails to load at all cannot report that
it failed. The boot shell and the error log cover a boot that gets far enough to run; a bundle that does not
parse is invisible to this channel and visible to CDP. That is the case CDP earns its prerequisites for, and
the reason it stays on the list below rather than being crossed off.

Verified end to end in a container on 2026-08-28, with a stand-in page: `map_state` (attached, with the
heartbeat), `map_snapshot` (the report came through), `map_screenshot` (an MCP **image** block, not a base64
string the agent would have to describe), `map_mode` and `map_goto` (the page acted and answered).

### The half that still needed a person (2026-08-28, the third pass)

Every tool above talks to a page that is ALREADY open, and the bus is honest about it: with nothing
attached it refuses and says to open the console with `&agent=1`. That refusal was the end of the road —
the one state no call could leave — so the first field run of a session stopped dead until somebody
picked up the phone and tapped a link. On a channel whose whole point is that an agent drives a
measurement without a person relaying the screen, the FIRST step was still a person.

`map_open` closes it. Android will open a URL for us — `termux-open-url` hands one to the browser exactly
as a tap on a link does — so the tool launches the panel's own URL and then waits on `MapBus` for the page
to phone home.

**The launch is not the success, and the tool says which it got.** A browser that started is not a console
that loaded: the static server may be down, the pak may be missing, the bundle may not parse — and that
last one is the case this whole channel is blind to by construction (above). So an attach is reported as an
attached map with what the page says it is showing, and a launch with no attach is reported as a launch,
naming what to read on the screen that is now in front of the operator. A console already attached is left
alone rather than covered by a second tab that would take the bus over.

**One module builds the links** (`app/console-urls.mjs`), used by the page and by `POST /api/map/open`
alike. The arithmetic is small — a port, a pak folder, a district, `&agent=1` — and that is exactly why a
second copy would have survived review: the two would drift, a tap and a tool would open different paks, and
the capture would name the one nobody was looking at. `termux-open-url` missing is a doctor `warn` rather
than a `fail`, with `pkg install termux-tools` as the fix: every link still works under a thumb.

## The protocol moved, and this server had not (2026-08-28, the second pass)

The first version was written against **`2024-11-05`** and pinned it: whatever revision a client asked for,
`initialize` answered with that one. Measured against the phone the same day, through the tunnel it was
actually serving — a client asking for `2025-06-18` was answered `2024-11-05`, and `server/discover` came
back `unknown method 'server/discover'` in 93 bytes.

Neither broke Claude Code, and that is exactly why it needed measuring rather than assuming. **The current
revision is `2026-07-28`**, and it is not an increment: it is a stateless core where the version rides every
request's `_meta`, there is no handshake at all, and `server/discover` is an RPC every server **MUST**
implement. A client that speaks both eras probes stdio with `server/discover` and falls back to `initialize`
on any answer that is not a recognised modern error — which is what saved us. A modern-only client does not
fall back, and gets nothing.

So the server is **dual-era** now, which is what the spec asks of a server in this position:

| | Before | After |
| --- | --- | --- |
| `initialize` | always `2024-11-05` | the CLIENT's revision when we speak it (`2025-11-25`, `2025-06-18`, `2025-03-26`, `2024-11-05`), our newest when we do not |
| `server/discover` | `-32601 unknown method` | the supported versions, the capabilities, the identity and the instructions, in one answer |
| a request declaring a revision in `_meta` | ignored | served statelessly, `resultType: complete`; an unknown one refused with **`-32022`** that NAMES what we do speak, so the client can retry |
| a malformed line or a truncated body | **killed the process** | `-32700`, and the next message is answered |
| a JSON-RPC batch | dropped in silence — an array has no `id`, so nothing was ever sent back | answered as a batch, notifications dropped from it |

There is no session state to keep on either side, which is why this was cheap: every tool call is already a
fresh HTTP hop to the panel. `mcp-protocol.mjs` holds the versions, the negotiation, the discovery answer and
the framing, because **the bridge has to speak for the phone while the phone is unreachable** and a second
copy of the version list in it is a second thing to forget.

### The crash was the real finding

`JSON.parse` sat in the socket's own data handler, in the server AND in the bridge, and on the HTTP transport
in the request's `end` handler. **One malformed byte killed the process** — verified on all three on
2026-08-28. On stdio that takes every tool in the session with it and costs a NEW session to get back, which
is the exact cost this panel exists to remove; on HTTP it takes the phone's MCP server down for good. A
tunnel that half-closes a connection mid-body produces that byte for free, and the agent sees only *"the
phone is not answering"* — the symptom of a dead tunnel, which is what it would then go and diagnose.

The HTTP transport also assembled a body of any size before looking at it; it now stops at 4 MB, answers
`405` to a GET rather than reading a body that is not coming, and cannot be taken down by a handler that
throws.

### The other half: saying what the tools ARE

Compatibility gets an agent connected. What it does next is a communication problem, and the surface was
silent about the four rules that are not visible in any signature: read `phone_state` first, one job at a
time and `phone_run` returns at the START, `map_state` before any `map_` tool because a missing page is a
PERSON's problem, and a failure comes back as a readable result rather than a protocol error.

Those are now `instructions`, handed over at the handshake and at discovery — including by the bridge when
there is no phone at all, where the empty tool list used to be the whole message. Every tool also carries a
`title` and its behaviour **annotations**: an unannotated tool is read as destructive and open-world, which
is right for a convert and wrong for the nine that only read. The no-argument tools state a closed schema,
`map_mode` and `map_goto` enumerate the words they accept instead of describing them in prose, and every
JSON answer now rides as `structuredContent` beside the text a model reads.

**No `outputSchema`, deliberately.** A schema makes conformance mandatory, and these shapes are the panel's
rather than the MCP layer's — a preflight check added on the phone would start failing validation inside an
agent's client, which is a worse failure than having no schema at all.

### What it cost, measured

[The round-trip benchmark](../../../../docs/benchmarks/tools/2026-08-28-phone-mcp-round-trip.md), taken
through the phone's own tunnel before the change:

- **the tunnel floor is ~230 ms and does not move with payload size** — `map_state` at 119 bytes and
  `tools/list` at 3 496 cost the same, so an agent pays per CALL, not per byte;
- **`phone_state` is 1 378 ms p50**, and it is the tool everything is told to call first. That is not
  transport: it is the panel re-running all sixteen preflight probes per call. Left alone here — it is the
  panel's endpoint, not this layer's — and written down so the next reader does not diagnose it as latency;
- the additions cost **+1 506 B of handshake and +2 071 B of tool metadata**, ~3.6 KB (≈900 tokens) once per
  session. One avoided round trip pays for the tool list.

Verified by 135 tests (was 112) and end to end on both transports locally: a malformed line answered and the
next one served, a batch answered as a batch, `2025-06-18` echoed, `server/discover` answered, `1900-01-01`
refused with the list. **What a container still cannot verify** is the same thing it could not verify the
first time — the phone has to restart `panel:tunnel` on this code before any of it is live over the tunnel.

## Next, and in this order

1. **CDP, for the one thing the page cannot do**: report that it failed to load. `adb` over wireless
   debugging plus `adb forward tcp:9222 localabstract:chrome_devtools_remote` gives navigation, console and
   a device screenshot of any page — worth it the first time a bundle breaks on the phone and the channel
   above goes silent, and not before.
2. **A scripted run.** The console can already drive itself (`?bake=tiles` does). A `?script=` that flies a
   named path and files its capture would make "take the field measurement" one tool call, with no browser
   protocol at all — worth costing against the CDP route before building either.

## Status

| Step | State |
| --- | --- |
| the panel as MCP tools (stdio + HTTP, the allowlist, `phone_exec` off by default) | **DONE 2026-08-27/28** — verified in a container end to end, then on the phone: `initialize` → `tools/list` → `phone_state` → `phone_run` → `phone_log` |
| the browser half (`map_*` over `MapBus`) | **DONE 2026-08-27** — verified in a container with a stand-in page; the screenshot comes back as an MCP image block |
| the tunnel (five providers, announce-on-connect) | **DONE 2026-08-28** on the phone: cloudflared cannot reach its edge here (carrier blocks 7844), `localhost.run` came up. **Superseded in practice the same day** — ngrok is installed now and is what the phone serves through; the ordering argument stands |
| the bridge (`.mcp.json` → `mcp-bridge.mjs`) | **DONE 2026-08-28** — the fix for `INVALID_CONFIG` at config-parse time, which cost a new session to clear |
| **dual-era protocol + the instructions surface** | **DONE 2026-08-28** — negotiation, `server/discover`, `-32022`, batches, a framing that survives a malformed byte, `title`/`annotations`/`structuredContent`, and `instructions` at the handshake. 135 tests. [Numbers](../../../../docs/benchmarks/tools/2026-08-28-phone-mcp-round-trip.md). **Owed: the phone restarting `panel:tunnel` on this code — until then the tunnel serves the old server** |
| **`map_open` — the console onto the phone's screen** | **BUILT 2026-08-28** — the refusal `MapBus` could only report is now one a tool can clear: `termux-open-url` launches any of the panel's six links and the tool waits for the page to reach the bus, so a launch and an attached map are never confused. The six links move into `app/console-urls.mjs`, shared with the page. 147 tests. **Owed: the run on the phone itself** — this is verified in a container, and `termux-open-url` is the one piece no container can exercise |
| `phone_state` costing 1 378 ms per call | **NOT STARTED** — the panel re-runs sixteen preflight probes on every `/api/state`. Measured 2026-08-28; wants a cheap read separated from the full probe, in the panel rather than here |
| CDP, for a bundle that will not parse | not started — see below |
| a scripted run (`?script=`) | not started — see below |
