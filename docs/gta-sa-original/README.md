# The original GTA:SA — the game we import from and ship into

Facts about **Rockstar's game and the real install we target**, kept apart from everything describing OpenSA.
The rest of `docs/` answers "what does our engine/pipeline do"; this folder answers **"what is the original,
and what is actually running on the machine our output lands on"**.

The separation matters because the two are constantly confused in planning. A 2004 ceiling is a fact about
the original; whether we have to design around it is a fact about the install. Plan 07 spent a fortnight
organised around a limit the target install had already set to `unlimited` — that is the mistake this folder
exists to prevent.

## The files

| File | Subject |
| --- | --- |
| [reference-install-config.md](reference-install-config.md) | **The verbatim capture** of that install — exe fingerprint, every plugin, both adjuster inis, our ASI log, the installed mods. Written to survive the deletion of the `NO_COMMIT/` copy |
| [reference-install.md](reference-install.md) | **The declared baseline install** (`NO_COMMIT/gta_sa`, 2026-08-07): which plugins own which limits, which stock ceilings it lifts and which it does not, and the numbers it actually runs (72 914 permanent text rows, a 9 627-row IPL file) |
| [img-archive-limit.md](img-archive-limit.md) | **The 8-archive ceiling and how it is lifted** (2026-08-15): it has TWO halves — `CStreaming::ms_files` (derived: 8 slots, `0x8E48D8`) and the CdStream handle tables — plus the mechanism read out of `SimpleLimitAdjuster_IMGfiles.asi`: relocate the table, then rewrite the 4-byte OPERANDS of the 14 instructions that referenced it, and probe `CdStreamRead` for an existing `0xE9` hook. What we take from it and what we refuse (it patches blind; our SDK verifies). **Not needed today** — the shipped layout fits the stock 8 exactly; the deferred task and its trigger are [`in-reserve/img-archive-limit-lift.md`](../in-reserve/img-archive-limit-lift.md) |
| [procedural-objects.md](procedural-objects.md) | **What `procobj.dat`'s columns mean** (recovered 2026-08-09): SPACING is a LENGTH (`area / spacing²`, and the file's own header comment says otherwise), MINDIST is a camera radius clamped to 80 and never a distance between objects, nothing prevents clumping, and the surface/entity gates that decide what scatters at all |
| [unloaded-map-data.md](unloaded-map-data.md) | **Stock data files the game never loads** (2026-08-15): `gta.dat` declares 106 IDE/IPL files, 112 sit on disk, and a map `.ide`/`.ipl` has no other load path. The concrete cost — `leveldes.ide` re-uses seven ids that the LOADED `countn2.ide` owns, which every folder-globbing tool reports as a conflict and the game never sees. The load list is `gta.dat`, not the directory |
| [timecyc24h.md](timecyc24h.md) | **The 24-hour timecycle plugins** (2026-08-22): there are TWO (`timecyc24h.asi` by Dante, `timecycle24.asi`), they read the SAME 23 × 24 × 52 format — stock's 27 groups plus `DirMult` — and each hardcodes ONE file name, the only such string in its binary. Swapping their `.dat` files does nothing because the plugin never finds its name, not because anything fails to parse. Plus the two content facts: Dante authors negative `FogSt` on 243 of 504 rows down to −1 700 (and −1000 collides with our parser's int-failure default), and his file fixes stock's corrupt `RAINY_COUNTRYSIDE` 8PM keyframe |
| [timecyc-fog.md](timecyc-fog.md) | **What SA does with `FogSt` and `FarClp`** (recovered 2026-08-22): the pair goes straight to D3D9 LINEAR fog — `D3DRS_FOGSTART`/`D3DRS_FOGEND` — with **no clamp anywhere**, and the fog colour is the sky-bottom colour. So a **negative `FogSt` is a haze already partly opaque at the camera** (`−FogSt / (FarClp − FogSt)` of it), which is how a weather says "no clear near zone"; stock authors it on 37 keyframes, Dante's table on 243 rows down to −1 700. Plus: the value is stored as `int16`, `−1` is a sentinel only for the extracolour override, and `m_FogReduction` and altitude move the FAR CLIP, never the start |
| [carmods-upgrade-ceilings.md](carmods-upgrade-ceilings.md) | **`carmods.dat` has two fixed-size ceilings and neither is checked** (2026-08-19): 30 `link` pairs game-wide (`CLinkedUpgradeList` @ `0xB4E6D8`, stock uses 23) and 16 listed parts per car (`m_anUpgrades[18]`, `hydralics`+`stereo` appended; stock `jester` is full). The old added-cars build sat at exactly 30. Part names: prefix = behaviour, ≤ 19 chars. Lifted by `asi/perfect-vehicle`, guarded by `tools/add-vehicles` until then |
| [modloader-data-files.md](modloader-data-files.md) | **What Mod Loader MERGES, what it REPLACES, and the file NAME that decides** (2026-08-19, seven field launches): a file named after a stock data file is a REPLACEMENT — `vehicles.ide` in a mod folder deleted the stock 212 cars, `handling.cfg` deleted every stock handling line. A `.txt` is only a data file if the NAME is one it knows (`veh_mods.txt`); anything else is scanned as a "readme", which is the road that works — its documentation matches `cars`, `peds` and `objs` part rows by SHAPE out of any text file. Also: an added car's rows baked into `data/` kill the game at boot with no dump (FLA shows a message box and exits), and the two adjuster settings tried during the bisect (`Vehicle colors`, `Vehicle Models`) are both NOT the answer — the second breaks the game outright |
| [vehicle-colour-table-128.md](vehicle-colour-table-128.md) | **`carcols.dat`'s palette is a 128-row table and the shipping build carries 140** (2026-08-19): stock has 127, the mod fleet added 13, and three of those are DUPLICATES — `addPaletteColors` appended a mod's colours on every re-run, so the palette grew with the number of rebakes rather than with the mod set. Fixed (a colour is reused when RGB + description match); the overflow itself is WARNED, not refused. **Raising `Vehicle colors` was TRIED and REVERTED the same day** — the install has been running 140 rows with the setting untouched, so the 128 is FLA's annotation rather than a watched ceiling |
| [car-generators-500-and-the-map-1045.md](car-generators-500-and-the-map-1045.md) | **The car-generator array is 500 and the map ships 1045 records** (2026-08-19): not a contradiction — the map's live in binary IPL STREAMS and come and go with their section, while a `cleo/Parked Car Maker.ini` `[Cars]` row is created by CLEO and holds its slot for the whole session. So the budget is our permanent rows plus the worst-case RESIDENT map generators, and the resident half has never been measured here (the field test that would settle it is in the file). Also: `Accept any ID for car generator = 1` changes the SAVEFILE format, by FLA's own log line |
| [carmods-unknown-part-crash.md](carmods-unknown-part-crash.md) | **A `carmods.dat` token with no IDE row is a null dereference at boot** (2026-08-17): `LoadVehicleUpgrades` → `SetupVehicleUpgradeFlags` on a null model info (`0x4C4576`), no log line, no id-range check — a mod may add parts, it just has to define them. Caught at build time by vehicle-installer's `assertCarmodsModels` |
| [rw-frame-list-parent-order.md](rw-frame-list-parent-order.md) | **A DFF's frame list must be topologically ordered** (2026-08-19): RenderWare's clump reader parents each frame in the SAME loop that creates it (`0x807B32` reads `frames[parentIndex]` right after writing `frames[i]`), so a parent index greater than the child's reads an unwritten slot — no bounds check, no ordering check. What the slot happens to hold decides between a crash and silent corruption, which is why the class is intermittent. Found through `0x007F0BF7` in `RwFrameAddChild`: the blade mod's `wg_r_lr_bl1.dff` (part 1107) serialises its two frames child-first where its own mirrored sibling does not. **Invisible in OpenSA** — our reader resolves parents by index |
| [atomic-model-one-atomic.md](atomic-model-one-atomic.md) | **An `objs` model is ONE atomic** (2026-08-17): `CFileLoader::SetRelatedModelInfoCB` → `CAtomicModelInfo::SetAtomic` overwrites the model info for every atomic of the clump it reads, so the last visited (the FIRST in the file — RW's clump list is head-inserted) survives, re-framed at the origin. Stock ships 34 multi-atomic map models and every one is an `anim` row; zero `objs` rows carry two. Why a verbatim LOD clone of `burger01_LAw` showed only its burger sign — the round-15 "LOD absent" |
| [tree-wind-sway.md](tree-wind-sway.md) | **How the original sways a tree** (2026-08-21, from gta-reversed): `CEntity::PreRender` calls `ModifyMatrixForTreeInWind` when the model info's `SwaysInWind()` (`IS_TREE`) is set, and it shears the entity's MATRIX `at` vector — not the vertices — so the bit costs the same on an 8-triangle impostor card as on the tree. `IS_PALM` (`IsSwayInWind2`) adds a wind-scaled term on top. The sway is seeded per ENTITY (`m_nRandomSeed`), so an HD and its own far-LOD lean independently, which is invisible while only one of the pair is drawn. Read before putting either bit on a generated row (lod-trees 013 step 02) |
| [skygfx-fork-building-pipe.md](skygfx-fork-building-pipe.md) | **What the install's SkyGfx (JuniorDjjr fork) does to a world atomic** (2026-08-17, read out of its source AND its shipped compiled shaders): the install's ini values that matter (`buildingPipe=PS2`, `stochasticTexturing=1`, dual pass at 200), the stock rule for who is on the building pipe (night-colour chunk), and the finding that **no building shader reads vertex normals** — prelit + ambient is the whole light term, in stock SA and in the fork alike; the stochastic PS is a barycentric blend of three hashed samples of the same tiled texture and depends on nothing in the geometry; the fork's own instancer `DNInstance_PS2` is the one place a re-encoded mesh meets fork code |
| [skygfx-fork-vehicle-pipe.md](skygfx-fork-vehicle-pipe.md) | **Which numbers of a vehicle DFF the install's car pipeline actually reads** (2026-08-18, from the fork's source): with `vehiclePipe=PS2` the reflection STRENGTH is the DFF reflection plugin's `intensity` (`0x253f2fc`), **quantised to an int8** and multiplied by **8.0** — while the MatFX env-map is only a gate, its `coefficient` float scaling nothing here. So anything at or above `intensity 0.125` leaves the term saturated: the stock 0.5 and a tasteful 0.16 look identical, and the visible band is ~0.02–0.12. Why a byte-perfect reflection retune can be invisible in this install |
| [vehicle-special-features.md](vehicle-special-features.md) | **Special vehicle abilities are hardcoded to model ids** (2026-08-18): pop-up lights, hydraulics, hooks, turrets, jets — a branch on the id, so only a slot replacement inherits them; the reference install's FLA ships a `model_special_features.dat` loader (`CustomModelName StandardModelName`, enabled, EMPTY) that re-points a model at a standard one — vehicle-installer plan 011 writes it from each mod's `features.txt` |
| [uncompressed-rasters.md](uncompressed-rasters.md) | **What the original ships UNCOMPRESSED, and why it matters** (2026-08-20, a census of every `.txd` in `gta3.img` + `gta_int.img`): 867 uncompressed rasters, **every one single-level** with filter `0x1101` — there is no stock example of an uncompressed texture with mips. `models/generic/vehicle.txd` and `models/particle.txd` are 32-bit end to end, and they are exactly the dictionaries mods patch with PNG folders. The one raster we know the game READS BACK is `platecharset` (`CCustomCarPlateMgr` copies glyph pixels out of it), and there is no way to tell the others apart by name |
| [cutscenes.md](cutscenes.md) | **The cutscene system as measured** (2026-08-12, the vehicle-cutscene gates): the 23 hand-rigged vehicle models and their per-car authoring chaos, txdcut.ide's typo/missing/dead rows, the narrow ok/dam-only frame collapse, ANPK anims binding by NAME with a channel for every bone, blank plates (CCustomCarPlateMgr never runs in cutscenes), and why a cutscene.img drop-in into the bottle is a clean A/B |
| [fla-id-limits-are-part-of-the-savefile.md](fla-id-limits-are-part-of-the-savefile.md) | **The adjuster ini is a savefile schema** (2026-08-19): FLA prints, every boot, that its IPL-flags block is sized by the current IPL ID limit — so moving `FILE_TYPE_IPL` (ours went 256 → 1024) makes every existing save a different format. SA's loader is block-tagged, so the ordinary symptom is a REFUSED load, not a crash; a save that loads far enough to crash needs a different explanation |

## What belongs here, and what does not

**Here:** how the original engine behaves, what its data means, what its formats are, and what the target
install provides. Anything a mod author or a modded install brings with it.

**Not here:**

- **A rule a new design must satisfy** — that is [`restrictions/`](../restrictions/README.md), which links
  here for the detail. The split is the same one that folder already uses: this side carries the
  measurement, restrictions carries the one-line rule and what breaks when it is violated.
- **A limitation of OUR pipeline** — [`edge-cases/`](../edge-cases/README.md).
- **How the original's code worked**, recovered for a design — that belongs in the plan doing the recovering,
  with a link to [gta-reversed](../links.md). We recover what the DATA means and write our own execution
  ([`project-goals.md`](../project-goals.md) directive 1); a page of ported logic is not a reference.

## The rule this folder is here to enforce

**Budget a map-content plan against the install you ship to, not against stock 1.0 — and say which one you
picked.** Both answers are legitimate and they are wildly different numbers. Writing neither down is how a
plan ends up conservative by two-and-a-half times without anyone noticing, because the result still works.
- [model-variations-sections.md](model-variations-sections.md) — the plugin's ini has TWO kinds of section and they are not interchangeable: a model's NAME says what it IS (its paintjobs, its parts, its trailers), a model's ID says what may spawn in its place. Mixing them had an added car wearing its base's parts; and an ADDED car's own section must be keyed by its ID, because its NAME does not exist when the plugin resolves headers — the row that names it is merged by Mod Loader later. A tuning defect that shows in traffic but not at Transfender is a binding defect, not a data one
- [vehicle-paintjobs.md](vehicle-paintjobs.md) — a paintjob is a whole texture dictionary named by CONVENTION off the car (`blade1.txd`), and **no row of any data file names it**: 36 across 13 cars in stock, invisible to anything sorting entries by which row claims them. FLA's `Make paintjobs work for any ID` lets an ADDED car have paintjobs — it is not a per-id COUNT lift — and a mod shipping fewer than stock is fine (field, 2026-08-20)
- [veh-mods-col-and-the-upgrade-object.md](veh-mods-col-and-the-upgrade-object.md) — a tuning part is previewed as an ordinary `CObject`, which dereferences `m_pColModel`; stock parts get theirs from `gta3.img : veh_mods.col` (194 entries, ids 1000–1193) and an ADDED part gets none, so it crashes unless its IDE flags carry `0x200000`
