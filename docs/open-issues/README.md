# Open issues

Resolved issues move to [fixed/](fixed/) with their full forensic history preserved.

Known unsolved problems that have been investigated but **deliberately shelved** — no shipped fix
yet. Each file records the symptom, the root cause we found, the approaches we tried (and why each
fell short), and pointers for whoever picks it up later. Same spirit as `docs/features/*`, but for
problems rather than implemented features.

These are NOT plans (`docs/plans/*` are for work we intend to do soon) and NOT features
(`docs/features/*` are for things that work). When an open issue gets a real fix, promote it to a
plan/feature and delete the entry here.

| Issue                                                              | Doc                                                                    | Status                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Two car mods ship a tuning part under the same stock name** | [fixed/vehicle-part-name-clash-between-mods.md](fixed/vehicle-part-name-clash-between-mods.md) | ✅ **FIXED 2026-08-20** (`vehicle-installer` 014) — a shipped part whose stock `veh_mods.ide` row is textured by ANOTHER car is a new part of the car shipping it, installed under `<stock name>_<token>` with the stock row, shop item, price and `link` cloned under it. **9 clashes → 0** over the same 212 folders; the voodoo wears its own eleven and the blade keeps its bumper. The guard survives the fix — a fleet still staging two different files on one entry name is refused, naming both mods and both sizes |
| **An added car in traffic wears its BASE car's paintjob and tuning parts** | [added-car-inherits-its-base-tuning.md](added-car-inherits-its-base-tuning.md) | 🔴 OPEN 2026-08-19 — one ModelVariations section describes one model, and `add-vehicles` 004 puts the added id into its base's section to reach traffic, so the base's paintjobs and body parts are offered to a different body (the blade's trunk spare wheel floating behind a Pontiac). 40 sections affected. NOT a conversion defect: the `sa` target's archives hold the mod's files byte-identically. Test armed in the bottle: all 115 ids in `ExcludeModelsFromInheritance` |
| **The added fleet's TUNING crashes at the end of loading** | [fixed/added-cars-crash-after-loading.md](fixed/added-cars-crash-after-loading.md) | ✅ **FIXED 2026-08-19, field-confirmed the same evening** — not the tuning data at all: `Vehicle colors = 256`, an FLA setting WE added on an inference (`db1f0ca4`, the palette is 142 `col` rows against FLA's annotated 128). Crossing "over 255" pulls in a uint32 colour-id patch family (+122 memory changes) and this install dies at the end of loading with it; commented back out, the full tuning loads. Four launches went into bisecting the tuning data because three docs recorded the setting as reverted while it was live in all three trees. **The lesson is in the doc: a bisect whose dump never moves is saying the variable is not in the data** — runs 3 and 4 matched to the heap address |
| Alpha cutout black edge (foliage/fences)                           | [fixed/alpha-edge.md](fixed/alpha-edge.md)                             | ✅ FIXED by construction in the own-engine texture pipeline (074/03: premultiplied offline mips + dilation + A2C); field-confirmed 2026-07-11                                                                                                                                                                                                              |
| A mod DFF renders INVISIBLE (Santa Maria beach "blue strip")                | [fixed/mod-dff-winding-and-atomic-frame.md](fixed/mod-dff-winding-and-atomic-frame.md) | ✅ FIXED 2026-07-30 ([plan 095](../plans/095-dff-geometry-parity/readme.md)) — we built triangles from the Geometry Struct FACE ARRAY, RenderWare draws the BinMeshPLG index data; `0. Map Fixes Pack`'s re-export wound them oppositely, so `roads32_law2` faces down and is back-face culled (collision + camera focus survive, which is why every data probe looked clean). 4 models map-wide, all mod-introduced |
| A model sits in the WRONG PLACE (`land_42_sfw`, sunk street trees)          | [fixed/mod-dff-winding-and-atomic-frame.md](fixed/mod-dff-winding-and-atomic-frame.md) | ✅ FIXED 2026-07-30 ([plan 095](../plans/095-dff-geometry-parity/readme.md)) — we applied the atomic's own frame transform; SA discards it for simple map models (`LoadAtomicFile` → `RpAtomicSetFrame(atomic, RwFrameCreate())`). The mod put a 90° Z rotation there. The COL oracle says DROP for every model whose kept atomic is its own geometry — including VANILLA `aw_streettree1` (165 inst) sunk 3.1 m and `grassplant` (44 inst) |
| "Locked" (anti-rip protected) DFF/TXD models                       | [fixed/locked-dff.md](fixed/locked-dff.md)                             | ✅ CLOSED 2026-08-04 — 5 lock variants recover (inflated item/struct/container sizes, hidden TexDictionary wrapper, and Variant E: a poisoned clump-ROOT matrix SA never reads — the gostown comet, invisible with live collision, now renders whole). The same hunt fixed two non-locks: the vehicle dictionary's upscale-to-max bloat (comet.osm 136.6 → 20.3 MB, now size-bucketed) and the VER2 `.img` u16 sector wrap past ~128 MB (now guarded loudly). Still open, low priority: the offline byte-editing tools need an explicit `unlockDff` |
| Two `PhysicsWorld` collider defects (+2 smaller)                   | [fixed/physics-collider-defects.md](fixed/physics-collider-defects.md) | ✅ ALL FIXED 2026-07-18, the day plan-077 coverage work found them — `createFalling` falls back to its box instead of throwing (regression-tested by BEHAVIOUR: the prop lands); the dead-and-lying `setColliderEnabled` deleted; the roadsign glyph table is a `Map`; `mat4Multiply`'s aliasing constraint documented on the hot path rather than guarded |
| Map car generators poison the physics world (wasm `unreachable`)   | [fixed/map-car-generators-poison-physics.md](fixed/map-car-generators-poison-physics.md) | ✅ FIXED 2026-08-03 and field-confirmed the same day, closed by the unit test the doc itself asked for (repro in 42 ms, after the field rounds cost ~10 minutes each). **A Rapier body is born MASSLESS** — a descriptor's mass properties are folded in only at the next `world.step` — and our step updates the raycast controllers BEFORE stepping the world, so every car spawned between two steps had its first suspension solved against mass 0 / zero inertia and came back all-NaN, which panics Rapier's own tree walk and leaves the world permanently borrowed. One line: `recomputeMassPropertiesFromColliders()` at creation. The generators were never the cause, only the RATE — 962 placements churning against 212 is a shorter fuse, which is all a bisection over populations can ever tell you |
| **Simulation residency: nothing decides WHAT may be simulated, WHERE** (the city-scale design question) | [bench-scene-transition-collision.md](bench-scene-transition-collision.md) | 🟡 HALF CLOSED 2026-08-09 by [plan 102](../../tools-debug/bench-harness/docs/plans/001-bench-settle-fall.md). The measured symptoms were a HARNESS bug and are gone: the bench settle exited on a poll that answered for the ring the player had LEFT, nothing waited for collision, and a teleport kept `Velocity.z`, so one lost race dropped him under the mesh at terminal velocity for the rest of the sweep. A/A `avgTriangles` spread 10.19 % → **0.14 %** (`lv-night`), `[cam]` jump lines 89 255 → **1**, 8 of 9 scenes now start their leg standing still. What STAYS open is the design question the doc was really about — three streaming radii, two distance metrics (2D grid vs `VehicleLodSystem`'s 3D), no authoritative readiness signal — for which **no observed defect currently forces a mechanism**; the requirements list and the eight killed hypotheses are still the starting point when one does |
| A bench anchor authored INSIDE a building (`strip-noon`) | [fixed/strip-noon-anchor-inside-a-building.md](fixed/strip-noon-anchor-inside-a-building.md) | ✅ FIXED 2026-08-09, verified the same hour — the anchor `[1934, 1177, 14]` sat inside the Flamingo (`flamingo01_lvs` at 1932.8, 1177.4), so the ground ray answered `13.00` off the building and the capsule fell out of the world on every run since the scene was added (2026-07-29). Moved onto standable ground south of it; `legStart.ok` true, `dz −0.08 m`, 27 cars live. **The first diagnosis was wrong and is kept in the doc**: the nearby cars' `no ground` deferrals are the designed spawn gate at the 150 m collision ring, and the district emptying was residency doing its job around a player 890 m down |
| **The real game loads the world as LODs only** (after installing our build) | [fixed/sa-world-loads-only-lods.md](fixed/sa-world-loads-only-lods.md) | ✅ **FIXED 2026-08-10, field-confirmed the same day.** The user bisected it to ONE mod (`60. Pacific Park Rotating Ferris Wheel`) and handed over both complete installs, which turned the rest into a diff. Its `Remove original/` folder holds empty-clump REPLACEMENTS, not a delete list — deleting the entries left 5 stock models declared by `LAw2/LAxref.IDE` and placed by 23 inst rows with nothing in any archive to stream, so the request could never complete. Fixed in [mod-installer/010](../../tools/mod-installer/docs/plans/010-remove-original-is-a-replacement.md); `install()` now THROWS on a declared+placed+unloadable model (reads 0 on the working install, 5 on the broken one). **The defect was in this very row's own triage, under "NOT this symptom"** — 5 definitions with no DFF, measured, written down, and mis-ranked because the clean game's single dangling model is placed NOWHERE. Left open on purpose: 10 stubbed beach seats keep their collision. Original triage: a clean game plus **only `.work/1-mods`** reproduces it, with the install's BASE ini — so every stage after the mod installer is exonerated, and so is our procobj layer in every form we tried. Measured: the mods stage takes `gta3.img` 897 → 1 185 MiB, and **all of the growth is TEXTURES (557 → 805 MiB, +45 %)** while `StreamMemoryForced` stays 1024. Archives are structurally clean (no dup names, no zero-size, none past EOF, no fragmentation) and the weight is AUTHORED — the mods ship those TXDs byte-identical. Ruled out by measurement: ID pools, duplicate ids, `StreamingObjectInstancesList`, and the two emptied stock interior IPLs (our own lossless slot compaction, now test-pinned). **Four wrong axes are recorded in the doc so nobody re-walks them** — including a mod bisection that ran without a negative control and 'found' a culprit a one-file mod reproduced |
| **Building LODs that never draw on the `sa` build** (~6 places citywide, always the same) | [fixed/sa-lod-visibility-budget.md](fixed/sa-lod-visibility-budget.md) | ✅ FIXED 2026-08-17, full rebuild field-accepted — ONE report was THREE defects, none of them a budget: (3) the rebuilt BinMesh's SPLIT ORDER (a blended split moved out of last place; `c754efdb`), (2) a multi-atomic `anim` HD byte-copied into an `objs` LOD, of which SA keeps ONE atomic at the origin (`977a2c1d`, merged now), (1) DXT clone textures with a side not a multiple of 4 — the real game refuses the WHOLE dictionary (`875cd8ed`, pow2 everywhere we encode + the optimizer resizes any such texture, mods' own included). Rounds 1–10 are falsified history kept on purpose; every diagnosis after round 8 was a one-model in-place swap (`model-lab.ts` / `img-patch.ts` / the census scripts), no rebuilds |
| **Texel smear on flat surfaces** — R\*'s own collapsed UV data, ships as authored | [texel-smear-authored-uv.md](texel-smear-authored-uv.md) | 🟡 shelved 2026-08-11, the day its one repair was built AND field-retired ([postmortem](../postmortem/uv-stretch-repair.md)) — a partial repair of a continuous defect reads worse than the smear, and the authored intent is not in the data (fit residual ~1.8 UV: nothing to restore into). 127 models / 134 placements after the world gates; the diagnosis and scanner stay valid. Known-viable exit: hand-authored UV fixes shipped as a data mod — shape undecided, not opened as an idea yet |
| **Objects on `sa` render with NO LOD** (`gaz9_law` fixed; `laehospital1`, `road_lawn33` unexplained) | [fixed/ipl-row-removal-breaks-lod-links.md](fixed/ipl-row-removal-breaks-lod-links.md) | ✅ **FIXED 2026-08-16, one cause behind both halves.** A text IPL's lod column is a ROW INDEX, and the mod-merge path rebases it correctly — but `0. Map Fixes Pack`'s own STREAM merges carried the AUTHOR's indexes, and stream merges run LAST, so they wrote over that rebase. `gaz9_law`'s link pointed one row past its LOD, at a tree the same pack had exiled to z = −300. The "second defect" (three `LAw.ipl` rows wearing a neighbour's transform in float64) is the SAME index read again by `sa-lod-generator`'s LOD-transform retarget, not a lost column — and the earlier claim that no mod ships `LAw.ipl` was simply wrong. 15 shifted `.merge` pairs repaired (all 15 land on their owner at 0.0 u once the author's shift is dropped), 11 broken links map-wide → **0**, and the class is now a hard build guard (`assertLodLinks`, stock: 6 103 links / 0 findings) plus `scripts/debug/lod-link-check.ts`. `merge-gen-mod` converts a mod's text + streams in one gated pass so it cannot recur. **`laehospital1` and `road_lawn33` resolve cleanly and stay unexplained** — if the field still sees them bare it is a different defect |
| Collision defect at the `road_lawn34` × `road_lawn01` crossroads | [road-lawn-crossroads-collision.md](road-lawn-crossroads-collision.md) | 🔴 open 2026-08-11, **lead found, not fixed (his call)**. His follow-up — "something very big slid or duplicated onto the junction" — points at `standard01_lawn`, and in the DATA that building has not moved and is not duplicated (one placement at stock's position, LOD link resolves, nothing new within 150 u). What changed is its COLLISION: `0. Map Fixes Pack`'s `lawn_4.col` contains it and re-authors it **427 faces + 31 boxes → 1173 faces, 0 boxes** (its DFF is `18. Project Lumos`); the roads' own COL is untouched. Bounds are unchanged though, and the footprint does not reach the junction — so either the reported position needs pinning down or a second model is in it. Cheap split: a build without `0. Map Fixes Pack` |
| **Crash on LOAD GAME** — the dummy pool fills and `RemoveIpl` cannot empty it | [fixed/sa-load-game-crash-dummy-pool.md](fixed/sa-load-game-crash-dummy-pool.md) | ✅ **FIXED 2026-08-19** by `perfect-map.asi` [plan 011](../../asi/perfect-map/docs/plans/011-ipldef-dummy-range.md) — the int16 `IplDef.firstDummy/lastDummy` pair wrapped negative past pool index 32 767 and `RemoveIpl` skipped the slot's whole dummy pass; now the same int32 sidecar as the building pair, two detours overlaid on FLA's. Field: 8 world entries at `Dummys = 100000` (6th used to die), 5 at 50 000 (3rd used to die), the pool's high-water frozen at the first entry's 40 960. **Kept `Dummys = 100000`**: the first entry alone peaks in [40 960, 49 151] against 33 043 map rows, so 40 000 crashes during it and the pmb guard cannot see that |
| **SCRASH2: a model of the LOCATION is missing** (cutscene scenery, suspected mod-introduced) | [scrash2-location-model-missing.md](scrash2-location-model-missing.md) | 🔴 open 2026-08-15, **reported, not investigated**. Surfaced during the plan-004 cutscene sweep and is NOT a `vehicle-cutscene` defect — that scene's CAR passed its row. Already narrowed for free: twelve scenes share SCRASH2's exact site (the Doherty garage, SF) and **five of them passed on the same build the same day**, so "the location is broken" is out; `GARAG3A` is the control that matters (same site, same interior area 0, passed). The method is written into the doc because it will be picked up cold — the CLEO cutscene-override instrument, a before/after-mods A/B, then a bisect for the mod that changes that location (the user recalls something like *Doherty*; no mod folder carries that word today, so treat it as a hint). **The doc also carries the trap**: a mod bisection without a negative control already 'found' a wrong culprit once on this project |
| **The OpenSA frame "lost 120 fps" on the 2026-08-17 build** — GPU pass ×2.5–3.3, four in-game arms | [fixed/opensa-gpu-pass-regression-2026-08-17.md](fixed/opensa-gpu-pass-regression-2026-08-17.md) | ✅ CLOSED 2026-08-18 on step 1 — **two lanes read as one**: the UNCAPPED headless sweep on the fresh pak reads `country-dusk` ×1.09 and the city scenes ×1.5–1.7 (the fleet, geometry-proportional), and the user's own display lane already read `country-dusk` 12.5 ms on 08-09 before any suspect change. Residual world cost on his lane, fleet pinned: +7–17 % city, −4 % country. Not a defect; the lesson is in `docs/benchmarks/readme.md` |
| Crash entering a freshly-spawned car (`readBody` null body)        | [vehicle-enter-null-body.md](vehicle-enter-null-body.md)               | shelved, NOT currently reproducing (2026-07-24) — a streaming/physics handle-pool race (teleport-triggered); needs a runtime trace to pin. Kept open; `a16930e` (enter/sit clips) did NOT fix it (animation vs physics)                                                                                                                                                                                                                                              |
| "Ghost barriers" — mass map instances corrupt real SA              | [fixed/ghost-barriers.md](fixed/ghost-barriers.md)                     | ✅ ROOT-FIXED by our `perfect-map.asi` (2026-07-09): the int16 `IplDef` ceiling is LIFTED via sidecar hooks — any object count works                                                                                                                                                                                                                       |
| 2dfx particle emitters on generated LODs (new-game crash)          | [fixed/lod-2dfx-particles.md](fixed/lod-2dfx-particles.md)             | ✅ crash ROOT-FIXED in-engine by `perfect-map.asi` (2026-07-09, plans 008/009: null-`m_SystemBP` guard); particles KEPT on LODs by default, confirmed in-game                                                                                                                                                                                              |
| Own engine: short-fog NIGHT renders pure black (rain/fog weathers) | [fixed/engine-night-sky-black.md](fixed/engine-night-sky-black.md)     | ✅ FIXED 2026-07-18 — SA authors NEGATIVE timecyc cloud colours; the driver's `lin()` power curve made them NaN, and a NaN in the frame UBO poisons WGSL `mix()` even at factor 0 (sky AND fog). Fix: floor at 0 + regression test                                                                                                                         |
| Carcer City D3D9 texture formats (rainbow / invisible models)      | [fixed/carcer-d3d9-textures.md](fixed/carcer-d3d9-textures.md)         | ✅ FIXED 2026-07-24 — a 2026 TC ships D3D9 TXDs: **DXT4** (mis-classed as 16-bit → rainbow) and **X8R8G8B8** (unused `X` byte copied as alpha 0 → invisible). Decoder now maps DXT2/4→dxt3/5 and forces X8R8G8B8 opaque; synthetic + real-asset fixtures (`fixtures/custom/txd/carcer-*`). Needs a pak rebuild                                                 |
| Off-road grip applied, still feels like tarmac                     | [offroad-feels-like-tarmac.md](offroad-feels-like-tarmac.md)           | 🟡 shelved 2026-07-27 by the field's call — NOT a plumbing bug: F2 shows `p_grassmid1 ×0.71` under the wheels and the `grass-corner` A/B measures −27 % top speed / −38 % yaw. A grip CEILING is invisible below the limit; what SA has and we do not read is `SAND` ("tyres sink and can get bogged down"), `ROUGHNESS` and `bOffroadAbility`. Options + the numbers in the doc ([081/10](../plans/081-vehicle-physics/10-surface-types.md)) |
| PAL4 (4-bit palettized) TXD textures                               | [pal4-textures.md](pal4-textures.md)                                   | 🟡 guarded 2026-07-24 — `expandPalette` assumes 8-bit-index/256-entry, so PAL4 (2 indices/byte, 16 entries) would decode half-size + mostly black. No shipped asset uses PAL4 (scanned original/carcer/gostown → 0), so the decoder now REJECTS it (clean skip) instead of corrupting. Implement the nibble unpack if a TC ever needs it                    |
| Cabin elements draw OVER the car's glass (crisp speakers/gauges through the windows) | [fixed/vehicle-glass-see-through.md](fixed/vehicle-glass-see-through.md) | ✅ FIXED 2026-08-04 (comet), field-verified same day from the reporter's own angles. TWO defects shared the symptom and the second masked the first: opaque interior parts on an alpha ATLAS were classed translucent by the whole-texture answer (now judged over the submesh's own UV region, `hasAlphaIn`), and the translucent sort's `centroid − radius` key over-reached on scattered submeshes (the dash cluster, radius 1.8, beat the equally-distant window sheet; now an exact eye-to-AABB key). Same-day sibling: a mod's separate door-glass atomic now swings with its door (`VehicleDoor.parts`, hinge subtree). Re-report 2026-08-17 (defect 3): a SCATTERED translucent submesh (dash gauges + shelf speakers on one material) has no honest key — the builder now splits translucent groups into spatial clusters, each with its own AABB |
| A modelled car interior: LIGHTING wrong (bright dash, black cabin at night, red braking) | [vehicle-cabin-lighting.md](vehicle-cabin-lighting.md) | 🔴 open 2026-07-28 — three symptoms on one mod car (previon). The first round of fixes was built and REVERTED the same day ([postmortem](../postmortem/090-vehicle-cabin-at-night.md)). Everything measured so far is offline data (the cabin's dash trim comes out CHROME at coefficient 0.5, sky 0.63; its night vertex set equals its day set at 255 white; its seat occlusion is noise, not bands) — and **no in-engine capture has ever been taken**, which is where the next attempt starts. Re-shoot on a REBAKED car first: the 2026-08-04 see-through fixes changed the blend roster and order it was measured against |
| Parked cars do not respawn after you leave and return (empty lot) | [fixed/parked-cars-do-not-respawn.md](fixed/parked-cars-do-not-respawn.md) | ✅ FIXED 2026-08-02, the day it was field-reported. Three defects, one symptom: a car was CREATED past the collision radius (`lodDistance` 250 vs `collisionDrawDistance` 150) so it free-fell; the LOD system measured its unload distance and respawn trigger from the car's LIVE position while respawning it at its PLACEMENT, so a fallen or shunted car took its spot's trigger away permanently; and `.catch(() => undefined)` meant none of it could ever be seen. `parked.json`'s 212 cars are now registered rather than all spawned at boot. The bigger half was found in the same hour and also fixed: plan 059's 1043 map car generators had been wired to NOTHING since `a312f0d`, which is why the map held 24 models; both populations now announce their size at boot, and a re-drive confirmed spawn/despawn/respawn in the field |
| Three mod ide rows lack the model/txd comma → duplicate id rows (593/585/586) in the built `vehicles.ide` | [fixed/vehicles-ide-missing-comma-duplicate-rows.md](fixed/vehicles-ide-missing-comma-duplicate-rows.md) | ✅ FIXED 2026-08-17 the day it was found — `splitRow` reads commas + whitespace as one separator class (`LoadLine`'s rule) everywhere IDE/IPL rows are split, `mergeIde` drops stale twins; both trees rebaked for the three cars, no duplicate ids left |
| **A folded mod `inst` row is read BEFORE the mod IDE that defines its id** (`gta.dat` order) | [mod-inst-rows-folded-before-their-ide.md](mod-inst-rows-folded-before-their-ide.md) | 🔴 open 2026-08-18, **root cause pinned, no fix**. `mergeGtaDat` APPENDS a mod's `IDE`/`IPL` refs, then the slot fold moves its `inst` rows into stock hosts — so `LAs.ipl` (line 93) places id 12780 whose `missing smokes fix.ide` sits at line 158, and `CFileLoader::LoadLevel` reads the file top to bottom. 39 rows on that one id. **modloader masks it in the bottle** by supplying the same mod IDEs itself, which is why it has never been reported; it surfaced the moment modloader was disabled as a diagnostic arm. Every check we have looks at the SET (defined? streamable? row index?), none at the ORDER. Fix: splice mod IDE lines before the first `IPL` line, then guard the pair positions at build time |
| **The `sa` build stopped BOOTING after a delivery** (heap fault in a DFF material-list read) | [fixed/sa-boot-crash-fla-pools-reverted-by-delivery.md](fixed/sa-boot-crash-fla-pools-reverted-by-delivery.md) | ✅ FIXED 2026-08-18, field-confirmed the same hour — the delivery copied the whole tree ROOT, so `mods-src`' copy of `fastman92limitAdjuster_GTASA.ini` (which never got the 2026-08-10 field raise) took the install's FLA ID pools from `TXD 6000 / COL 400 / IPL 1024` back to `5000 / 280 / 256` against a build carrying **5 177 `.txd` archives**. Restored in `mods-src` (the only durable place) and it boots. **Six false leads are kept in the doc**, including the plan-011 `.dat` block, the nine-car rebake, and a full `sa` rebuild that proved the pipeline byte-deterministic. The guard is fixed in the same session: `checkImgIdBudgets` reads the pools off the ini the build ships (`flaIdPools`), falls back to FLA's defaults for a `#`-disabled line / an unapplied patch / a missing ini, and names the file — a guard number ABOVE the install's can only fail to fire |

