# 201 — The dispatch console: a CAD map, trimmed to what it draws, on a phone

> **This chain is subordinate to [202 — PCAD Dispatch](../202-pcad-dispatch/readme.md)**, the final plan.
> That document is the product — a web dispatch application for a SA-MP server, paired with a client-side CAD
> plugin — and OpenSA's role in it is exactly one component: **the 3D map**. Everything below is the engine
> and map work that component needs. Read 202 first for why any of it is being done.

**The engine's second consumer, declared** (priority set 2026-08-06). `apps/dispatch` is a computer-aided-
dispatch operator surface over the streamed world — a top-down 3D map, live units, a call queue, and
click-to-inspect that answers with the model and TXD names the pak was built from. It arrived in a single
commit (`d57c92d`) with one write-up ([features/dispatch-console.md](../../features/dispatch-console.md))
and **nothing else**: no plan chain, no roadmap row, and not a word in
[project-goals](../../project-goals.md), every line of which was written for one consumer — the game.

Two facts make that gap expensive right now.

**This is the surface that reaches a phone.** It is the only one in the repo that does. The game cannot: a
pak built from SA assets is BC throughout and no mobile GPU has BC, which is the whole reason
[200](../200-platform-reach/readme.md) exists. The console already opens an `--rgba8` world today — no gate,
no pending concept. Meanwhile the project's entire mobile evidence base is **one synthetic row**, and 200
says so itself: *"nothing here has been run on the phone"*. Its step
[1/04](../200-platform-reach/1-device-truth/readme.md) deliberately refused to write a residency ceiling
rather than fit a constant before a device was measured. This chain is how that measurement gets taken.

**A map view never reads part of what it ships — and only that part may be cut.** The direction is to
optimise the engine for the 3D map without the extra, and the second half of that sentence is the load-
bearing one: **cars and peds are drawn, the palm sways, the day turns, the weather colours the world.** That
is what makes a 3D map a 3D map instead of a tile stack, and it is what
[directive 6](../../project-goals.md#6-the-target-is-a-aaa-grade-game-and-that-is-a-measurable-claim) calls a
world that is alive. What may be cut is the dead half: pak entries no frame of this surface ever requests,
passes with no consumer on screen, and bundle code that never executes.

## The decisions this chain is built on

Taken 2026-08-06 with the user; every step below inherits them.

| Decision | What it rules in | What it rules out |
| --- | --- | --- |
| **The SA world only** | the console reads the same pak and the same authored data the game does | geo import (OSM / tiles / CityGML), a CRS layer, CAD over a real city |
| **The console leads the mobile work** | phone work is ordered by the operator surface, because it is the one that runs there | the game shell's touch chrome setting the order |
| **The dispatcher is a player** | the operator is a player on the server, on one or two monitors plus a phone; the units are players too, and there is no decorative population on the map | a desk product for staff outside the game |
| **The data comes from a native CAD plugin** | that plugin is the source of calls and the channel to units; the console stays a separate web application beside the game | the console inventing calls, or owning voice/chat |
| **The board stays a mock for now** | `stepOperations` remains the feed; the seam stays a seam until the plugin is there | a backend or a socket protocol in this chain |
| **Three ways to draw the world** | live render, a baked 3D city map, a flat 2D map — the operator picks, with an automatic floor | a silent quality ladder |
| **Time is an axis, not a field** | the shift can be scrubbed, units leave trails | extrapolating past what the feed sent — and, since 2026-08-22, ~~interpolating between fixes~~ too: a track answers with the last fix, on the user's call ([8/02](8-the-time-axis/readme.md)) |
| **Cut only what is dead** | one declared map profile, over the build and the frame, removing what this surface **provably never reads** | cutting whatever looks "game-ish": cars and peds are drawn, vegetation sways, the day turns |
| **One engine, PC and mobile** | shared code and a shared frame; the platform difference is a **budget**, not a feature set | a forked renderer, a "mobile shader path", a second codebase |
| **UI is designed, not eyeballed** | the design skills are loaded before layout and colour; tokens live in `apps/dispatch/src/ui/styles.ts` | picking colour and density by feel, styles scattered per component |
| **The MAP is optimised first, and THE FIELD RUN is the map** (the user's call, 2026-08-31) | the field run carries **no board** — `units=0&calls=0&inventory=1` — so every window the map's own work is judged in is a window with nothing drawn over it but its own layer; the declared 150 units keep their own link and their own turn | measuring the map through a 150-unit symbology layer, and taking 5/02's or 5/04's numbers before the map is the shape we want it |

## The evidence this chain is answering

Every number is already in the record.

| What | Measured | Where |
| --- | --- | --- |
| Phone, synthetic world | 41 fps, 162 draws, 37 MB resident — Mali-G51, 360×800 @ DPR 2, `?demo=1` (no streaming, no LODs, no water) | [benchmarks, mobile row](../../benchmarks/index.md#mobile) |
| Phone, emulated | Pixel 7 412×839 @ DPR 3, 38–46 fps under SwiftShader, 576 recorded draws, 44/144 cells visible | [features/dispatch-console.md](../../features/dispatch-console.md#verification) |
| Phone, real world | **taken 2026-08-23 on the PINNED district** — MGA-LX3 / ARM Bifrost, 403x334 CSS @ DPR 2, ASTC pak, 605 drawn frames over 64.8 s with **599 skipped by the idle gate**: CPU body mean 3.43 ms (5.8 % of the interval), **worst frame 600.3 ms of which overlay-2d 422**, draws 112, **resident 76.1 MB**, 4 chips of 12 dropped. Idle-dominated, so it does not yet answer the 60 fps budget | [the 08-23 row](../../benchmarks/index.md), filed and committed by the phone itself |
| Phone, the operator surface | **2026-08-25 at 360x800 CSS, DPR 2, coarse pointer**: the layout measured **403 px wide** in a 360-px screen (a bare `1fr` track), 9 controls under 44 in one axis, the tool cluster ~380x390 over a ~350-px map. After: 0 overflowing, 0 under 44, map 500 px; landscape 740x360 map 98 → 210 px | [3/01](3-the-operator-surface-on-a-phone/readme.md), guard in `apps/dispatch/src/ui/styles.test.ts` |
| Desktop, populated drive | 1004 slow frames, p50 21.3 ms · **GPU pass mean 15.64 ms against a CPU render of 0.1–0.6 ms** | [091](../091-frame-time-attribution/readme.md) |
| Cold entry into a district | first frame `cell-collision-read` **235 ms**, then ~20 frames of 110–170 ms | 093 sweep |
| Boot frame | **576.1 ms** | 091 |
| The shareable console | ~490 kB of ASCII-escaped JS, single-file **for `?demo=1` only** — the pak worker is a separate `assets/pak-worker-*.js` chunk | [features/dispatch-console.md](../../features/dispatch-console.md#verification) |
| BC pak reference | 1,272,901,632 B at 1137 cells | [universal-texture concept](../../postmortem/universal-texture-transcode.md) |

Read together: the desktop engine is GPU-bound in steady state and main-thread-bound in transients, and the
phone has never been measured against real content at all. So the order is **cut the dead weight, then
measure the thing we intend to ship, then tune against that measurement** — never the other way round.

## The budgets this chain is held to

Named by the user on 2026-08-06, before the work. Every one of them is a specification, not a hope
([directive 5](../../project-goals.md#5-performance-is-a-requirement-not-an-outcome)).

| Budget | Value | Where it bites |
| --- | --- | --- |
| Units on screen, worst case | **150**, each drawn as a **model with a symbol over it** | 1/04, 5/02, 5/04 |
| Frame rate on a phone | **60 fps** | the whole of 1 and 2 |
| Time to a working picture | **≤ 3 s** | 2/01, 6/02 |
| Resident memory on a phone | **hard ceiling 300–500 MB** | 1/03, 2/04, 8/01 |
| First download | as large as needed **if it caches** | 2/01, and the offline cache deferred to 0.6.0 |

**These may not all be satisfiable at once, and the chain says so up front.** 150 textured vehicle models at
60 fps inside 300 MB is not implied by anything measured: the only mobile row in the repo is 41 fps and 37 MB
on a *synthetic* city with no streaming. Two honest outcomes exist — a screen-size threshold where a distant
unit drops to its symbol, or a mode ([chain 6](6-display-modes/readme.md)) in which the world is cheap enough
that the budget is comfortable — and [chain 2](2-real-device-truth/readme.md) decides between them with a
measurement rather than an argument.

## The chains, in execution order

| # | Chain | Why here |
| --- | --- | --- |
| 1 | [The map profile](1-the-map-profile/readme.md) | Dropping bytes nothing reads needs no device to justify it, and it changes every number the rest of the chain is tuned against |
| 2 | [Real device truth](2-real-device-truth/readme.md) | The first real mobile row in this repo, and it should be a row of what we intend to ship. Hands 200 the ceiling it refused to invent |
| 3 | [The operator surface on a phone](3-the-operator-surface-on-a-phone/readme.md) | "It loads" is not "an operator can work it" — 360 CSS px, touch, legibility, and the no-GPU floor |
| 4 | [A console is not a game](4-a-console-is-not-a-game/readme.md) | A game is always moving; a dispatch map idles most of a shift on a device that runs hot and flat |
| 5 | [Symbology and picking as product](5-symbology-and-picking-as-product/readme.md) | The console's central interaction stands on a flag named `debug`, and its units are debug lines |
| 6 | [Three ways to draw the world](6-display-modes/readme.md) | Live, baked-3D and flat-2D. Two thirds of it already exists unrecognised, and it is what makes the budget above reachable by choice rather than by giving up |
| 7 | [The operator's map](7-the-operator-map/readme.md) | Orthographic mode, pitch clamp, flyTo, follow, bookmarks, fit bounds, a minimap, measuring, drawing, search, keys, embedding — what a map application has that a game does not |
| 8 | [The time axis](8-the-time-axis/readme.md) | Where time lives in the data model is cheap now and a rewrite later. Moved forward from 0.6.0 for that reason alone |
| **9** | **[The mobile frame, audited](9-the-mobile-frame/readme.md)** | **URGENT, and out of order — it runs NEXT.** Opened 2026-08-31 out of an audit of the console's own captures against the code that produced them: the phone sits at **21 fps against a declared 60**, and ~38 ms of a 48 ms frame belongs to something no span in this repo names. Seven fixes, most of them one constant |

## The steps, at a glance

40 steps, 9 chains. Execution order is chain order — except [chain 9](9-the-mobile-frame/readme.md), which is urgent and runs next. Inside a chain, step order. The one-line form — each
step's own section carries the budget it must fit and the number it owes.

| # | Step | What it produces |
| --- | --- | --- |
| **1** | **[The map profile](1-the-map-profile/readme.md)** — cut only what is provably never read | |
| 1/01 | The inventory | the before-table (passes × ms, entry kinds × bytes, modules × kB) + **the pinned district** |
| 1/02 | [The protected list](1-the-map-profile/protected-list.md) | what may never be cut: cars, peds, sway, the day cycle, weather, one engine PC+mobile — each against what carries it, and whether losing it is silent |
| 1/03 | The pak profile | a build omitting only what 01 proved unread; bytes and resident MB before/after |
| 1/04 | The frame profile | every pass judged *kept / cheaper / removed*, with its ground |
| 1/05 | The streaming profile | rings anchored to the map focus — **and the per-cell geometric error the pak lacks** |
| 1/06 | The bundle | dead code only; kB before/after on the single-file artifact |
| **2** | **[Real device truth](2-real-device-truth/readme.md)** — the trimmed console on real hardware | |
| 2/01 | A phone-sized district pak | `--rgba8` over the pinned district, profiled vs unprofiled |
| 2/02 | Serve it from the shareable build | close the pak-worker chunk gap so a real `?src=` streams |
| 2/03 | The field run | **the repo's first real-world mobile benchmark row** |
| 2/04 | The residency ceiling, derived | the number 200/1-04 refused to invent, handed back |
| **3** | **[The operator surface on a phone](3-the-operator-surface-on-a-phone/readme.md)** | |
| 3/01 | 360 CSS px | the layout spec, fixed before code |
| 3/02 | Gestures on real touch hardware | the three re-cast desk gestures, verified where a mouse cannot fail |
| 3/03 | Legibility at city zoom | a labels-per-frame budget and a collision rule |
| 3/04 | The floor, measured | plan mode (no GPU) on the same real phone |
| 3/05 | The chrome tells the truth | an honest fps and frame time, a live link status, and a metrics panel that folds |
| **4** | **[A console is not a game](4-a-console-is-not-a-game/readme.md)** — session and battery | |
| 4/01 | Render on demand | idle draws → 0, a named wake-latency budget, a battery delta |
| 4/02 | The long session | resident MB at 0 / 30 min / 2 h; bundle re-records |
| 4/03 | The wait before the first frame | a boot shell that leaves on a picture, a pak cache, async pipelines |
| 4/04 | Sway on an idle map | the idle RATE the field verdict asked for — a frame's rate is the rate of the fastest thing moving in it |
| **5** | **[Symbology and picking as product](5-symbology-and-picking-as-product/readme.md)** | |
| 5/01 | Picking off the debug flag | an honest pick capability with its memory cost stated |
| 5/02 | Units as instanced symbols | draws and frame time at the declared count |
| 5/03 | District names in the readout | the layer fork taken explicitly, and written down |
| 5/04 | Units get real models | cars and peds drawn; the fallback when a model is absent |
| **6** | **[Three ways to draw the world](6-display-modes/readme.md)** | |
| 6/01 | The baked 3D city map | the LOD tier as the only tier, lit like a map — against the live render |
| 6/02 | The flat 2D map | tiles from our own orthographic pass, shipped as one PMTiles archive |
| 6/03 | Switching | camera, selection and the moment survive; an automatic floor that says why |
| **7** | **[The operator's map](7-the-operator-map/readme.md)** | |
| 7/01 | Orthographic mode | the same matrix that bakes 6/02's tiles; culling correct under it |
| 7/02 | Where the camera may go | pitch clamp, zoom levels, `flyTo` that does not stall on streaming |
| 7/03 | Getting somewhere | follow a unit, bookmarks, fit bounds, search by place |
| 7/04 | The minimap | the 2D map at lowest zoom + a viewport rect — nothing like it exists in the repo |
| 7/05 | Measuring and drawing | distance, radius, perimeter — on **classification**, not tessellation |
| 7/06 | Keyboard | a base set plus remapping |
| 7/07 | Leaving the console | a link to a view, iframe embedding, image export |
| 7/08 | The workspace | the map becomes the desk; the lists become windows on it |
| 7/09 | Skins | four themes as data, and an APCA guard that makes four cheaper than one |
| 7/10 | Skins from the field | seven CADs read as skins — and the three layers of a screen a theme cannot carry |
| **8** | **[The time axis](8-the-time-axis/readme.md)** | |
| 8/01 | A position is a function of time | the track type, its sampling policy, its memory cost |
| 8/02 | Interpolation, honestly | between packets only — never past them |
| 8/03 | The clock and the timeline | scrub, ×2/×8, live; game hour vs wall time kept apart on screen |
| 8/04 | Trails | the last N minutes, on the same ground-following work as 7/05 |
| **9** | **[The mobile frame, audited](9-the-mobile-frame/readme.md)** — the urgent optimization fixes | |
| 9/00 | The ablation instrument | there is no `timestamp-query` on the device, so a pass is priced by REMOVING it and re-flying the route |
| 9/01 | The overlay's missing arm | the third A/B arm (cleared, not drawn) that says whether the ~21 ms is the LAYER or its content — plus the DPR-1 surface and the sprite cache |
| 9/02 | The mock publishes at the protocol's rate | the board tick from `PUBLISH_INTERVAL_MS` instead of 20 Hz, and what every earlier 150-unit row was really measuring |
| 9/03 | Board work on the board's clock | `beacons` and `unitModels` off the camera's rate, on the dirty flag the gate already computes |
| 9/04 | The frame's attachment set on a tiler | three one-constant arms over 48 bytes/pixel of tile working set, read off the vsync ladder |
| 9/05 | The post chain's pass count | the bloom level count derived from the render size; 19 render passes a frame priced |
| 9/06 | The per-frame bakes already cached one line above | `cloud-field` amortized like the sky LUT and the probe beside it |
| 9/07 | The per-frame allocations | a static sort cached, three scratch arrays hoisted, and a capability that retains what it never reads |

Above all of it sits [202](../202-pcad-dispatch/readme.md)'s phase order: **0** measure (1–2), **1** the three
modes (6), **2** speak PCAD's protocol (+8), **3** the map becomes vibecode's module 10, **4** what PCAD owes
back (publish rate, positions on foot). **Chain 9 sits inside phase 0 and is the reason phase 0 is not
finished**: the row exists, and it says 21 fps.

## What this chain does NOT own

[200](../200-platform-reach/readme.md) keeps all of it, and 201 duplicates none of it: universal texture
transcode and its concept gate, workers and `crossOriginIsolated` transport, the runtime read of the baked
collision, the WebGL2 fallback backend and its concept gate, and the game shell's touch controls. Where this
chain needs one of those, it says so and waits rather than building a second version.

The relationship runs the other way too: 200's phone-side steps are blocked on a device measurement nobody
has taken, and [2/03](2-real-device-truth/readme.md) is the cheapest way to take it.

## Ruled out, 2026-08-06

Recorded so they are not re-litigated, not because they are bad:

- **Real-world geography — the door is closed** (2026-08-06, asked twice). No OSM/tile/CityGML import, no CRS
  layer, and no "world source" abstraction kept open for one. GTA coordinates stay the coordinate system, and
  the one conversion that exists (`apps/dispatch/src/map/coords.ts`) stays the one place it lives.
- **Interiors and floors** — the map is the street. A unit that goes inside keeps its symbol at the door.
- **Roles beyond the dispatcher** — one role, everyone sees everything. No observer mode, no field-unit
  screen; the field unit's app is the native plugin's business, not this console's.
- **Decorative population** — only real players are drawn, so nothing on the map is ever mistaken for data.
- **A live operations feed, multi-operator, install + offline cache** — deferred to
  [roadmap 0.6.0](../../roadmap/0.6.0/plans/05-dispatch-cad-depth/readme.md). The feed's source is now known
  (the native CAD plugin), which is why the contract is worth writing before the transport.
- **Extrapolating unit positions** — see [8/02](8-the-time-axis/readme.md). A map that invents a position is
  worse than one a second behind.
- **Interpolating BETWEEN them** — added 2026-08-22 on the user's call, and it is the same argument one step
  further: at a 4 s publish rate a straight line between two fixes runs a car through buildings, so a track
  answers with the last fix. Reversible on a field verdict that stepping reads badly at a rate nobody
  managed to raise ([202 phase 4](../202-pcad-dispatch/readme.md) is where that measurement lives).

## Status

| Step | State |
| --- | --- |
| **WHERE THE NEXT SESSION STARTS** | **THE SPEC IS [`handoff.md`](handoff.md) — read it, then come back here for the step history.** **THE NEXT AGENT TAKES THE POST CHAIN**, and the first thing it must do is RE-FLY `nobloom`: the 7.7 ms that makes it the frame's largest item was measured on an app that is an ancestor of the change which moved the console's bloom to a half-resolution prefilter, so it prices a chain that no longer runs. **THE STATE, measured 2026-09-05 evening** at the settled pose `[1500,-1500] h900 pitch −1.15 yaw π`, `?surface=720x640`, pak `./build/phone-cars`: **empty map 20.2 ms / p50 16 / CPU 3.3 / 112 draws**, **declared board 23.8 ms / p50 18 / CPU 7.9 / 3 571 draws** — so everything the 150 units cost is now **+3.6 ms over the empty map**, and the 20.2 ms floor is the whole remaining budget. The empty map was flown three times across the session (20.03 / 20.43 / 20.25) and that 0.4 ms spread is what makes the subtractions worth having. **WHAT MOVED TODAY**: [instancing](../../benchmarks/opensa-engine/2026-09-05-mobile-vehicle-instancing.json) took the board from 11 810 draws to 3 571 with triangles identical; [the per-unit allocations](../../benchmarks/opensa-engine/2026-09-05-mobile-per-unit-allocation.json) took `sym:units` from 4.04 ms to 1.62 and the board's cost over empty from +6.1 to +3.6; the mark rule and the label rule (the operator's calls) cut 150 chevrons to what the shift is about, beacons and trails with them; `overlay-2d` and `symbology.render` are split permanently, which is what turned a 6 ms unattributed remainder into a number; and the panel's `behind` count fetches, having read 0 on a device three archives stale. **THE RULES THAT COST THE MOST**, in full in the spec: a change can ship completely INERT with every test passing (instancing did — the device returned 11 810 draws unchanged to the unit, because the tests exercised the mechanism rather than the configuration every caller produces); a monotonic sequence across alternating arms is WARM-UP and the first pairing will read as a clean win; measure the instrument before the effect (a ~100 µs `performance.now()` clamp read as a 15 % result it could not resolve); and fly the baseline twice in the same session. **AND THE BRANCHES ARE READ, 2026-09-05 — the answer is *almost nothing was lost*, and it is written down so it is not asked a third time** (spec §5b): six branches sat on `origin`, five were stale ancestors with nothing unique, and the sixth (`claude/chain9-desk-work`) carried earlier builds of **9-05 and 9-06 that are entirely SUPERSEDED** — its `bloomLevelsFor` makes the default cut a tail the sweep priced at **0.2 ms of noise**, and its `CLOUD_FIELD_HZ = 10` is chosen where `cloudFieldDue` derives the rate from texel travel. Both step sections now carry a do-not-resurrect note. What DID survive is a restriction worth more than the code — *a long-lived process serves the table it started with* — plus the debt it exposed: `query-parameters.md` documented **none** of this chain's nine parameters and still called `msaa` field-removed while 9-04 ships it. All nine are in the table now. **`git log main..branch` compares patch-ids and answers the wrong question**; ask whether the CONTENT is superseded. |
| the chain itself | **OPENED 2026-08-06** — declared, ordered, and the decisions above taken with the user |
| the requirements round | **CLOSED 2026-08-06** — the product, the budgets, the display modes, the camera and the time axis settled in one questioning pass; chains 6–8 exist because of it |
| [1/01 The inventory](1-the-map-profile/readme.md) | **DONE 2026-08-09 — all three tables, on the pinned ground.** [The before-table](../../benchmarks/opensa-engine/2026-08-09-mobile-pinned-district-inventory.json): 414 frames / 153.9 s on `los-santos-centre`, 4/4 cells, `warnings` EMPTY — the first capture that is both clean and on the district this chain pinned. It supersedes the 08-07 Ganton row as the baseline (that row stays a valid measurement of Ganton, and is not its before/after partner). **33 fps · p50 30.3 ms · 110 draws · 265 k triangles · 148 MB resident**, and the two things the collector was extended to answer: **the frame is WAITING, not working** — the main thread runs 5.83 ms of a 27.56 ms frame (21.2 %), idle for 21.7 — and **it is not a half-rate vsync lock** either, because dt spreads over thirty bins with humps at 8–15 ms (25.6 % of frames) and 30–39 ms (41.8 %); a world that produces a 10 ms frame is not locked out of one. So the 94 % that had no owner on 08-07 is now four fifths **downstream of the CPU**, and no amount of loop tuning reaches it. Inside the body the largest item is **`overlay-2d` at 2.44 ms — more than `engine-frame`'s 2.10** — while drawing nine units, which is a finding for [5/02](5-symbology-and-picking-as-product/readme.md) rather than for the profile. The pak's texture ceiling for the same district is filed beside it ([99.7 MB as built, 23.4 MB if ASTC](../../benchmarks/opensa-engine/2026-08-09-district-texture-budget-los-santos-centre.json)). **BUNDLE HALF DONE the same day** ([the table](../../benchmarks/opensa-engine/2026-08-09-dispatch-bundle-inventory.json)): `dispatch.html` ships **501.5 kB raw / 166.5 kB gzipped** over six chunks, and the surprise is what dominates it — **react-dom at 44.1 %**, more than the whole engine (35.5 %) and four times the console's own code (11.0 %). The largest engine file after `engine.ts` is `hosek-wilkie-data.ts` at 33.4 kB, a tabulated sky model in a surface whose sky is a backdrop — the one line item [1/06](1-the-map-profile/readme.md) should price first. Dead code: one unused export, and the tool had to be repaired to say even that (`apps/dispatch` was missing from `knip.json`, so knip had never checked this app and called its whole tree unused). Two things the map ships from elsewhere, both small and both worth knowing: `apps/engine-lab`'s synthetic city (2.4 kB, what `?demo=1` builds) and **`apps/web`'s camera maths** (1.8 kB — `map-camera.ts` imports `cursorRay`/`forwardFrom`/`dollyStep`/`panStep` from the GAME app; permitted by the boundary lint, and still the map leaning on the game shell). **The BYTES table's instrument landed the same day**: every pak read now goes through one counted call (`postPakFetch`, `packages/engine/src/stream/pak-traffic.ts`) — the eager texture load, the streamer's cells and arrays, the collision source, and the loose `water.bin` the worker never sees — so the report carries `bytes` by entry kind, in WIRE bytes and request counts. A kind absent from that list is one no frame of this surface ever asked for, which is the gap the whole chain is written against. **AND THE BYTES TABLE CAME BACK THE SAME DAY** ([the capture](../../benchmarks/opensa-engine/2026-08-09-mobile-pinned-district-bytes.json), 799 frames with the camera worked down to street level): **36.4 MB read in 28 requests** — texture arrays **20 of 20, 99.9 % of the pak's whole texture payload**, water 2.66 MB in one read, 4 hd + 3 lod cell entries of the 8 the pak holds, and **collision: ZERO requests against a 49 870-triangle bake**. So the profile's hunt for unread entry KINDS finds exactly one, and it is the one [1/03](1-the-map-profile/readme.md) already suspected: the map runs no physics and never touches the baked collision — confirmed in code as well as in the capture (`bootDispatch` never reads `setup.collision`). Everything else the build contains, this surface reads in full: on textures the lever is the FORMAT and the cap, never selection. **THE STEP IS CLOSED** — frame, bytes and bundle, all three on the pinned ground, all filed before they were read. What the second capture also showed and the collector cannot yet answer: a **1068 ms body** on one frame (against 185.9 on the baseline), which is a cell build or upload landing inside `engine.frame`; the collector keeps per-segment means and only a whole-body maximum, so per-segment maxima are the next thing it owes. **Both were answered by reading the ENGINE instead of the console** ([the audit](../../audit/dispatch-engine-api-usage.md)): `StreamingDriver.update()` has always returned `StreamStats` — blob-handler and upload milliseconds, worst-call maxima, creates, evictions — and the console kept `pendingCells` and dropped the rest; `Engine.ledger()` has broken residency down by category since 074/01 and the console never called it. Both are in the report now, along with the worst body's own segment breakdown. The audit also lists ten capabilities the console does not use yet, each against the step that will want it — `setManualCells` (a capture with a PINNED cell set is an A/B without camera variance, which both 08-09 captures lacked), `listCells`, `unloadAll`, the debug line sets, `setMissingHighlight`, the frustum tests, and the whole vehicle-model path 5/04 will need. |
| [1/02 The protected list](1-the-map-profile/protected-list.md) | **DONE 2026-08-09.** Six rows, written before the first cut and each anchored to what carries it in code or data — the vehicle/ped model path, the baked sway amplitudes plus `windStrength`, the timecyc hour the console already drives, the weather mood and the lit world, `reflectionStrength` + `EnvProbe`, and one engine across PC and mobile. **Four of the six are SILENT if lost** (a still world, a duller reflection and a flat mood all render fine and pass every test in this repo), which is what makes the chain's closing field verdict an instrument rather than ceremony. The list also records what is NOT protected and why that is a decision: baked collision, which 1/01 measured at **zero requests**, is 1/03's candidate — conditionally on [5/04](5-symbology-and-picking-as-product/readme.md), because the day units are simulated rather than kinematic it comes back. Every later step now closes with a line naming what it touched from the list, normally `nothing`, and a step that does touch one owes the cheaper version it built instead — never a removal |
| the ASTC baseline | **TAKEN 2026-08-12** — [the capture](../../benchmarks/opensa-engine/2026-08-12-mobile-pinned-district-astc.json), 685 frames on the pinned district, `warnings` AND `errors` both empty. **Residency 74.9 MB against 148 on the rgba8 side of the same A/B**, with the texture category at **25.81 MB** — a quarter of the same texels, and within 0.4 MB of what [the texture budget](../../benchmarks/opensa-engine/2026-08-09-district-texture-budget-los-santos-centre.json) predicted for ASTC4x4. That is 200/2-02 paying off, measured. **The second reading of the same ledger is the one 1/03 should act on: `target` at 36.54 MB is now the LARGEST resident category, larger than every texture in the district** — render targets are a cost of RESOLUTION, and no trimming of the world touches them. The capture was only possible after a fix in the engine: `requireFormatSupport` computed its demand as `format !== RGBA8 ⇒ BC`, so an ASTC pak passed the manifest gate and was then refused per array on a GPU that carries ASTC — 20 of 20 arrays, `cellsTotal` 0, and a capture that VOIDed itself. `OSTEX_FORMAT_FEATURE` was already the rule; the second copy of it is gone, with both directions tested and the defect reintroduced to prove they catch it. **BOTH OF ITS OPEN QUESTIONS WERE CLOSED 2026-08-12, and neither answer was in the world.** `cellsCreated` 2454 was never churn: `StreamStats.created/evicted/lateCreates` are running totals the driver never resets, and the collector SUMMED them — four creates counted once per frame for the rest of a 685-frame window. Read, never summed, now; pinned by a test that fails at 400-for-4 on the reintroduced defect, and filed as [a restriction](../../restrictions/architecture.md) because the wrong number is plausible, self-consistent and *grows with the window*, so a longer capture makes it more convincing. And `target` 36.54 MB is now attributed [to the byte](../../benchmarks/opensa-engine/2026-08-12-dispatch-render-target-attribution.json) — 23 textures summing to exactly the measured figure, **MSAA 4× being 23.99 MB of it (65.7 %), more than the whole district's textures cost after ASTC**. The reading that changes 1/03: **the category is FIXED COST** — resolution and sample count, nothing from the world — so it is the largest item only because the district is four cells, and it will be the same 36.54 MB on a full map while everything else grows. Largest, not first. The MSAA lever is [priced and not pulled](../../performance/deferred-optimizations/msaa-sample-count.md); the console gained `?scale=` and a `surface` block in the report, so the next capture states what it was drawn at instead of being annotated by hand. |
| [1/06 The bundle](1-the-map-profile/readme.md) | **DONE 2026-08-12 — and it CORRECTS 1/01.** [The table](../../benchmarks/opensa-engine/2026-08-12-dispatch-bundle-inventory.json): **506.5 kB raw / 167.6 kB gzip** over six chunks, against 501.5 / 166.5 on 08-09 — so 161 merged upstream commits reached this surface as **1 kB of gzip**, which is what the layer boundaries are for. Taken with an instrument that is now committed ([`bundle-inventory.ts`](../../debug/README.md)), because the 08-09 row was a one-off script and a before/after produced by two different scripts is not an A/B — and that is exactly how the hole was found: **the 08-09 table attributed 394.2 kB of a 501.5 kB bundle and lost `shaders.ts` (394.2 + 107.3 = 501.5 exactly)**. So the reading this chain has been carrying is wrong: `packages/engine` is **247.1 kB (48.8 %)** against **185.1 kB (36.5 %)** for react + react-dom + scheduler — the engine is the LARGER half of what a map downloads. What survives from 08-09 is narrower: `react-dom-client` at 170.4 kB is still the largest single FILE. **The step's own verdict: there is no dead code to cut.** knip's two unused exports are used inside their own files — the `export` keyword is what was dead, and un-exporting it saves zero bytes. What the bundle carries is live, and its one lever is [WGSL shipped as written](../../performance/deferred-optimizations/wgsl-source-text.md): comments and indentation are **22.1 kB gzip, 13.2 % of the download**, for a build-time transform that changes no shader — priced and NOT taken, because it moves every shader-error line number and cannot be verified without a GPU. |
| [5/01 Picking off the debug flag](5-symbology-and-picking-as-product/readme.md) | **DONE IN THE ENGINE 2026-08-12; the district number owed by the next field run.** `CellStore.debugPicking` is `CellStore.picking` — a named capability, no `debug` in it — and the three hosts that arm it (the console, the map viewer, the game shell's overlay) name the capability rather than the mode they think they are in. The [restriction](../../restrictions/architecture.md) is marked resolved in the same change. The half that made it more than a rename: **the capability had no price anywhere**, because what picking retains is CPU-side (the placement mapper's rows plus the cell index bytes a cell would otherwise drop after upload) while `Engine.ledger()` counts GPU residency — so every instrument in this repo reported it as free. `CellStore.pickingBytes` counts both halves and `?inventory=1` carries it as `world.pickingMb`, kept apart from `residencyMb` because they are different memories. Three tests, each half pinned separately and each verified by reintroducing a half-count. Still open and needing a device: `PLACEMENT_ROW_BYTES` is an accounting figure derived from the shapes allocated, not a heap reading |
| [5/02 Units as instanced symbols](5-symbology-and-picking-as-product/readme.md) | **THE COUNT IS DECLARED AND THE BOARD CAN REACH IT, 2026-08-21; the milliseconds are owed by [2/03](2-real-device-truth/readme.md).** Three things were true when this step opened and none of them was visible: the budget table's **150** existed in no line of code, the beacon buffers were a bare `MARKER_CAPACITY = 96` that **silently returned without drawing** past it (at 150 units a fifth of the shift would simply not have been on the map — no throw, no warning, no missing pixel), and **the board could not be loaded past nine units on any device**, so the number this step owes could not be taken at all. Now: `UNITS_ON_SCREEN = 150` in one place (`apps/dispatch/src/ops/budget.ts`, cited to the budget table above), the beacon buffers are allocated at it and **GROW** rather than drop — an allocation, never a ceiling ([directive 2](../../project-goals.md#2-legacy-limits-are-not-our-limits)) — with the growth counted into the report, and `?units=150&calls=40` seeds the board there. The desk half is [counted](../../benchmarks/opensa-engine/2026-08-21-dispatch-symbology-call-counts.json): at 150 units + 40 calls the overlay asked the canvas for **190 `measureText` and 190 `ctx.font` assignments EVERY frame** — a font-shorthand re-parse and a text measurement per chip per frame, for labels that never change — and now asks for **151 measures once and 0 thereafter**, one font assignment a frame. `fillText` is unchanged at one per chip: this step did not reduce the symbol count, and it should not — that is [3/03](3-the-operator-surface-on-a-phone/readme.md)'s decluttering rule. The report gained a `symbology` block (units, calls, symbols, chips, chips dropped, `measureText` calls, beacon capacity and growths) so the next capture **states the load it was drawn under** instead of leaving 2.44 ms to be read against an unknown symbol count. What is NOT done here and is named rather than quietly dropped: the units are still the chevron-and-chip pair on the 2D canvas, not an instanced draw, and whether that is needed at all is a question the ms at 150 answers first. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing. **AND THE MILLISECONDS WERE UNREACHABLE FOR A SECOND REASON NOBODY HAD SEEN, FIXED 2026-08-31.** The 2026-08-30 field run measured `unitsAsModels: 0` against `unitsAsSymbolOnly: 150` and it read as a thin convert, which two of the three named models were. The third was not: **stock San Andreas has no `copcarls`** — the LS police car is `copcarla` — and `patrol` is five of every seven generated units plus four of the named nine, so most of this step's declared count resolved to nothing on EVERY pak, `--vehicles all` included, from the day [5/04](5-symbology-and-picking-as-product/readme.md) landed. It survived a week because **the fallback is not a diagnosis**: a name this build cannot draw and a name no build can draw leave the same line in the log. Closed both ways — the board asks for `copcarla`, `phone.sh` converts the board's own three rather than the `admiral,infernus,comet` it carried from before this console drew models, and `DEMO_MODELS` owns that list with `seed.test.ts` failing on any drift (proven red on the old value). **So what this step owes is now only a flight**, and it is the first one that can answer it |
| [5/03 District names in the readout](5-symbology-and-picking-as-product/readme.md) | **DONE 2026-08-21 — and the answer was NEITHER option the step offered, because the question was posed wrong.** The console does not need the game layer's zone code: what `packages/game` owns is `ZoneNameSystem`, an ECS system tracking a PLAYER across frames, and this surface has no player. What it needs is one pure question — *what is at this point* — which is a property of `info.zon`'s FORMAT (the boxes nest; the SMALLEST containing one wins). So the rule moved to `zoneAt` beside the parser in `@opensa/renderware`, and three consumers read that one function: the game's HUD, the console's readout, and the pack that bakes the table. The game reaches it through a new `adapters/named-zones` — [the boundary lint](../../restrictions/architecture.md) caught the first attempt, which imported renderware straight into `zones/` — so `ZoneNameSystem` lost its private copy of the containment rule and this change **removes an owner rather than adding one**. **The fork question also hid the real obstacle, which was not layering:** `info.zon` holds GXT KEYS and the text is in `text/american.gxt`, two files of the game dir, and a surface streaming a pak over HTTP **has no game dir** — no import would have produced one name. So `opensa-pack` resolves them at build time into `districts.json` beside the pak (`manifest.districts`), the way the water bake rides beside it, and the general form is now [a restriction](../../restrictions/build-vs-runtime.md): a name taking two `data/` files to compute cannot be computed by a pak-only consumer. A click now answers **model, TXD, district and coordinates**; a call takes the world's own name for its spot; the hardcoded twenty-landmark table stays as the fallback and is labelled one. Cost: one small fetch at boot, a few tens of kB per build, and no districts at all on `?demo=1`, plan mode, an older pak or a game with no `info.zon` — a supported state, not a failure. `docs/contracts/mods.md` §7 records what a mod replacing either file changes, and that a misspelled GXT key ships the district under its own key, silently. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [8/01 A position is a function of time](8-the-time-axis/readme.md) | **DONE 2026-08-22.** `UnitTracks` — a ring of samples per unit, column-wise (`t` u32 ms, `x`/`y`/`heading` f32, `status` u8), so a sample costs exactly **17 bytes** and the figure is a fact rather than an estimate of what an array of objects weighs. **A store beside the board rather than a field on `Unit`, and that deviation from the chain's own sketch has a reason**: `Operations` is an immutable snapshot and `stepOperations` a pure reducer, and a ring buffer cannot be immutable at a per-tick cost — so `Unit.at` stays the state at `ops.now` and the store answers every other T. Recording happens OUTSIDE the React state updater, because a side effect inside one runs twice under StrictMode and a track written twice cannot be scrubbed. The sampling policy is three rules, all from the feed rather than from taste: one sample per **4 s** (PCAD's own publish rate, read out of its source — the mock ticks at 20 Hz and recording at tick rate would make its tracks five times denser than the live feed's), a status change always samples, and a stationary run collapses to its two ends. **The number it owed** ([measured](../../benchmarks/opensa-engine/2026-08-22-dispatch-track-memory.json)): **17.51 MB for 150 units × 8 h, and the accounting is EXACT** — `arrayBuffers` moves by 17.51 against an accounted 17.51, which is the check 5/01 could not run on its own figure. Two readings matter more than the number: **it is HOST memory and may not be charged against the 300–500 MB ceiling** (that is `Engine.ledger()`, GPU bytes — the report keeps `tracks` apart from `residencyMb` exactly as it keeps `pickingMb` apart), and **`heapUsed` alone reports 0.2 MB for it**, because a typed array's backing store lives outside the V8 heap — the first version of the measurement watched only `heapUsed`, which is plausible, self-consistent and wrong, the same family as the 2454 cell creates. **The collapse turned out to buy history rather than bytes**: the ring is pre-allocated, so every scenario pays 17.51 MB and what differs is what it HOLDS — 8.0 h when all 150 move all shift, **24.0 h at a plausible 25 % duty cycle**. `SHIFT_HOURS = 8` is [a recorded hack](../../hacks/dispatch-shift-length.md), not a decision: the rate either side of it is measured and the unit count is the user's; this one nobody named. **A review pass on 2026-08-22 found one defect in this step and it was the worst kind**: the status table was an array read with `indexOf`, which answers **-1** for a status the union does not carry — written into a `Uint8Array` that is 255, replayed as `available`, and sampled on EVERY tick because 255 never equals -1. Measured on the reintroduced defect: **1201 samples in a 60 s window against the 16 the policy asks for**, so a whole shift's ring burns in six minutes. Silent three ways over: no throw, no warning, a plausible status on screen. The ids are a `Record<UnitStatus, number>` now, so adding a status without one is a compile error. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [8/02 Interpolation, honestly](8-the-time-axis/readme.md) | **THE ENGINE HALF IS SETTLED AND THE STEP SHRANK — interpolation is OUT, the user's call, 2026-08-22.** A track answers with the last fix at or before the moment asked for; between two fixes it steps rather than slides. It had been built in 8/01 and was removed before anything on screen depended on it. The call is the same argument that killed extrapolation: at PCAD's 4 s rate a car at 100 km/h covers ~110 m between fixes, so a straight line between them runs through buildings — [202 §4](../202-pcad-dispatch/readme.md) already called that *"smooth, confident and wrong, worse on a 3D map than on a tile map because the world around it makes the error obvious"*. Stepping is the honest picture of a self-reported feed, and the cheap fix for the jumping is not ours — 202 phase 4 proposes raising the publish rate and says to measure that before building anything to compensate for it. Nothing on screen needed the slide either: the mock integrates at 20 Hz so the live map is already smooth, and a drag across an 8 h timeline moves about one sample per pixel. **The latency this step owed is now zero by construction.** What would bring interpolation back: a field verdict that stepping reads badly at a rate nobody managed to raise. **AND THE UI HALF LANDED 2026-08-22, closing the step.** A unit whose last fix has aged past one publish interval is drawn hollow rather than filled, fades with age, and carries that age on its chip (`4-XRAY-7 · 12s`). Hollow AND faded, because fade alone reads as distance on a tilted map and the difference between *far away* and *we have not heard from it* is the whole point. **Both thresholds are PCAD's, not ours** ([202 §4](../202-pcad-dispatch/readme.md)): 4 s is the publish rate, so a younger fix is not late; 300 s is when the backend itself sweeps the unit as stale. Between them it fades rather than flips — a fix does not become wrong at a threshold, it gets older — so nothing here was chosen by eye and there is no hack entry. The age needed the time axis to exist at all: a `Unit` carries a position, not the moment it was reported, and the live board's `now` is the console's clock rather than the feed's. **A regression this step caused, caught by reviewing the diff before it landed**: the chip label now changes every second, and keying the width cache on the whole label made 150 new entries per frame, blew the 512-entry cap and re-measured everything — **150 `measureText` per frame, forever**, against [5/02's steady-state 0](../../benchmarks/opensa-engine/2026-08-21-dispatch-symbology-call-counts.json). Widths are assembled from cached parts now and the steady state is 0 again. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [8/03 The clock and the timeline](8-the-time-axis/readme.md) | **DONE 2026-08-22.** **The whole of replay is one substitution, which is what 8/01 bought.** `useOperations` holds two boards — `live` is what the feed is doing and what the history records, `ops` is the board AT THE CLOCK — and everything downstream takes `ops` without knowing which it was handed, so a scrub needed no second rendering path: the map loop, the symbology, the panels and the detail card work unchanged. A unit and a call get different histories because they change differently: a unit MOVES and is sampled, a call does not move at all and changes STATUS a handful of times, so its history is an event list — smaller and exact. That is what makes the chain's own verification hold (*units where they were, calls with the state they had*). **The two clocks are labelled because the console was already confusing them:** the top bar's dial said `Time`, the one label it may not have now — it says **WORLD** (it turns the sky) and the new strip says **SHIFT** (it moves the board), with a loud **REPLAY** badge whenever the picture is not the current one. Playback stops at the newest sample rather than slipping into live — a console that silently becomes live while the operator reads the past has changed the screen without being asked. ×1/×2/×8, `Live`, and bookmarks. **The number it owed** ([measured](../../benchmarks/opensa-engine/2026-08-22-dispatch-scrub-cost.json)): a full-board resolve at 150 units + 40 calls over an 8 h history is **p50 0.071 ms, p95 0.193, worst 0.636** — 0.4 % of a 16.7 ms frame typically, 3.8 % at worst; a drag re-solves everything every frame and does not come near stalling. Named rather than assumed: React's re-render per scrub frame is not measured (the loop is immune, reading through a getter; the panels are not), and the calls' event lists are the one structure here that is **not bounded** — ~125 kB at an absurd 49 transitions per call, so nothing to do, but that is the thing to watch. **The same review pass found the replay's roster came from the LIVE board**, so a unit that went off duty vanished from the replay of the hour it worked — latent on a mock that never removes a unit, and certain on a feed whose units go off shift constantly. The history keeps its own roster now. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [8/04 Trails](8-the-time-axis/readme.md) | **DONE 2026-08-22, and there is no N.** The span is **derived**: a trail covers the unit's current LEG, back to its last status change — the question an operator is actually asking (where did this responding car come from since it was dispatched) and one a clock cannot answer. So the debt this step warned about, *a constant chosen by eye*, is not taken: there is no hack file here, and that is the point. The single number is a WORK bound (240 points, 16 min at the publish rate) capping what one very long leg may contribute to a frame; a normal 6-minute leg is 90 samples before the stationary collapse trims it. **Cheap, as 8/01 promised** ([measured](../../benchmarks/opensa-engine/2026-08-22-dispatch-trail-cost.json)): **p50 0.200 ms, p95 0.649, worst 1.055** for all 150 legs, resolved at the BOARD's rate rather than the frame's so the 60 fps loop redraws from the same object between ticks. What it produces is **51 points/unit, 7 500 segments, 176 kB of line buffer per frame** — that, not the resolve, is the figure to watch on a phone. **The clamp-to-ground this step shares with [7/05](7-the-operator-map/readme.md) is deliberately NOT built here**: trails ride at the fixed height the assignment lines already use, because inventing a clamp now would be building it twice, the second time properly and for both consumers. Flat is honest on flat Los Santos and visibly wrong on a hill; saying so beats a clamp nobody measured. **Reviewing the diff before it landed caught a regression the numbers themselves caused**: 45 000 floats of trail do not fit a set allocated for 150 markers (2 700), so the `trail` set grew five times on the first frame — and every growth counts into `grownSets`, the number 5/02's inventory panel warns on to say *the board went past its declared unit budget*. One frame of trails and that warning is permanently on and says nothing. The trail set has its own budget now and its growth stays out of the marker counter. Also kept: the first cost run reported a confident **0 points** for all 150 units, and the code was right — the probe landed exactly on a status change, and a one-sample leg is correctly no trail. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [7/01 Orthographic mode](7-the-operator-map/readme.md) | **THE DESK HALF IS DONE 2026-08-22; the frame cost is owed by a device run.** The plan view is `CameraState.orthoHalfHeight` plus `mat4OrthographicZO` — **one field and one matrix, not a second camera**: the view, the culling, every pass, the symbology and plan mode are the same code, and 6/02 will bake its tiles with this matrix rather than a second one that drifts. `?proj=ortho` or the `PLAN` button; the projection rides in the map camera's POSE, so a restored or shared view opens as it was left. **Three of the four things it had to settle are invisible if you get them wrong, which is why the step is more than a matrix.** The box is `distance × tan(fov/2)` — what perspective frames at the focus plane — so a switch is a projection change and not a jump. The front plane sits as far in FRONT of the focus as the far plane sits behind it, which a perspective near plane cannot express and which is what stops a tower taller than the eye being sliced off at block zoom (depth ≤ 24 km, ~1.5 mm on a `depth32float`). **Picking swaps halves** — perspective fans the DIRECTION out of one eye, orthographic slides the ORIGIN across the image plane — and reading it the perspective way silently lands every pick under the middle of the screen. **And the overlay's "behind me" test was clip `w`, which is 1 for the whole world under an orthographic projection**: a unit behind the operator would have kept its callsign on screen, so both projections now read the view matrix (`depth` is untouched for perspective, where `w = −z_view`). Culling needed nothing: `frustumFromViewProj` is Gribb–Hartmann over any view-projection, and the ortho planes come out parallel — pinned by a test, beside the case that reads the two apart (a sphere 60 units off-axis and 500 deep is inside the perspective frustum and outside the box). **Stated rather than discovered:** fog, specular and the sky read the eye POINT, exact under perspective and an approximation under parallel rays — fog is invisible while the console pushes the cut out (`?fog=1&proj=ortho` is where it shows), and the SKY keeps the approximation on purpose, because a truly parallel view has one direction and an honest orthographic sky is a flat single colour. No shader branch either way. **And the capture now says which arm it is**: `camera.projection` joins `at`/`height` in the inventory report, because a plan-view frame and a perspective one cover different amounts of world at the same pose — the standing rule that an A/B is self-describing, applied before the measurement rather than after it. Desk evidence: 534 tests green, 21 of them new or moved (`math.test.ts`, `map-camera.test.ts`, the new `projection.test.ts`). Touched from [the protected list](1-the-map-profile/protected-list.md): nothing **THE DEVICE NUMBER CAME BACK 2026-08-30, AS A BAND RATHER THAN A VERDICT** ([the capture](../../benchmarks/opensa-engine/2026-08-30-mobile-lens-ab-circuit.json)). The two legs fly the IDENTICAL circuit — four poses, two laps, one height, one tilt, one heading — back to back in the same page, so only the lens differs. Raw: perspective mean **28.8 ms** (p50 26, p95 52), ortho **36.3** (p50 32, p95 72). **The confound is the size of the difference and is stated rather than buried**: the phone's browser chrome collapsed between the legs and the surface grew 460 800 → 622 080 device px (+35 %), which is fragment cost only the ortho leg paid — normalised by area the order REVERSES (62.5 vs 58.3 ms per megapixel). So what this round supports is that **the lens is not what costs**, within a band it cannot tighten; a number for either needs a re-run with a pinned surface. The same three surfaces gave the useful constant instead: the render target is **~69 MB per megapixel** (70.2 / 69.1 / 68.4 over 0.46, 0.62 and 0.82 Mpx), so on a phone the URL bar collapsing moves residency by 24 MB with no input from the operator. |
| [7/02 Where the camera may go](7-the-operator-map/readme.md) | **THE DESK HALF IS DONE 2026-08-22; the flyTo-vs-stream number is owed by a device run.** Three parts, and the two constants they replaced are the argument for the step: the pitch floor was `-0.35 rad` and the zoom-out cap `7000` units against a **2200-unit LOD ring**, so one drag and one wheel turn framed ground nobody had loaded — a map that ends in mid-air and reads as a broken pak. **Both bounds are now derived from the world's own reach and re-taken whenever the FRAME moves, not only when the tilt does**: the shallowest tilt is the one whose top edge lands inside the ring (perspective `d·sin t / tan(t − f)`, orthographic `half / sin t` — one bisection over a monotonic function, 32 fixed halvings), and the widest view is `reach / tan(fov/2)`, because at the top-down limit the frame's half-span IS `d·tan f`. `reach` is the world's number: the LOD ring streamed, the demo's own extent, nothing in plan mode. **The three zoom levels are things the world is made of** — one render cell (`CELL_SIZE`), the baked district box under the view (5/03's table), everything around the focus — with the GEOMETRIC mean as the fallback when a pak carries no zones, since zoom levels are logarithmic; keys `1`/`2`/`3`. **`flyTo` is Van Wijk & Nuij's path** ([links](../../links.md)), the one MapLibre flies: the camera pulls up, crosses and settles, and the duration is the path length in zoom space over a speed in screenfuls per second — derived rather than picked, ρ = 1.42 being the paper's own user-study value. That arc is also the honest answer to *do not stall on streaming*: a long trip crosses at a zoom where the resident far-LOD covers it, the destination streams as the focus approaches, and the same zoom cap clamps the arc's widest point, so the flight can never outrun the ring. Any input cancels a flight. **Writing the tests found a real defect**: the first bisection answered with the wrong side of the bracket and slammed the camera to straight-down for a request one degree too shallow — the bound is the SHALLOWEST legal tilt, not the steepest. **And a second defect the review pass caught before it landed**: the bound overwrote the tilt instead of narrowing it, so it was ONE-WAY — zoom out and the view tilts down, zoom back in and it stays down, and a flight's own climb would have cost the operator their viewing angle permanently. The camera keeps the tilt the operator ASKED for beside the effective one now, and the bound is re-derived from the request every time the frame moves (a drag still measures from what is on screen, so dragging up out of a clamped view fights the bound rather than eating invisible slack). Desk evidence: 551 tests green, 17 new (7 in the new `fly.test.ts`, 10 in `map-camera.test.ts`). The rule this produced is now [a restriction](../../restrictions/architecture.md) — SILENT, because nothing throws when a frame reaches past the ring. Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [7/03 Getting somewhere](7-the-operator-map/readme.md) | **THE DESK HALF IS DONE 2026-08-22; the streaming-at-speed verdict is owed by a device run.** Four capabilities, one mechanism — a target becomes a camera pose — and all four fly, because [7/02](7-the-operator-map/readme.md) already built the path. **Follow** rides the selection and takes the streaming anchor with it (on this surface the anchor IS the focus), with the damper **re-based on the subject** per [the restriction](../../restrictions/architecture.md) — the offset is carried across the unit's own motion, so a car at constant speed leaves it nothing to do rather than towing the camera at a fixed lag — and a time constant of **one publish interval over three** (`PUBLISH_INTERVAL_MS / 3`, PCAD's own 4 s), which is 95 % of any gap closed before the next fix lands: at most one fix behind, by construction rather than by feel. Smoothing the CAMERA is not smoothing the DATA — [8/02](8-the-time-axis/readme.md)'s rule stands, the marker still steps exactly as the feed sent it. **Bookmarks** are named poses in `localStorage`, shape-checked on read (a half-pose from an older version does not read as corrupt data — it puts the operator somewhere nobody asked for) and never throwing, because a console that will not boot for want of a bookmark is worse than one with none. **Fit bounds** frames every unit and open call with one render cell of air, and answers the step's own question about a set that spans the state: 7/02's zoom bound caps it, so the fit shows as much world as there IS instead of a picture of emptiness. **Search** is the consumer side of 5/03 — the world's own baked district table, case- and accent-folded, prefix matches first, and the boxes of one name UNIONED, since `info.zon` cuts a place into several and flying to a third of Vinewood is not flying to Vinewood. Keys `f` / `c` / `Escape`, plus the `ui/map-tools.tsx` cluster on existing tokens. A pan, a locate or a bookmark ends a follow; orbit, dolly and pinch do not. **Two defects the review pass caught before it landed**: a flight did not end a follow (a fit or a bookmark left the ride and the flight both writing the focus, the flight winning and the follow fighting it silently), and the degraded-map banner ran across the top of the map — which is where the cluster now sits, so in plan mode, the mode with the least else to work with, it covered the search box. Desk evidence: 577 tests green, 26 new (the new `bookmarks.test.ts`, plus `map-camera.test.ts` and `zones.test.ts`). Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| the cross-platform rule | **WRITTEN 2026-08-22, after breaking it three times in one session.** Chain 7's chrome (7/03's operator cluster, 7/06's nav cluster and key sheet) was desk-shaped: **24-px** map controls and **19-px** list rows against a 44-px minimum, and — the sharpest — **the three zoom levels existed only on keys `1`/`2`/`3`**, so on the phone this console is aimed at that capability did not exist at all. Fixed in the same change as the rule: one component with a `touch` size from `useCoarsePointer()`, level buttons in the nav cluster, saved views no longer dropped on a compact layout, and the clusters capped so they cannot crowd a 360-px map. The rule is now [`docs/restrictions/cross-platform-surface.md`](../../restrictions/cross-platform-surface.md) and a standing rule in `CLAUDE.md`: **a feature ships on a phone AND on a desk in the same change**, ≥ 44 CSS px of target where the pointer is coarse (WCAG 2.5.5 / Apple HIG / Material all agree), fits at 360 CSS px, reachable without a keyboard and without hover, and ONE component that takes a size rather than two layouts that drift. **Width and pointer are two questions** and answering one with the other is the usual mistake. **SILENT in every way this repo can be**: it typechecks, it lints, every test stays green — this is geometry, not behaviour — and it looks perfect on the desk it was written on. What is NOT closed by it: [3/01](3-the-operator-surface-on-a-phone/readme.md)'s 360-px layout spec, which is now the narrower question of whether two clusters, a sheet and a map still leave a usable picture on the real phone |
| [7/06 Keyboard](7-the-operator-map/readme.md) | **DONE 2026-08-22 — desk work end to end, and it swept up what the two steps before it left behind.** The map is **one table** (`map/keymap.ts`) that the input layer, the sheet and the rebinder all read: a key handled inline where it was convenient — which is what 7/02's `1`/`2`/`3` and 7/03's `f`/`c` were — is a key that exists nowhere an operator can read, cannot be rebound, and collides silently with the next one added. Defaults: pan `WASD`/arrows, turn `Q`/`E`, tilt `Shift`+`↑`/`↓`, zoom `+`/`-`, levels `1`/`2`/`3`, north `N`, fit `F`, follow `C`, calls `[`/`]`, stop following `Escape`, the sheet `?`. **Held is not pressed and they cannot share a path**: movement runs in the FRAME LOOP while the key is down, because acting on the OS's key repeat moves the map in the OS's stutter — a long gap, then a burst, at a rate the operator set for typing. The rates are the frame's own units where one exists: pan is **screenfuls per second**, so a key crosses the same share of the picture at every zoom; turn is a quarter lap per second, tilt half that, zoom a factor per second; opposite keys cancel and a diagonal is normalised. **Rebinding is a press, not a text field** — click a row, press the key — and only what DIFFERS from the defaults is stored, so a later change to the base map still reaches an operator who never touched that command. A key that is taken is taken AWAY from the other command rather than leaving two rows claiming it. **The on-screen controls** (`ui/map-nav.tsx`, top-right: compass, turn, tilt, zoom) are the same commands through the same handle, which is what makes them right rather than duplication — the phone has no keyboard, and the compass is the only thing on screen that answers *which way am I facing*. **Two defects the tests caught as they were written**: a key released while the window is unfocused never sends `keyup` (alt-tab mid-pan left the map panning by itself — the blur clears the held set), and a modifier pressed in the MIDDLE of a hold changes what the release resolves to (hold `↑`, press Shift, and the release reads as `Shift+↑` — `panNorth` stayed held with no key down). Desk evidence: 608 tests green, 33 new (`keymap.test.ts`, `keys.test.ts`, `map-camera.test.ts`). Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [7/07 Leaving the console](7-the-operator-map/readme.md) | **DONE 2026-08-22.** Three ways out, and the two things the step owed are stated rather than discovered. **The link** (`map/view-link.ts`) owns its parameter names and BOTH directions — a writer here and a reader elsewhere is how a share button produces a URL that silently opens the default view, and nothing throws. It carries the pose, **the projection** (7/01: the same pose is a different picture in plan view), the world hour and **how far behind live the shift clock was** (8/03). It deliberately does NOT carry the selection: an id from one board means nothing on another, and once the feed is real a call id belongs to the CAD. **The embed policy** is a table now: `?embed=1` shows the map and its own controls, never the queue/roster/timeline/status bar (the host has its own board), never writes the address bar (not its to write — the same reason `dispatchParams()` reads `window.__opensaDispatch`), keeps the keyboard, the gestures and per-operator storage, and reports out through the handle. **The export composes BOTH canvases**, which is the defect it exists to avoid: a `toDataURL` of the WebGPU canvas captures a city with no units on it, because every unit, call, callsign and trail is on the second canvas — an export that looked right would have been a screenshot of a video game sent into a report as evidence of a shift. It resolves on the NEXT frame, since the two layers are only in step at the end of one, and stamps place, coordinates, eye height, projection, time and pak build under the picture. Plan mode exports its single canvas and stamps `plan mode`. **A defect caught while wiring the moment in**: the shift clock runs on `performance.now()`, not `Date.now()`, and mixing the two puts a scrub decades off with nothing to show but an empty board. Desk evidence: 619 tests green, 11 new (`view-link.test.ts`). Owed by a field run: the phone verdict on the clipboard and the download, which behave differently on Android's browsers. **This closes chain 7 except 7/04 (the minimap, which stands on 6/02's baked tiles) and 7/05 (measuring and drawing on classification, which needs a GPU to judge).** Touched from [the protected list](1-the-map-profile/protected-list.md): nothing |
| [1/05 The streaming profile](1-the-map-profile/readme.md) | **THE DESK HALF IS DONE 2026-08-22; the seconds and the megabytes are owed by a device run.** The step had one answer, not two: *what* to keep and *at which level* are the same question asked of the same projection. **The prerequisite it was blocked on was already paid for — by the bake, not by a new measurement.** `opensa-lod-generator` culls what covers fewer than `minLodPixels` at `hdDrawDistance`; read backwards that IS a geometric error (**2 px at 300 u on the bake's 1080p view = 0.64 world units**), and the pack now writes it per entry beside the budget it was judged against. Fed into the screen-error rule (3D Tiles / CesiumJS) it reproduces the bake's own 300 u ring exactly at the view it was written for, so **the HD ring stops being a constant the engine is told and becomes a consequence** — ~660 u on a phone's 1600 px buffer, ~450 on a 1080p desk, and never past district zoom in the plan view. The view half needs a HEIGHT (`aabbY`) because a frustum cannot be answered by a distance on the ground, so both are pak capabilities and **a pak that states neither keeps the rings** — every pak built before today. [The census](../../benchmarks/opensa-engine/2026-08-22-dispatch-residency-census.json), a Node run of the shipped driver over a synthetic grid: **block zoom 276 → 26 cells tilted / 16 plan, district 84 / 60, city 202 / 176**, and the HD column goes from 12 at every zoom to **0 above block zoom**. Two things landed with it: the fetch queue and the create budget are ordered nearest-focus-first (no new pak needed — the driver used to spend both on whatever the manifest listed first), and a new restriction, [view-gated residency is only valid where nothing behind the camera is simulated](../../restrictions/streaming-residency.md) — the game shell keeps its rings, and its next dynamic body is why. **Owed:** seconds to a working picture and resident MB on the pinned district ([2/03](2-real-device-truth/readme.md)); and the per-CELL error, which only the generator can measure |
| [7/08 The workspace](7-the-operator-map/readme.md) | **DONE 2026-08-26 — the map is the desk now, and the change came out of measurement rather than taste.** Four working consoles were captured and measured on 2026-08-26 (the first attempt could only READ them: headless Chromium answered `ERR_CONNECTION_RESET` on every site, which turned out to be its TLS 1.3 post-quantum key share against the session's egress gateway — `--ssl-version-max=tls1.2` loads all of them). **Not one makes the map its main screen**: SnailyCAD puts it on a separate page, SonoranCAD in a separate window, Resgrid in a card measuring **475x302 of 1665x947 = 9.1 % of the screen**, CrowdCAD's Lite Mode has none. The desk layout here was the same category mistake — a 300-px and a 264-px column either side of the map, **564 of 1280 px, 44 %, on two lists read in glances**. `styles.app` is one column now and the queue and roster are **windows over the world**, moved and sized by pointer or keyboard and remembered under `STORAGE_KEYS.windows`. The geometry is pure and tested (`ui/window-frame.ts`, 13 cases): a window is always FULLY inside the map, and where the map is smaller than `MIN_WINDOW` the map wins. **No library** — `react-rnd` would be this package's first runtime dependency in something that ships as an embeddable widget with none, and GridStack is a grid whose tiles reflow, which takes space from the map in exactly the way this step exists to stop; Pointer Events cover mouse, pen and touch in one handler. Two mechanics taken from SonoranCAD — the **status tally in the panel header** and the **callsign as a filled pill** — and one rejected on evidence: its light theme fills the whole ROW with the status colour and the text contrast fails where the row matters most. Measured after: at 1280x800 the map went from 716 to **1280 px wide (+79 %)**; at 360 CSS px the phone sheet is untouched and `document.scrollWidth` is 360. **Owed:** the phone verdict on the drag handle and the 44-px grip, which a desk cannot give. |
| [7/09 Skins](7-the-operator-map/readme.md) | **DONE 2026-08-26 — four skins, and the guard found a defect in the one that was already shipping.** A theme is DATA (ramp, accent, semantics, shadows, font, one density lever) emitted as CSS custom properties under `[data-theme]`, so switching is **one attribute on the app root — no re-render, no new style objects**; that is why colour left the TypeScript table. `theme.test.ts` runs every preset through **APCA** and fails the build under **Lc 90 primary / Lc 60 secondary** across all five surfaces plus the accent and danger pairs. **It immediately caught Night's secondary text at Lc 47–50 against the Lc 60 DESIGN.md had claimed since 2026-08-25** — a target written down and never measured; corrected to `#a8bbd0` (Lc 61 on the worst surface). Three danger readouts corrected the same way (Night 38 → 61, Contrast 49 → 64, Amber 50 → 65) and Day's step 5 lightened so a selected row clears Lc 90. A second guard reads the COMPONENTS for raw hex — a colour written into JSX has opted out of the theme and stays dark-blue-on-white under Day; two were found (`#0e3a52`, `#7d8ea1`). Ships **Night** (default), **Day** (a phone outdoors, built in its own direction rather than inverted, because inverting breaks Carbon's layering rule), **Contrast** and **Amber** (the identity slot: warm near-black, monospace chrome, rows two pixels tighter, the lever SonoranCAD's `mike19` pulls). What a theme may NOT touch is stated with reasons: `SET_COLORS` (the engine's own draw colours), `TOUCH_TARGET`, and the layout — SonoranCAD moves its dock between skins, which breaks muscle memory. **Not shipped and filed:** a colour-vision-safe status palette; it is the engine's table, not a variable, so it needs a hook in 201/5. **Owed:** the phone verdict on the switcher, and Day read in real daylight. |
| [7/10 Skins from the field](7-the-operator-map/readme.md) | **DONE 2026-08-30 — the contract grew two fields, the Mark43 preset shipped, and building the host validator found a hole in the guard itself.** The evaluation half (Mark43, Tickets CAD v3 and v4, SnailyCAD, CrowdCAD, free-the-cad, run and measured 2026-08-28/29) stands as written in the step; what follows is what the BUILD changed about it. **`shape: { edge, radius }` and `density`**, both as data emitted into custom properties, both guarded — and **no `--os-edge-width`**, though the plan asked for one: it would read 1 px in every preset we ship (our floats carry a hairline BESIDE their shadow), and a constant that never varies is a token that lies about being a lever. **The density guard the plan specified could never have failed** — *a row that clears the criterion at a coarse pointer* is true by construction, because `styles.rowTouch` carries `minHeight: TOUCH_TARGET` and its own padding and `--os-row-padding` never reaches it; what actually travels from a desk to a phone is **10-px TYPE inside a 44-px row**, and that is what is asserted now, with the clamp emitted as a `@media (pointer: coarse)` block so it survives a skin change React never sees. **The hole**: APCA moved to `src/ui/apca.ts` so the runtime validator and the test share one formula, and that exposed a defect open since 7-09 — `luminance` yields `NaN` for a colour it cannot read, and `NaN < 60` is **false**, so a malformed palette SKIPPED the threshold instead of failing it. An unmeasurable pair is a named failure now. **Mark43**: ground, panel and separator are the source's own sampled values; the steps between are fitted (a luminance sort of their swatches is not monotone — their grid rows measure below their ground) and the text is fitted, not sampled, which was not a formality — **Night's own danger pink measures Lc 58 on this preset's lighter surface**. All pairs clear with margin (text 92–96/90, muted 63–66, accent and danger 63–73/60). **The three switchability gaps are closed** against one stated ordering — a link's or host's `theme=`, then the stored choice, then `prefers-contrast` / `prefers-color-scheme`, then Night — with the link's theme never persisted and an unknown id falling through to the operator's own skin. **The owed bundle number is taken** ([the row](../../benchmarks/opensa-engine/2026-08-30-dispatch-bundle-theme-contract.json)): **one preset = 689 B raw / 197 B gzip**, over three builds so the palette is separated from the contract; the whole step is +4.5 kB / +1.56 kB, so **87 % of it is the contract and the validator rather than the skin**. Verified: tsc clean, eslint 0 errors (18 warnings, the unchanged baseline), 406 dispatch tests. **OWED, and it needs the same field run chains 1 and 2 wait on**: the phone verdict on Mark43 in daylight (a lighter ground than Night, which cuts both ways), and that a skin change is still one attribute write on a device — no reflow of the board, no dropped frame on the map. **CORRECTED 2026-08-30, after the merge**: the shape guard only asserted that `RADIUS`/`TEXT` hold variables, so it could not see the token TABLE writing its own corner — `keyHelpRow` kept 4 px and both `mapToolsHit` variants 3 px, off the scale and out of the theme, which under Mark43's `radius: 0` left three controls rounded while every neighbour went square. A second guard now refuses any `borderRadius` in `styles.ts` that is neither a `--os-radius-*` variable nor the one deliberate exception, `50%` — the compass, the minimap and the tally dot are MARKS whose roundness carries an argument rather than a preference. The three are `RADIUS.control` now; 53 dispatch UI tests, tsc clean. |
| [5/04 Units get real models](5-symbology-and-picking-as-product/readme.md) | **THE DESK HALF IS DONE 2026-08-26, and the decision [1/03](1-the-map-profile/readme.md) was blocked on came out of it: units are KINEMATIC.** A unit's position is a claim the feed makes, not a simulation this surface runs — so a car is a root matrix written from the fix (`gtaRootMatrix`, in the one file this app converts coordinates in) and **nothing here reads collision**, which is what lets the baked collision leave the map profile's pak. **Confirmed with the user the same day and promoted to a [restriction](../../restrictions/architecture.md):** the fix arrives FINISHED — the game that published it applied collision, which is why `pos_z` is a road height — and here a unit is a model drawn ON the map rather than an object in a world, so the map corrects nothing (no ground snap, no height grid); the violation presents as an improvement while moving every unit off the place the server reported. Exactness is therefore TESTED rather than asserted: our placement equals the game's own `[x, z, −y]` with the height verbatim, and SA's z-angle reaches the map through `headingFromZAngle` — raw it mirrors every facing about north–south, plausibly. **The angles were audited, not assumed:** the yaw matrix equals the handle's QUATERNION path at all 360 z-angles (max 4e-16) and the basis change has determinant +1, so nothing is mirrored; roll and pitch are absent from the packet, so a car never leans (~0.6 m of nose buried on a 15° slope, [recorded](../../edge-cases/dispatch-console.md), and deriving one is barred as inventing an orientation); and the model's forward is pinned to the 2D chevron's `aheadOf` at 16 headings, since one sign between those two files draws a car and its own symbol facing different ways. What a unit DRIVES is board state, not feed state: PCAD publishes a position, so `Unit.model` is a NAME the board carries. **No format changed and the pak did not grow**: *nothing about a vehicle lives in the pak* is a restriction with a purpose (it is what lets `--rebake` add a car on its mod's id), so the console resolves `<model>.osm` BY NAME out of the built game's own archives — `vehicles.img` → `vehicles2.img` → `peds.img` → `gta3.img`, over Range requests, directory first and one entry when a unit asks for it; `gta3.img` is last because its ~477 kB of directory must not be paid by a board that only draws cars. The reader MOVED rather than being imported: `readModelOsm` is `@opensa/loaders/model-osm` now, beside `openLazyVer2`, because this surface reaches the game layer through the environment driver alone — the same fork 5/03 took, taken the same way, with `packages/game` re-exporting so its fourteen hosts are untouched. **The fallback is the step's real content**: no game dir, no `--vehicles` in the build, a conversion with no car called `copcarls`, or a board that does not know what a unit is in — each leaves the unit as 5/02 drew it, says so ONCE per name in the log, and is COUNTED (`unitsAsModels` / `unitsAsSymbolOnly` / `unitsUnresolvedModels` / `modelTypes` / `modelTextureMb` in `?inventory=1`, `cars 7/9 · 3 types · 12.5 MB` in the readout). A hole where a unit should be is the one outcome not allowed — it reads as a unit going off duty. Measured on the desk: **dispatch bundle 131 650 → 135 848 B raw (+4.1 kB)** plus a 1 153 B chunk, no cleo and no game-build dragged in; one upload per TYPE (150 cars of six kinds = six models), idle types trimmed to 64 MB — a quarter of the phone's smaller ceiling, derived here rather than copied from the game's 256 MB — and a type on the board never trimmed. **Owed by a device:** draws and frame time at 150 with models, and the resident MB. Deliberately not done first: the `_vlo` band ([priced](../../performance/deferred-optimizations/unit-model-lod-band.md)) and peds on foot. |
| [2/02 Serve it from the shareable build](2-real-device-truth/readme.md) | **DONE 2026-08-27, and the artifact is reproducible now — which was half the defect.** There was no single-file BUILD in this repo: the shareable console had been made by hand, so the thing that broke was also the thing nobody could rebuild. `npm run build:share:dispatch` emits **one `dist-share/dispatch.html`, 655 kB raw / 217 kB gzip**, and it streams a real `?src=`. **The seam is the fix rather than the inlining**: `setupStreaming` takes a `StreamingHost.createWorker` the host may supply, so every ordinary build is unchanged (dispatch chunk 135.85 → 136.06 kB, +215 B; the worker stays a 31.3 kB chunk beside it) and only the share entry carries the worker inside itself. **Two guards, because the failure was silent in two ways**: the build refuses an artifact whose MARKUP points outside itself, and — with the beside-the-entry worker module aliased out of this build — one whose CODE names a chunk it does not carry. The second is the load-bearing one: a worker is not loaded by a tag, so an artifact can look self-contained and still 404 mid-boot, which is exactly what read as a hang for months. Verified BOTH ways — with `?worker&inline` it emits one file, without it the build fails by name. **Not verified here and said so**: headless Chromium in this container exposes no `navigator.gpu` (three flag sets probed), so the artifact could not be opened against a real pak; that half rides with [2/03](2-real-device-truth/readme.md) on the device that has a GPU and a pak. |
| [6/03 Switching](6-display-modes/readme.md) | **DONE 2026-08-27 — and the step's real find was that nothing had ever been torn down.** The operator switches with **`m`** or the `2D`/`3D` key in the nav cluster; `?mode=` follows the choice (`replaceState`, so Back keeps meaning the page you came from) and an embedded map writes no address bar it does not own. The rule is `world/mode-switch.ts`, DOM-free so the thing this step owes a test for is testable: **read the pose, dispose, boot, apply the pose**, in that order, with each move pinned — the pose read BEFORE the dispose (a pose off a disposed camera is nothing), the dispose BEFORE the next boot (two surfaces = two GPU devices on a device budgeted for one), a second request while one is in flight IGNORED (a 44-px target and a double tap must not boot two engines), and the mode that came UP reported rather than the one asked for — so no WebGPU means the flat map **and the banner saying why**, while a mode the operator CHOSE raises none. **The selection and the moment needed no carrying**: they are `useOperations`' state read through getters the switch never sees, and a test pins that the boot is handed nothing but a mode and a pose. **What it uncovered**: both modes were written when a surface was the end of the page, so the 3D host leaked its WebGPU device (every buffer, texture and bundle the world uploaded — `GPUDevice.destroy()` is the whole teardown) and its pak worker, while plan mode left the WebGPU canvas `display:none` (the next 3D boot would `init` on a zero-size canvas — black screen, no error), the overlay eating every gesture, and a `ResizeObserver` per switch. None was a bug until this step; all were the moment it landed. **The cost is the device's**, so every switch logs `[mode] live → flat in N ms` — unmeasured here, since this container has no WebGPU at all, and it rides with [2/03](2-real-device-truth/readme.md). Desk cost: the dispatch chunk **136.06 → 138.32 kB** raw (+2.26 kB for the switch, the key, the button and the teardown), the shareable artifact 655.23 → 657.55 kB. 392 tests green, 10 new. **A FIELD DEFECT, 2026-08-30, and it is the one shape a desk run cannot see**: on the phone, a round trip **live → flat → live** comes back with the camera's `pitch` and `height` as **NaN** — the status bar prints `NaN m`, the scale bar reads 5000 m and the 3D surface renders BLACK while the engine is still drawing it (112 draws, 4/4 cells visible, 278 k triangles: the world is there and the camera is nowhere). Three things make it worse than a bad frame. **It does not heal**: an explicit `map_goto` with a height does not restore it, because `flyToPose` re-applies the camera's OWN height (`applyPose({ ...pose, at, height: this.height() })`) — so once the value is NaN every later flight carries it forward, and the only way back is a reload. **It costs battery silently**: the window that followed reported `framesSkipped` **0** over 758 frames with `idle: false`, so 4/01's render gate stops idling entirely — a black console drawing 30 fps forever. **And JSON hides it**: `JSON.stringify` writes NaN as `null`, which is why the flat-mode readout says `"height": null` and reads like a mode that simply has no eye height. **Narrowed, not yet fixed**: `MapCamera` is EXONERATED — the whole sequence (perspective → ortho → `pose()` → a fresh camera's `applyPose` → a `flyToPose` → 200 advances) was replayed against the real class and every pose came back finite, so the NaN enters in what the boot does around the camera rather than in the camera itself. Reproduced on app `311f2d1`, pak rect 5,-7,6,-6. |
| [6/01 The baked 3D city map](6-display-modes/readme.md) | **THE BUILD TARGET LANDED 2026-08-27; the LOOK is deliberately not built.** `opensa-pack --lod-only` welds the LOD level and skips the HD one — the whole of *a world whose only tier is the far one*, since the cell LODs are already a simplified city. `LODONLY=1` carries it through `phone.sh` and the panel converts it in one tap into its own `-map3d` folder. Three things it had to get right: **the recipe records it** (a pak with one tier and one with two are different builds, invisible in a folder listing, and `phone.sh` reuses by comparing recipes — an unrecorded flag is a run served the wrong world), **the folder is its own** (two recipes in one folder is a run that serves nothing or a rebuild that throws the other pak away), and **the runtime needed nothing** — a level is chosen from what the CELL HAS, so a pak with no `hd` key resolves to `lod` at every distance, which is now pinned by three cases including the control that a both-tier pak still prefers HD at the same focus. The occluder ring stays HD on purpose: it is an input to the AO bake, not an entry in the pak. **The bake-time recolour is NOT written**, because a look change is judged by looking and looking needs the device with the game files — writing one blind is the assumption [directive 4](../../project-goals.md#4-better-must-be-demonstrated-not-assumed) forbids. **Owed by a build:** bytes and resident MB against the live render on the pinned district, and frame time at the 150-unit board. |
| [7/04 The minimap](7-the-operator-map/readme.md) | **DONE 2026-08-22, and it is ROUND on the user's call** — a shape that carries an argument rather than a preference: a radar answers a distance question, and a circle is the only frame in which a pixel of travel means the same thing in every direction. **The step's own cheapest version was not available and a better one was.** It asked for [6/02](6-display-modes/readme.md)'s flat tiles at lowest zoom — a bake that does not exist — so the dial draws the world's OWN description of itself instead: the baked district boxes (5/03's table, now reachable as `DistrictLookup.boxes`), the board, and a new `MapCamera.groundFootprint()` that is a **polygon, not a rectangle**, because a perspective frame's footprint is a trapezoid and a rectangle would claim the view reaches ground it does not (the plan view makes it a rectangle by itself; a corner that never meets the ground is pulled back to the world's reach rather than dropped). North-up, so the world stays put and the footprint turns inside it; a tap flies there keeping the current zoom. **The two owed numbers are paid** ([the census](../../benchmarks/opensa-engine/2026-08-22-dispatch-overlay-census.json)): **914 canvas calls a repaint** at 150 units + 40 calls + a 160-box city, and **0 for a frame where nothing moved** — the dirty check is the whole answer to chain 4, and a still map costs 20 repaints a second instead of 60. The static outline is 160 of those calls; caching it is [priced and not taken](../../performance/deferred-optimizations/radar-outline-cache.md). Milliseconds are [2/03](2-real-device-truth/readme.md)'s |
| [7/05 Measuring and drawing](7-the-operator-map/readme.md) | **DONE 2026-08-22 — and the classification pass the step named was NOT taken, on an argument rather than on cost.** Cesium's ground primitives are right for a shape that must look like paint on the road, and wrong for the shape this step is for: **an operator's cordon has to be visible THROUGH the buildings it is drawn around**, and a draped one disappears behind the first block it crosses. So a sketch is symbology — world points projected with the frame's own view-projection, like the unit chips, drawn last so nothing hides it. **The ground-following rule is therefore stated rather than solved**: a sketch does not drape and nothing occludes it; a distance is the straight ground line, never the drive (the path graph is `original`-only, so a routed number would be a lie on every total conversion); there is no ETA circle because it needs a travel speed nobody has measured — all of it in [edge-cases/dispatch-console](../../edge-cases/dispatch-console.md). Three tools (ruler, circle, area with its shoelace area), a store owned by the MAP rather than by React or the board, an armed tool taking the tap whole so a cordon can be placed over a unit, and **it works in plan mode too** — the no-GPU fallback IS a 2D map. Owed and paid: **422 canvas calls for ten shapes of eight points, 42 a shape** |
| [4/03 The wait before the first frame](4-a-console-is-not-a-game/readme.md) | **ALL THREE BUILT 2026-08-26, AND THE SPLIT ANSWERED THE 77.9 ms ON THE FIRST CAPTURE.** 4/01 made the first frame cheap and left the seconds in front of it untouched — on the phone, a black rectangle that a slow pak and a crash both look like. The shell is inline in `dispatch.html` and painted before the module graph, because anything it imported would put the 944 kB engine chunk in front of the first pixel; its ramp colours are literals for that reason, with a comment saying so. **The rule worth carrying: it leaves when there is a PICTURE, not when `bootDispatch` returns.** Returning only means the loop started, and the world arrives cell by cell after it — `reportBoot` releases on `cellsVisible > 0`, or `cellsTotal === 0` (nothing to stream), or frame 40. The bar sweeps while nothing has a denominator and becomes a real fraction the moment the cell count is known; bytes ride along as a note, because nothing knows in advance how much of the pak the opening view pulls. Both failure shapes are covered: a throw calls `bootFail` and leaves the reason on screen (the WebGPU fallback reports its switch to plan mode rather than vanishing), and a boot that neither finishes nor throws is caught by the shell's own 30 s watchdog — which then REMOVES itself, since a fixed overlay left in the tree keeps eating taps over the whole map. Verified in a harness at 360 px across five states (indeterminate, a fraction, failed, removed, removed twice); **still owed: the real device.**  **The cache** answers the second open: the reads are `Range:` requests over an immutable file, so they cache perfectly, and the only thing that makes a slice stale is a rebuild — which the manifest already stamps. Keyed by `buildTime`, other builds of the same pak dropped on open, and three shapes of "no cache" that all read from the network exactly as before (no Cache Storage, no `buildTime`, a refused write). Two API facts cost the first attempt and are now written down: `cache.put` REFUSES a 206, and `cache.match` IGNORES the `Range:` header — so the range lives in the key. It is counted rather than assumed (`pakTraffic.cachedBytes`, a subset of the total) and shown on the boot shell, because on a phone a capture is the only devtools there is. **Still owed: a repeat-open capture on the device** — against [38.6 MB for four cells](../../benchmarks/index.md). **The pipelines** are compiled asynchronously and overlapped now — 34 of them started, then awaited once, instead of blocking the boot thread one after another; a shader that no longer validates rejects at that await rather than at the first draw that binds it. **And the recommendation that opened this step was half wrong, which the code says plainly: `compileAll` runs inside `engine.init`, NOT inside a frame**, so this shortens the `starting the GPU…` phase and does not by itself touch the 77.9 ms. That number is now SPLIT instead of theorised — the first three frames time `frame:targets` / `sky-lut` / `probe` / `cull` / `record` / `submit`, and no frame after them pays for it; three frames rather than one because the two behind the first are what separate a one-shot allocation from a steady cost. It is the engine's own recorder, deliberately not `frameSpans` (the game shell SUBTRACTS that total to print `unattributed`, so a span from inside a frame would drive it negative). The capture carries `boot.gpuMs` and `firstFrames`. This is the 08-25 lesson applied rather than restated, and it paid the same day: [the device capture](../../benchmarks/opensa-engine/2026-08-26-mobile-boot-split.json) put **`frame:sky-lut` at 75.8 of the 77.9 ms**, 0 on the second frame and 0.1 on the third — one-shot CPU, not per-frame. **And the cause was not the atmosphere maths**: `f32ToF16` allocated a `Float32Array(1)` AND a `Uint32Array` view per call, 18 432 calls per build. Module-level scratch + a precomputed constant alpha: **13.29 → 4.05 ms per build in node, 3.3x, output bit-identical over 55 296 values**. It is not only a boot cost — the LUT rebuilds on every hour and weather move, so that was a ~76 ms main-thread hitch on each one, on the axis chain 8 scrubs. **The capture's other number has no owner yet: `engine.init` at 2 607.5 ms**, larger than the rest of the boot combined and with no before/after partner (the counter shipped with the async compile), so nothing is claimed for that change; `init` is split by phase now — `boot.phases` — for the same reason the frame was. Field note from the operator, consistent with the row: nothing on screen for ~50 frames, then the buildings at once. The user picked all three items 2026-08-26, progress only — no flat map, no skeleton. **A FOURTH piece came out of that 2 607.5 ms the same day, and it needed no device to justify: the boot was using its two machines one at a time.** The GPU ran with the radio idle, and only when `engine.init` returned did the world start looking for its manifest, then its worker, then `data/timecyc.dat`, then the water, then the districts — each round trip behind the last. The pak's engine-free half is `openPakSource` now (the manifest, and a worker already probed onto its IO mode and slice cache); `bootDispatch` STARTS it, with the timecyc read beside it, before it awaits the GPU, and `setupStreaming` takes the result as its `opened` argument and does the engine half only. The water mesh (2.66 MB) and the district table are fetched together rather than one behind the other. **The boot pays `max` instead of `sum`** — a scheduling change, no new machinery, and the seam is held by a test that could not pass if either half were done twice (`Worker` does not exist in node, and `fetch` throws in that test). It is COUNTED rather than claimed: `boot.openMs` beside `boot.gpuMs`, and **`boot.overlapMs`** — how much of the two really ran at once, derived from the wall clock across both. What it costs is stated rather than hidden: a world that FAILS now reports after the GPU is up instead of before it, and the shell narrates the pole (the GPU) rather than the race. It costs **0.41 kB of the dispatch chunk** (121.47 → 121.88 raw, 41.51 → 41.70 gzipped) and nothing of the engine chunk. **THREE OF THE FOUR OWED NUMBERS CAME BACK THE SAME DAY** ([the second-open capture](../../benchmarks/opensa-engine/2026-08-26-mobile-boot-warm-second-open.json)): the sky-LUT fix measured **75.8 → 15.4 ms of the first frame on the device, 4.9x** — more than the 3.3x node predicted, whole first frame 85.1 → 23.7; the repeat open is answered — **23.60 MB of 26.26 (89.9 %) over 40 of 41 requests** against 33 % / 59-of-88 when the open reached past what filled it, blob mean halving with it — and **the single miss is `water.bin` to the byte**, a loose file beside the pak rather than a pak slice, so a miss by construction rather than a cache failure — and `installWater` now reads Resource Timing's `transferSize` for it, counting a hit only at 0, so the next capture says whether those 2.66 MB cross the wire instead of leaving it arguable. **And `boot.gpuMs` reads 398.4 against 2 607.5, which is claimed for NOTHING**: the only code difference is the sky-LUT fix, worth at most the 17.6 ms `init:sky-lut` now reports, so ~2.2 s changed hands with no owner. The split says where the boot lives — `init:pipelines` **226.8**, `init:device` **117.4**, resources 28.9, targets 4.9, canvas 2.0 (397.6 of 398.4 attributed) — and the standing hypothesis for the gap is the browser's persisted pipeline cache, WARM on this open and COLD on that one, which would mean the boot this step is about (the first one after an app rebuild) is still a 2.6 s run and is now unmeasured. **The test is one capture with the site's data cleared against one straight after it.** **The overlap's own number is still owed after THREE captures, and the cause was upstream of the app both times it repeated**: the device kept running the pre-overlap build, and the third `git pull` printed `no such ref was fetched` — its branch tracked a branch name the remote no longer has — did not merge, and **exited 0**, so the `&&` chain behind it re-extracted the archive already in the tree. The warm boot reproduced meanwhile ([the repeat](../../benchmarks/opensa-engine/2026-08-26-mobile-warm-boot-repeat.json)): `gpuMs` **347.2** with `init:pipelines` **218.3**, so ~220 ms is the series and 2 607.5 is the outlier; the second open reproduced at 89.3 % over 38 of 39, `water.bin` missing again at the same 2 658 756 bytes. **A capture now names the app it ran** (`app` — the commit vite stamps in), and the trap it closes is written up in [restrictions/architecture](../../restrictions/architecture.md): three captures measured a build nobody was testing, the numbers were all real, and nothing anywhere errored. **THE FOURTH ATTEMPT PAID IT** ([the capture](../../benchmarks/opensa-engine/2026-08-26-mobile-boot-overlap.json), the first in this repo that names its own app): **`openMs` 230.5, `overlapMs` 227.7 — 98.8 % of the world's open ran under the GPU, so the pair cost 690.8 ms of wall against 918.5 serial.** 227.7 ms off the boot for 0.41 kB and no machinery. It also put a RANGE under `boot.gpuMs` — 688 here against 398.4 and 347.2, `init:pipelines` spreading 218–358 and `init:device` 81–265 over three warm runs — so a single boot number means little, and 2 607.5 is still an outlier nobody has explained. **Two more reads then moved in behind it**: `water.bin` (2.66 MB, the largest single read left on the serial path, and the one request a repeat open's cache never answers) and the district table are loose files the manifest points at, so they ride a second wave inside the same wait — `streamedWorld` now reaches the network for nothing. And the stamp's own first reading was a bug worth the same rule: `67432d1+` off a tree that had been clean one command earlier, because `appBuild` ran `git status` from inside the build, after `tsc -b`. **A `+` that fires on every build is a `+` that means nothing** — it reads tracked files only now, minus the incremental cache. [The contract](../../features/dispatch-console.md) **THE 2 607.5 ms OUTLIER HAS AN OWNER, 2026-08-30** ([the pair](../../benchmarks/opensa-engine/2026-08-30-mobile-boot-cold-vs-warm.json)). This step left the persisted pipeline cache as a standing hypothesis with no way to test it; the panel's `webapp` job produced the test by accident — it re-unpacks the served app, which gives every asset a new identity, so the FIRST open after it cannot be answered by that cache. It cost **`gpuMs` 5339.7 with `init:pipelines` 4681.1**. Two opens of the same app minutes later, nothing else changed: **689.2 (429.1)** and **404.3 (318.3)**. `init:pipelines` **4681.1 → 429.1 is 10.9x** and accounts for 4252 of the 4650 ms between them, so the hypothesis holds and 2 607.5 was a cold-pipeline boot. **What that means for this step is not comfortable**: the boot an operator takes the first time they open a NEW build is the cold one, the 227.7 ms overlap this step bought was measured against a ~690 ms warm boot, and the cold path is still unoptimised. The overlap itself reproduced on both — **99.5 % cold (840.5/844.3) and 99.4 % warm (619.8/623.7)** — as did the sky-LUT fix (`frame:sky-lut` 20.7-23.3 ms on the first frame, ~0.1 on the second) and the repeat-open cache, whose single miss is `water.bin` to the byte again. |
| [4/01 Render on demand](4-a-console-is-not-a-game/readme.md) — the first frame | **DONE 2026-08-25, and the fix was found by splitting a span rather than by reasoning about it.** The console's first drawn frame was ~2 s on the phone, all of it inside one span called `overlay-2d`. The first attempt guessed FONT RESOLUTION and warmed the metrics; the capture after it shipped measured no improvement, which is the whole reason the next step was an instrument instead of a second guess. Splitting the first three draws into `overlay:clear` / `overlay:symbols` / `overlay:sketches` named it in one capture: **`overlay:clear` 212.1 ms of a 333.1 ms frame — 64 %** — the first touch of the 2D canvas's GPU-backed store, against 22.6 for the first glyph raster. Warming the surface at boot (where it overlaps the wait for the pak) takes it to **0.1 ms**, the worst frame to **123.0** and `dtMax` to **140.1** ([before](../../benchmarks/opensa-engine/2026-08-25-mobile-first-draw-split.json), [after](../../benchmarks/opensa-engine/2026-08-25-mobile-first-draw-warmed.json)). A guard came with it: `resize()` assigned `canvas.width` unconditionally, which resets the backing store even for an identical value, so the `ResizeObserver`'s first tick threw away what boot had just warmed — and cleared both canvases on every tick for the rest of the session. **Two things left open and said so:** the `fillText` glyph warm bought 2.3 ms of 22.6, so `overlay:symbols` is not explained; and `engine-frame` measured 77.9 ms on the first frame in BOTH captures, to the tenth — a fixed engine cost, now the largest item on that frame. The split stays in the build as the regression detector |
| [3/01 360 CSS px](3-the-operator-surface-on-a-phone/readme.md) | **DONE 2026-08-25, and the chain's premise was right: "it loads" was not "an operator can work it".** The user's verdict was *"impossible to control on the phone"*, and rendering the console at 360x800 with a coarse pointer found the reason — **the whole layout was 403 CSS px wide inside a 360-px screen.** A bare `1fr` grid track keeps `min-width: auto`, so the top bar's 403-px content widened the single column and the map with it, which put **the entire right-hand column of the turn/tilt/zoom cluster off the edge** (`⟳`, `▼`, `BLK`, `−` — all unreachable, nothing to scroll to), the `Auto` switch off the edge, and `ASSIGNED` cut off every call row. Nothing was clipped visibly, nothing warned, and the desk it was written on never showed it. Fixed at the root (`minmax(0, 1fr)` on every flexible track, the three full-width bars made to give way). **Nine controls were also under the criterion in the axis nobody checks**: the compass 42x42, `Fit` 40 wide, `×1`/`×2`/`×8` 33x22, the sheet tabs 32 tall, the search box 22, both sliders **16 px tall**, and `Auto-dispatch` a native checkbox at **13x13**. All at ≥ 44 in BOTH axes now; the switch is a button because no inline style reaches a checkbox. **And the map got its screen back**: the tool cluster was ~380x390 over a ~350-px-tall map and now opens from one `TOOLS` handle, the nav cluster folds turn/tilt/levels behind one key, and the sheet is capped rather than given a fixed 44% (which had left ~200 px of black under two calls). Map height at 360x800: **350 → 500 px**. **Landscape was worse and width could not see it** — at 740x360 the map was **98 px**, so `useShortViewport()` is a third question beside width and pointer, and the sheet opens collapsed there (map **98 → 210 px**). Measured in a headless Chromium at DPR 2 with `(pointer: coarse)`, cross-checked against the user's own screenshot; the phone still owes the feel. Guard: `apps/dispatch/src/ui/styles.test.ts`, which is the lint the restriction said nothing prevented writing "when this bites a second time" |
| [3/05 The chrome tells the truth](3-the-operator-surface-on-a-phone/readme.md) | **BUILT 2026-08-30 on the operator's report, out of turn** — one defect in three places: a surface stating things it is no longer measuring, on the one console where the person holding the phone IS the instrument. **The frame rate was counted over the LOOP rather than over the frames** — `1000 / mean(dt)` over sixty passes whether they drew or not, so a console at rest for six seconds reported **10 fps** and then climbed back over the next sixty frames as the idle samples aged out (every number real, none describing the frame on screen), and the interval that spans a rest was reported as a frame time. It is a COUNT of frames drawn in the last second now, beside the median interval between two CONSECUTIVE drawn frames, with `—` rather than an invented `0.0` when the window holds no pair, `cpu N ms` on the desk bar and `idle · 16.7 ms last` at rest (`world/frame-clock.ts`, 6 tests; the plan-mode loop had the same defect and takes the same clock). **The panel link's status was a claim made once and never withdrawn**: `held` was only ever entered on a poll that SUCCEEDED and a failed poll reported nothing, so a panel that died left *keep this tab in front* on screen in front of somebody doing exactly that — there is an `offline` state now, every reading is stamped with when the panel last answered, and the band counts that age up. **And a command now says what it DOES** — one line per instruction settling into `done`/`failed`, because an agent flying the camera, switching the whole surface and taking a picture were the same sentence, and a view that moves with no hand on it reads as a defect (*"the map jumped"*). **The metrics panel folds**, opening folded where the pointer is coarse: 14 rows of monospace over 360 CSS px was most of the map, and folded it still carries the frames, the frame time and a `⚠ n` over the warnings, so the fold cannot swallow one. **VERIFIED ON THE DEVICE 2026-08-31**, through the panel's MCP channel with nobody relaying a number ([the capture](../../benchmarks/opensa-engine/2026-08-31-mobile-honest-frame-counter-150u.json), 150 units on `los-santos-centre`, app `016c1e7+` — the `+` is the proof it ran this chrome rather than `main`'s archive). **Three readings of one run, and the spread is the finding: the collector's own field says 10 fps / p50 100.6 ms, the new on-screen counter says 17 fps · 60.9 ms · cpu 10.3 ms mid-flight, and 21 fps (p50 48 ms) is what the 129 moving frames derive to.** The old readout used the collector's formula, so it would have shown 9–10, and the still-map heartbeat read 8–9 — **the fix is worth ~7 fps of truth: the console was under-reporting itself by nearly half while it worked.** At rest the bar reads `idle · last cpu 8.5 ms` (no consecutive pair in the second, so it names the body), and `cpuMs` reads a real drawn body rather than a wake's ~0.1, so the `drawnBodyMs` split holds on the device. **The step's second owed item is now MEASURED and was understated when this row was first written** — the correction is in the step: the collector never samples a skipped pass, but a skipped pass arms the next entry with `setTimeout(100 ms)`, so the frame after it carries a dt that is 99 % sleep, and on a live 150-unit board that is **706 of 835 samples**. `dtP50` therefore reads the idle poll and `shareOfFrame` (2.3 %) describes a resting loop, which is why every capture since 4/01 landed (08-22) has had its moving half derived by hand in prose — the 08-30 row says so in its own note. `frame-clock.ts` does that in CODE for the screen; pushing it into the collector changes what a filed capture MEANS and owes its own before/after, and the 08-31 row is now that before. **1/01's baseline is clean** — 2026-08-09 predates 4/01, so its p50 30.3 ms and its *"the frame is WAITING, not working"* stand. **AND THE COLLECTOR WAS CLOSED THE SAME DAY, with that row as its before.** The rule has ONE owner now — `FrameClock.drew()` returns the interval's kind and the collector is TOLD, so the status bar and a filed capture cannot disagree about one loop; `frame.*` is the paced intervals alone, a `rest` block carries the other population rather than dropping it, and `outsideMeanMs`/`shareOfFrame` divide by the same population their numerator came from (`bodyMeanMs` deliberately does not — a body is measured on the frame, not on the gap). The panel says the split on screen, verified in a browser at 360 CSS px: **`frame p50 16 · p95 16 ms` beside `rest 82 of 253 frames · 68% of the window`**, and 68 % of 12 s over 82 rests is ~100 ms each, which is the idle wake exactly. **The storage went with it and that is its own finding**: the collector kept every `dt` and copied-and-SORTED the array on every report — one the panel asks for twice a second — so at 216 000 frames (two hours at 30 drawn fps, which is 4/02's own run) **a report cost 58.2 ms, ~12 % of the main thread the instrument was reporting on**, and no capture ever said so because the collector does not measure itself. A bounded histogram makes it **0.002 ms and 1.65 MB → 0** ([measured](../../benchmarks/opensa-engine/2026-08-31-inventory-collector-storage.json)); the price is a percentile that is a bin's floor, which is the precision every row since 08-22 already had. The bin range is two resolutions **because a test caught it** — a single tail at 100 ms would have saturated `dtP95`, and the 08-31 row's was 108.4. 44 tests across the two modules, both regressions proven red→green. **THE AFTER WAS TAKEN THE SAME DAY** ([the row](../../benchmarks/opensa-engine/2026-08-31-mobile-split-collector-after.json)) and settled it on ONE window arithmetically: over all 527 drawn frames the median lands at index 263 and only 46 fall below 100 ms, so the old collector's median was a rest interval and its `fps` reads **10** — over the 47 intervals that are frame times it reads **36** (p50 28 ms, p95 76), and `shareOfFrame` goes 1.5 % → **41 %**. The rest population is named rather than derived for the first time: **480 frames, mean 103.74 ms** (`IDLE_WAKE_MS` plus the loop prologue), **82 % of the 60.7 s window**. It is a STILL-MAP window — the `map_goto` answered but the pose never left the opening one and Android throttled the tab throughout — so it is not a device comparison against the flown before-row, and the row says so. **And it found the next layer, which is why a field run is not a formality**: `cpu.bodyMs` is paired one pass late on purpose, so after a SKIPPED pass it carries the gate's own ~0.2 ms — `bodyMeanMs` read **1.48 ms against a real 13.84**, every segment ~11× low. The whole CPU block is restricted to the paced population now (the streamer, engine timings, spans and world stay over every drawn frame — those are measured on the frame rather than paired to the pass before it). **The archive the phone serves carries all of it now** (`prebuilt/`, stamped `a50a4a6`, 2026-08-31 — refreshed twice that day, the second time for the `copcarla` fix below): the committed one was `6bdffcf`, so a device that pulled and extracted got neither the honest counter nor the split collector — grep the chunk it holds and there is no `after-rest` interval and no `offline` link state in it at all. Both field rows above reached this code only because they were built from a working tree, which is what the `+` on `016c1e7+` and `fd0c8ae+` says; the flown window owed below would have been taken against `6bdffcf` on any run that did not. The whole of 3/05 costs **+6.35 kB raw / +1.79 kB gzip** on the dispatch chunk (148 207 → 154 561 raw, 49 857 → 51 647 gzip) — the number [1/06](1-the-map-profile/readme.md) tracks. **Owed: a FLOWN window**, to give the device numbers a partner |
| [3/03 Legibility at city zoom](3-the-operator-surface-on-a-phone/readme.md) | **DONE 2026-08-22, and the budget it asked for turned out not to be a number.** A viewport holds `floor(area / chipArea)` labels and the collision index cannot place past it — **1371 on a 1920×1080 desk, 152 on a 360×640 phone** — so there is no constant to choose and none to retune when the font changes (the same shape of answer 8/04 reached for trails). **Which label wins** is the operator's job rather than a preference: the selection, then open calls worst-priority first, then units committed to a call, then nearest the eye; colour carries none of it. **The loser keeps its SYMBOL** — an icon is the datum and is never dropped — and only its name goes, with the count reaching the operator (`namesHidden`, *"104 names hidden"* in the status bar), because a crowded map with no count reads as a complete one. The mechanism is [MapLibre's](../../links.md), variable placement included: a chip that cannot go above tries below, then either side. Measured at 150 units + 40 calls ([the census](../../benchmarks/opensa-engine/2026-08-22-dispatch-overlay-census.json)): **179 of 190 placed on a desk, 86 on a phone** — one rule, and the screen decides. Whether 86 names is ENOUGH in an operator's hand is a field verdict; the lever if it is not is collapsing a cluster into a count |
| [4/01 Render on demand](4-a-console-is-not-a-game/readme.md) | **BUILT 2026-08-22; the battery delta is owed by the device.** The budget was named before the build and is two budgets, not one: an operator's input is answered in the NEXT animation frame (the pointer/wheel/key handlers re-arm the fast schedule themselves), and a change nobody touched — a board tick from the feed — within the **100 ms** idle poll, which is two orders of magnitude inside PCAD's 4 s publish rate. **"Nothing changed" is a STATE**, exactly as the step demanded: `RenderGate` compares the picture's whole input set (pose in six degrees of freedom, the board by identity, selection, hour, drawing-buffer size, sketch revision, and the streamer's pending/created/evicted) against the frame that last DREW, so a signal arriving while the loop sleeps is still there when it looks. `wake()` only makes the answer sooner; it is never how it is found. **Stopping the loop entirely is [priced and refused](../../performance/deferred-optimizations/idle-loop-stop.md)** — an event-driven wake is one forgotten `wake()` away from a map that looks frozen. In BOTH modes, plan mode included, because that is the one a weak device gets. The claim is readable rather than asserted: the status bar shows `idle`, and `?inventory=1` carries **`framesSkipped`** beside `frames`. **Owed:** wake latency under a thumb and a battery/thermal delta over a shift-length idle, on the [2/03](2-real-device-truth/readme.md) device **A DEFECT IN THE SCHEDULER, found 2026-08-30 by the overlay A/B and fixed the same day.** `schedule(true)` stored the idle timeout's handle and NOTHING dropped it when the timer fired — so after every idle wake that drew, `idleTimer` still pointed at a timer that had already run, and `wake()` reads that as *an idle wake is pending*: it clears the corpse and arms a `requestAnimationFrame` on top of the one the drawn frame had already armed. **Two entries into the loop for one displayed frame**, the second 0-2 ms behind the first, each paying a full prologue (held keys, camera advance, gate) and a wake-up — which on this device is battery. It is silent in every direction: nothing errors, the picture is correct, and the second entry usually draws nothing because the gate compares values; the ONLY trace is frames faster than a vsync interval, and only in a capture — **192 of them under 8 ms** in the A/B's overlay-off window, which is where it was caught. The fix drops the handle as the timer fires. **VERIFIED IN THE FIELD the same hour** ([the capture](../../benchmarks/opensa-engine/2026-08-30-mobile-idle-timer-fix.json)), because a timer defect no test can see is what this was: the same circuit with the same `?overlay=0` went from **192 of 808 frames under 8 ms (23.8 %) to 6 of 229 (2.6 %)** — a 9x drop, the remainder being the legitimate case of a real touch waking a genuinely idling loop. **The rate is the comparison and not the distribution**: the two windows read different paks (the panel's out field changed between them), so the moving-frame figures beside them are context — mean 30.6 -> 27.5 ms, p50 24 both. The fix is claimed for the wasted loop entries and the wake-ups they cost, and for no frame-time improvement. **AND IT HAD A DEFECT THAT COST A FIELD SESSION, found and fixed 2026-08-31.** The gate compared `pending` as a VALUE where it is a PREDICATE. A cell's texture arrays finish in `drainUploads` — 1.5 ms of budget, inside `world.follow()`, so only on a DRAWN frame — and `textures.has(ref)` stays false until the last write lands: the cell is not created, `pendingCells` does not move, nothing else moves either, `same()` says nothing happened, and **the frame the gate skips is the one that would have finished the upload**. The console rests on a black map holding fetched cells until somebody touches it, and a touch buys a handful of frames rather than the seconds the arrays need — 19 drawn against 851 skipped in 86 s on the phone, `errors` empty. It killed [9/01](9-the-mobile-frame/readme.md)'s `field` arm and read as a streaming bug for a day ([the issue](../../open-issues/fixed/dispatch-map-void-no-cells-created.md)). `FrameSignals.pending`'s own comment already said *"the picture is not finished while this is above zero"*; the gate agrees with it now, at the honest cost that a world which never finishes arriving keeps the loop awake rather than resting on a wrong picture. Field confirmation still owed. **FIELD-CONFIRMED the same night**: the `field` arm on app `f0e7bdd` streams — 4/4 cells, 14 creates, 112 draws, `pendingCells` 0 and no VOID warning, where two attempts hours earlier had zero cells against fetched bytes. [The issue is closed](../../open-issues/fixed/dispatch-map-void-no-cells-created.md). |
| [2/03 The field run](2-real-device-truth/readme.md) | **HALF PAID 2026-08-23 — the row exists and the loop that carries it is closed; the 60 fps question is not answered.** [The capture](../../benchmarks/opensa-engine/2026-08-23-mobile-pinned-district-inventory.json) is the first on the PINNED district from a real phone AND the first that reached the repository without a chat paste: the operator filed it in `tools-debug/phone-console` and the phone committed and pushed it itself. **MGA-LX3 / ARM Bifrost**, 403x334 CSS at DPR 2, ASTC pak (rect 5,-7,6,-6, built 12-08), app from the prebuilt archive refreshed the same day. What it settles: **resident 76.1 MB** — a fifth of the 300–500 MB ceiling, with `target` 37.5 of it (fixed cost, as 1/01 found) and textures only 25.8 on ASTC; **112 draws, 278 k triangles, 17 cell creates and 0 late**; 29.3 MB of transport in 53 requests; `warnings` and `errors` empty. What it does NOT settle, and the row says so on its face: **the window is idle-dominated** — render-on-demand skipped 599 of the frames, so the 100 ms poll is the histogram hump at 94–100 ms and `fps 16` is the rate of DRAWN frames rather than a frame cost. **The finding worth acting on is the worst frame: 600.3 ms, of which `overlay-2d` is 422 and `engine-frame` 163.5** — on a phone the symbology layer, not the engine, is what a bad frame is made of, which is 5/02's and 3/03's business rather than the profile's. `chipsDropped` 4 of 12 symbols at 403 CSS px is 3/03's budget measured in the field. GPU time stays unmeasurable (no `timestamp-query` on this adapter), so the 94 % that had no owner on 08-07 still has none from the GPU side. **And the first frame, isolated 2026-08-25.** A second capture — one frame, taken by pressing copy the momen **THE 60 fps QUESTION IS ANSWERED 2026-08-30, and the answer is a LADDER rather than a number** ([the capture](../../benchmarks/opensa-engine/2026-08-30-mobile-driven-map-150u.json)). The whole round was taken through the panel's MCP channel — the agent opened the console on the phone, flew it, read it and filed it, and no number was relayed by a person. The 08-23 row could not price a frame because render-on-demand left the window idle; this one was DRIVEN, and it is still MIXED, which is the thing to read it with: of 1406 frames, 903 sit in the >=100 ms bin (the gate's idle poll, plus one 18.2 s gap where Android froze the backgrounded tab) and **503 are moving frames**. So the report's own `dtP50` 100.4 / `dtP95` 106.0 are NOT frame costs — they average the poll in — and the frame is read off the histogram instead: **p50 30 ms (33 fps), p90 64, p95 66, p99 86**. The shape is the finding: **189 driven frames (38 %) land on ONE 16.7 ms vsync interval, 152 (30 %) on two, 108 (21 %) on three, 40 (8 %) on four** — the console DOES reach 60 fps on this device and holds it for over a third of the driven frames; what it does not do is HOLD it. **And the largest CPU item is not the engine**: `overlay-2d` 2.37 ms mean against `engine-frame`'s 0.57 (5.53 against 1.67 on a continuously drawing window), which is 5/02's business and became the reason `?overlay=0` exists (201/2, the same day). Also settled here: **streaming kept up with every flight** — 37, 40 and 55 cell creates across three legs, `lateCreates` **0** in all of them, worst create 22.6 ms (7/03's owed verdict, at agent flight speed rather than at a drag's); **`chipsDropped` 93 of 148 symbols at 360 CSS px** (3/03 in the field at the declared count); and the render target is a **fixed ~69 MB per megapixel of surface** over three sizes the browser chose for itself, which is 1/03's "target is fixed cost" measured and 2/04's input. **Not taken, and why**: the served app was `311f2d1`, which PREDATES 7/10 — so no Mark43 daylight verdict rides on this round, and the phone needs a rebuilt `prebuilt/` archive before one can. **A DEFECT came out of it, in 6/03's territory** — see that row. |
the page opened — says what the 08-23 worst frame was: **1654.9 ms, of which `overlay-2d` is 1528.1**, with
ZERO cells resident and twelve draws. Nothing was being streamed and nothing was on screen; that is the
symbology layer touching the canvas for the first time, which is where the browser resolves the font stack.
`warmTextMetrics` now does it at boot, before the loop — the work is the same, but it happens inside the wait
the console already spends on the pak instead of inside a frame an operator feels. It is also a test: if the
next capture still opens with a frame like that, the font is ruled out and the cost is elsewhere. Filed as
[mobile-first-frame](../../benchmarks/opensa-engine/2026-08-25-mobile-first-frame.json), which is explicitly
NOT a performance row.

**Why no moving capture existed yet, found 2026-08-25.** Not for want of trying: the inventory panel polled the collector every 500 ms through an effect keyed on a prop the host recreates on every render, and the host re-renders on every readout — four times a second. The interval was rebuilt before it could fire, so the report froze at its first tick and every capture taken while the map was being FLOWN was a capture of the boot. It healed whenever the console went idle, which is why the 08-23 row carries a 65 s window and reads as idle-dominated. Fixed (the getter through a ref, the effect depending on nothing) and recorded as [a restriction](../../restrictions/architecture.md), because it fails in the direction nothing catches. **Owed:** the same capture with the camera MOVING, which is what the budget is about — and on more ground than a 500 m square. Two things changed the same day so that the next one answers more: the district table gained **`los-santos-wide`** (the pinned rect extended one cell on every side — 16 cells, 1000x1000 units, same centre and same opening point; sized off THIS capture, since `target` is 37.5 MB of the 76.1 and does not grow with the world, so even all 16 resident at once is ~192 MB), and the collector now records **what the worst frame happened under** (when, the drawing-buffer size, the cells created and evicted in that frame alone, what was pending, the draws) — because 600.3 ms with no conditions can be argued about but not acted on |
| [6/02 The flat 2D map](6-display-modes/readme.md) | **BUILT 2026-08-23; every number it owes needs a bake, and a bake needs the game files.** The mode is content now rather than a frame: `plan-mode.ts` opens ONE `tiles.pmtiles` beside the pak over HTTP range requests and draws it under the symbology, keeping the grid only where no tile covers. Four pieces, all tested (39 files / 418 tests green): a **PMTiles v3 codec** in `packages/engine-formats` (hilbert ids, leaf directories once the root outgrows the format's 16 KB first fetch, content dedupe so a bake of the sea is one blue square), **SanMap's tile scheme** with the world square taken from the ARCHIVE rather than assumed to be San Andreas' 6000 — a total conversion has its own extent and a map read on the wrong square is silently in the wrong place — a **range reader** that refuses the two silent failures by name (a server answering `200` with the whole file, an archive with no square in its metadata), and a **baker** (`?bake=tiles`) that renders the pyramid with the console's own orthographic pass, waiting for the streamer before every capture. **The layer is exact under the plan view and under nothing else**, and that is geometry: the ground plane maps affinely to the screen under an orthographic projection at any heading and tilt, while under perspective it is a homography a 2D canvas cannot express — so opening the pyramid takes the camera to the plan view, and a perspective view draws no tiles and says so in the status bar. The bake runs in the BROWSER because the development machine has no headless Chromium, and it refuses a run past 4096 tiles with the count in the message. **Measured:** the dispatch chunk goes **102.64 kB → 112.13 kB raw (33.95 → 38.38 gzipped)** for all of it — format, scheme, reader, layer and baker. **Owed:** bytes per zoom level, time to first usable picture, and the run on a device with no WebGPU — the baker prints the first two as one `[tilebake]` line, shaped to be pasted into a benchmark row **SEEN IN THE FIELD 2026-08-30 and it behaves exactly as this row says it would**: with no `tiles.pmtiles` beside the pak the mode keeps its projected grid and NAMES the reason in the status bar — the readout carries `tiles: "grid — tiles: …/tiles.pmtiles answered 404"`, `draws` 0, `residencyMb` 0, and it runs at 60 fps (114 on one poll) because it is drawing a grid and symbols and nothing else. So the surface is not the constraint and never was: every number this step owes is still owed by a BAKE, and the bake needs the game files. It also has no `inventory` collector of its own — a capture taken in flat mode returns the readout alone, which is worth knowing before a round is planned around it. |
| the engine's own frame (`?overlay=0`) | **BUILT 2026-08-30, on the user's call, and it came straight out of the field round.** The measurements above say a frame on this console is mostly its symbology — `overlay-2d` 2.37 ms of CPU against `engine-frame`'s 0.57 at 150 units, 5.53 against 1.67 on a continuously drawing one — so every number the STREAMING and TEXTURE work is to be judged on was being taken through a tax that has nothing to do with the engine. `?overlay=0` stops drawing it: the symbol pass, the chips, the sketches, the radar and the per-frame update that feeds them. **Skipped rather than subtracted**, because subtracting a segment afterwards credits the engine with work the GPU still did. **The render gate stops watching the board with it** (`NO_BOARD`, one frozen value compared by identity) — a window meant to price the engine may not be paced by a roster nobody can see. The layers stay CONSTRUCTED, so it measures the draw cost and never the allocation, and **the capture says which run it is** (`overlay: false`), because with the pass skipped `overlay-2d` reads ~0 for a reason no reader could otherwise tell from a fast frame. `?embed=1&overlay=0` is the engine with nothing above it at all. tsc clean, eslint 0 errors, 302 dispatch world/map tests, [the parameter table](../../development/query-parameters.md). **The archive the phone serves was rebuilt for it** (`prebuilt/`, stamped `e3c4831`) — the device was running `311f2d1` all round, which is also why 7/10 owes its daylight verdict still. **Owed: the A/B itself** — the same circuit with the overlay on and off, which is the first measurement of the next round. **THE A/B CAME BACK THE SAME DAY** ([the capture](../../benchmarks/opensa-engine/2026-08-30-mobile-overlay-ab-150u.json)): both halves on one pak (17:45 12-08), one app (`e3c4831`), one circuit. **The CPU body is 5.114 ms per drawn frame with the overlay and 2.256 without, and 3.785 of that 5.114 — 74 % — is symbology and board against the engine's own 0.723.** The frame interval halves with it (moving frames only: mean 53.9 -> 30.6 ms, p50 48 -> 24), and the confound runs AGAINST the result rather than for it — the overlay-off half ran on a 35 % larger surface and still finished faster. Two readings ride with it. **`engine-frame` goes UP, 0.723 -> 1.463, and that is SELECTION rather than cost**: with nothing over the map the gate only draws while the camera flies, so every sampled frame is a moving one, while the overlay half is diluted by cheap frames unit motion woke. And **19 of the engine's 98 draws (36 886 of 234 852 triangles) are symbology drawn INSIDE the 3D pass** — the beacons — so the layer is not only a 2D cost. The battery half of 4/01 is measured too: 5.9 drawn frames per second of wall clock against 10.4. **The run also found a defect in the frame scheduler** — see 4/01's row. **Owed: the same pair on the 28-08 pak** — the page was pointed at `build/phone-ls` when it was taken, so this pair is internally valid and not comparable to the 08-30 driven-map row.  **AND THE ROW'S OWN INFERENCE IS CORRECTED, 2026-08-31** — by arithmetic on the numbers already in it, not by a new measurement. *"A frame on this console is mostly its symbology"* is true of the FRAME and false of the CPU it names: the overlay's body is **3.09 ms** and turning it off returned **24 ms** of frame, so **~21 ms of the difference is in no span this repo records**. It is the second full-screen canvas — its raster and its composite over the WebGPU layer — and the A/B cannot prove which, because `?overlay=0` skips the `clearRect` as well and leaves the layer clean for the compositor to skip. The arm that separates them is [9/01](9-the-mobile-frame/readme.md), and until it is run this row may be quoted for *what the overlay costs* and never for *where that cost is*. |
| THE FIELD RUN, redefined | **THE FIELD RUN IS THE MAP, NOT THE BOARD — the user's call, 2026-08-31, and it reorders what gets measured next.** The panel's `field` link carried `units=150&calls=40` from the day it existed, so **every window the map's own work was to be judged in was taken through a 150-unit symbology layer** — one measuring 3.09 ms of CPU and returning 24 ms of frame when it was switched off ([the overlay A/B](../../benchmarks/opensa-engine/2026-08-30-mobile-overlay-ab-150u.json)). The map and its optimisation come first; the board follows. So `field` is now `units=0&calls=0&inventory=1`, its two arms (`cleared`, `engine`) carry the same empty board, and **the declared count keeps its own link (`board`) and its own turn** — nothing is retired, and 5/02 is still one tap when it comes up. **The circuit reads better for it**, because each link now differs from its neighbour by exactly one thing: `cleared` − `engine` is the LAYER, `board` − `field` is the CONTENT, and the content half no longer has to be inferred out of a window that carried both. `links.test.mjs` holds the four together (a test fails if an arm grows a board, or if `board` differs from `field` by anything but the roster). **What this does NOT do is retire a number**: every 150-unit row already filed stands as a measurement of the board, and the rows that are the mock's cadence rather than the product's are marked as such by [9/02](9-the-mobile-frame/readme.md). **Owed first, and it is the whole of the next device session: the map circuit** — `engine` / `cleared` / `field` flown the same way on the pinned district, with the vsync ladder for each |
| the mobile frame audit | **OPENED 2026-08-31 as [chain 9](9-the-mobile-frame/readme.md), URGENT and out of order — it runs NEXT.** Not a new measurement: a read of the console's two most recent phone captures against the code that produced them, and it disagrees with this table in three places. **(1)** The frame is **48 ms — 21 fps — against a declared 60**, and with the CPU body at 10.3 ms, **~38 ms of it belongs to nothing this repo names**. **(2)** *"Mostly its symbology"* is a claim about 3.09 ms of CPU that is carrying a 24 ms result — see the correction on the `?overlay=0` row, and [9/01](9-the-mobile-frame/readme.md) for the arm that separates the LAYER from its content. **(3)** Render-on-demand cannot rest: the mock board replaces itself **20 times a second** and the gate compares it by identity, while the feed it stands in for publishes every **4 s** — so **every 150-unit row this chain has taken measured the mock's churn**, and [9/02](9-the-mobile-frame/readme.md) is one constant. Beside them, four fixes with a measured number behind each: the scene pass's **48 bytes/pixel** of tile working set against the 16 the GPU family budgets for (`target` is now **59.87 MB of 96.45**, 2.3× every texture in the district), **19 render passes a frame** with three bloom levels under 12 px, a `cloud-field` bake paid at the display's rate beside a sky LUT that is keyed, and a static submesh sort redone per instance per frame. **The instrument is already in the captures**: no `timestamp-query` on this adapter, but the vsync ladder (24/32/47/23/2 frames at 1–5 intervals) is a quantized reading of the whole frame, and every step must move it a whole rung or it did nothing. **FOUR OF THE SEVEN STEPS ARE BUILT THE SAME DAY — 9/01, 9/02, 9/03 and 9/07 — and the split is not arbitrary**: those four are the ones a container can finish, because each is either a constant or an arm and none of them is a tuning decision. **9/04, 9/05 and 9/06 are deliberately NOT built**, and the chain's own rule is why — *nothing is tuned before its arm is run*: the MSAA sample count, the scene format, the render scale, the bloom level count and the cloud-field rebake rate are all numbers whose right value is a reading off the vsync ladder on a tiler, and this session had no device. Building them blind would produce three plausible constants and no way to tell which one bought anything. **What the four that shipped owe is one circuit**: `engine` / `cleared` / `field` at the feed's rate, with the ladder for each, the `board` segment on a camera-only frame, and `engine-frame`'s body before and after. **Since 2026-08-31 that circuit carries NO BOARD** — THE FIELD RUN is the map now (the user's call, the row above), which is also the order this chain should have been read in: the map's own frame is what 9/04, 9/05 and 9/06 are about, and none of them was ever a board question. |
| [9/01 The overlay's missing arm](9-the-mobile-frame/readme.md) | **THE ARM IS BUILT 2026-08-31; the three-arm circuit is owed by the device.** The step's whole content was that the existing A/B cannot answer its own question: `?overlay=0` skips the `clearRect`, so the canvas is never dirtied and the compositor is free to skip the layer whole — which makes *the layer* and *its content* one number. **`?overlay=clear` dirties it and draws nothing into it**, so `clear` − `off` is the LAYER and `on` − `clear` is the CONTENT: two subtractions off one circuit instead of one. The rule has one owner (`world/overlay-arm.ts`, 5 tests) rather than a boolean read in four places, and the gate stops watching the board on every arm but `on` — the reason `?overlay=0` already carried, that a window pricing the surface may not be paced by a roster nobody can see. **The capture's `overlay` field is the arm's NAME now, not a boolean**, because there are three runs and a boolean can name two; a row filed before today carries `true`/`false` and reads as `on`/`off`. **And the circuit is one tap rather than three typed URLs**: the panel carries `cleared` between `field` and `engine`, and `links.test.mjs` fails if the three differ by anything but the overlay switch — a pair typed by hand differs by something nobody wrote down, which is the defect this chain was opened on. `projector.update` moved behind the content gate so the cleared arm carries none of it. tsc clean, eslint 0 errors, 612 tests across the console and the panel. **AND THE CIRCUIT'S SHAPE CHANGED THE SAME DAY, for the better**: with THE FIELD RUN redefined as the MAP (see the row above), all three arms carry an empty board, so `cleared` − `engine` is still the LAYER while the CONTENT half moves to `board` − `field` — measured against a run that carried none of it, rather than inferred out of a window that carried both. **Owed: the circuit itself** — `engine` / `cleared` / `field` flown the same way on the 2/03 device, with the vsync ladder for each; then the DPR-1 surface, and the sprite cache ONLY if the content half turns out to be guilty, which is a question for `board`'s turn **THE CIRCUIT WAS FLOWN 2026-08-31 AND CAME BACK HALF-ANSWERED, on app `4ce659b` over the pinned district — [the row](../../benchmarks/opensa-engine/2026-08-31-mobile-map-circuit-arms.json).** Two arms reached a world and the third did not, and neither subtraction can be taken from what they carry, because **the drawing buffer moved under the circuit**: the browser's viewport changed as its chrome collapsed and returned — 720x1218, 720x864, 720x746 and 720x640 inside one session, a 1.9x spread in pixels — so `cleared` and `engine` were measured on different surfaces and the difference between them is not the layer. **Pinning the surface is a PREREQUISITE of this step now, not a refinement**, and it is the same prerequisite 9/04's `?scale=` arm and 9/05's derived pass count need: a capture has to state the size it was taken at and hold it, which today's `overlay.width = clientWidth * dpr` cannot. What the two arms do say is worth having: the empty-board map runs a moving **p50 of 32 ms (31 fps)** in both of them at viewport sizes 1.9x apart, the `overlay:clear` span costs **0.0016 ms** of CPU, and **`target` is a function of the viewport rather than a constant** — 59.87 MB at 1218 tall, 43.01 at 864, 32.35 at 640, which is where 9/04's quoted 59.87 comes from. **And `field` — THE FIELD RUN itself — is VOID twice over**: cell bytes fetched (8 requests / 5.29 MB, then 4 / 2.64 MB on a fresh load) and **zero cells created**, `pendingCells` 0 then stuck at 4, `errors` empty, the screen black, and a `map_goto` — a wake, a flight and drawn frames, so `world.follow()` ran — did not clear it. It is therefore not the render gate sleeping through an arrival and not a fetch failure, the same links streamed 12 and 28 cells on the other two arms in the same session, and the ONLY thing that reports it is the collector's own `VOID: no cells streamed` warning. That void blocks the arm, and it is the next thing to diagnose. **BOTH PREREQUISITES ARE BUILT THE SAME DAY, and neither is a frame fix.** **(1) `?surface=WxH` pins the drawing buffer** (`world/capture-surface.ts`, 8 tests) at that many device pixels whatever the viewport does — the CSS box is still the page's, so the picture is stretched into it, which is right for an arm and wrong for an operator. The report's `surface.pinned` says which way a run was taken, an unparseable value pins NOTHING rather than something else, and **the panel's four measurement links carry `surface=720x1218`** — this phone's full-screen buffer, the largest of the four seen, so an arm cannot come out cheap for having been measured in a smaller window — while every operator link carries none of it (`links.test.mjs`). **(2) The VOID names its cause**: `StreamStats` gained `blockedOnBlob` and `blockedOnArrays` (per update, a cell in exactly one of the two, blob first) and the warning reads them, so the next occurrence arrives as *"4 cells want a level, 1 waiting on their geometry blob, 3 on a texture array"* rather than *"no cells streamed"* — the three cases being the RING, the fetch path and the upload path, which is the distinction the code could not make. **That is an instrument, not a repair**: the cause is still unknown and a guess would be a fix nobody could verify. tsc clean, eslint clean, 39 + 48 + 177 tests across the console, the engine and the panel. Both reach the device with the next `prebuilt/opensa-webapp.tar.gz`. **AND THE ARM IS UNBLOCKED, 2026-08-31 the same night**: with the gate fixed and the archive rebuilt (`--base=./`, which the first rebuild missed), `field` opens on a PINNED 720x1218 buffer — `surface.pinned` true against a CSS box of 360x491, so the viewport may now move without moving what the frame costs. The circuit itself is the next thing to fly, and it is finally an experiment rather than a hope. **TAKEN IN FULL THE SAME NIGHT, and the step's own question is ANSWERED: both subtractions are ZERO** ([the row](../../benchmarks/opensa-engine/2026-08-31-mobile-map-circuit-pinned.json)). `engine` 32 ms, `cleared` 32, `field` 30 at moving p50, three fresh pages over one route with the buffer pinned at 720x640 while the CSS box moved 550 → 491 → 609 → 320. **The LAYER is free** — `cleared` − `engine` is 0 ms and the `overlay:clear` span is 0.0006 ms — and the empty-board pass is −2 ms, inside noise. **So the ~21 ms `?overlay=0` removed on 08-30 is the CONTENT**, which is `board` − `field` and 5/02's turn; the exit this step was told not to take (symbology as instanced quads plus a glyph atlas) is now worth pricing. **And the map with nothing over it runs 31–33 fps against a declared 60 on a MOSTLY EMPTY view** — the pak is four cells and the route flew above them (the operator's correction) — so half the budget goes before a unit is drawn, and the rest of chain 9 is where the time is. |
| **the agent's camera drew a BLACK SCREEN, and nothing in the report said so** | **FOUND AND FIXED 2026-09-05, by the operator's eye again — two flights into the sweep.** `map_goto` makes `yaw`, `pitch`, `height` and `projection` optional, which is right for a tool that is not holding the camera; the console took the argument object, cast it `as MapPose` and flew it. An omitted `yaw` therefore arrived as `undefined` inside a type whose every field is a required number: `forwardFrom` returned `[NaN, NaN, NaN]`, the eye went NaN, **the map drew black and the readout said `NaN, NaN`** — while the streamer, which follows the FOCUS rather than the eye, went on filling cells, so `cellsVisible: 4`, `draws: 96`, `triangles: 241863` and every millisecond in `map_snapshot` described an ordinary frame OF NOTHING. An omitted `projection` did the quieter half: `state()` reads `projection === 'perspective'` as false and composed the frame **orthographically** with `near = 2·distance − far`. **A black frame is also a CHEAP frame** — the geometry clips away — so a window flown through one reads as fast rather than as broken, which is what makes this a measurement defect and not only a display one. The fix is at the seam where untrusted JSON becomes a pose (`world/agent-pose.ts`, 7 tests): `at` must be two finite numbers or the command is refused by name, every other field falls back to **the pose the map is holding**, and the answer states the pose that was FLOWN. Written up as a restriction ([architecture.md](../../restrictions/architecture.md)) because it is silent in every way this repo can be silent: it typechecks (the cast IS the bug), it lints, and no test in the repo could see it — every caller inside the bundle has a complete `MapPose`. tsc clean, eslint clean on the touched files, 22 tests across the link and the new parser, the archive rebuilt with `--base=./` and `webapp-smoke.ts` green. **Owed: the sweep itself**, and the control flight above. |
| **THE PROBE PAYS FOR A READER NOW, AND THE SKY LUT DISSOLVED** | **2026-09-05, and the useful half is that one of the two was not a defect at all.** The sweep left `noprobe` at 1.6 ms and `noskylut` at 1.0 — both flown ~40 minutes after their baseline, inside the session whose `field` drifted 2.1 ms, so both were a band rather than a reading. **Read the code before fixing anything**, and the two split: `PROBE_FRAME_INTERVAL` is **2** — a cube face every OTHER frame, in its own submit, and on a map with no car on it the whole cube is rendered for nobody, since the rigid lane's reflection term is its only reader. Gated on DEMAND rather than on cadence (`hasReflectiveInstance`, 3 tests): no reflective instance, no faces, and neither the latency nor the sharpness moves — which is the variant [the lever's card](../../performance/applied/env-probe-cadence.md) itself called free, with both halves of its trigger fired (a top-three line, and *"a scene class where cars are rarely on screen"* — THE FIELD RUN draws none). The card moved to `performance/applied/`. **The sky LUT, by contrast, cannot be what its arm said**: `skyLutKey` is quantized and this console's hour is static, so `refreshSkyLut` builds a string and early-returns every frame — microseconds. Its 1.0 ms is drift, and it was LEFT ALONE deliberately: a fix for a pass that is already an early return would be a change with no defect behind it. **Owed: the paired re-flight** of the probe gate, which is what turns 1.6 ms from what the ablation says into what the fix removed. |
| **[9/05](9-the-mobile-frame/readme.md) IS DONE, AND THE MAP RUNS AT 60 — the operator's verdict, same day** | **2026-09-05.** The night pair was shot at hour 22 (`night` / `nighthalf`, differing by one budget field, pinned by a test), the operator chose the half-res arm, and **the console's default is `bloomPrefilterScale: 0.5`**: **17.16 ms against 21.52, and 91 % of frames on ONE display interval where the shipped default had 67 %.** `rg11b10ufloat` is NOT shipped with it — it adds nothing on top (17.38 vs 17.16) and stays the fallback for a surface that cannot take the look change. **What that overturns is the load-bearing part**: the 2026-08-12 attribution kept the prefilter full-res *"so sub-pixel emitters survive thresholding; at night that is every street lamp and every headlight, and dimmer emissives are a protected-list item"* — and that reasoning is right about the cost. It was not argued away; it was LOOKED AT, in exactly that case. **A protected-list item is released by a field verdict and by nothing else**, and [1/02](1-the-map-profile/protected-list.md) now carries the release. **Scoped to the console**: the verdict was taken at map zoom looking down, the refusal was written for a street camera, and the GAME still reads `DEFAULT_RENDER_BUDGET`. The previous default stays re-flyable as `?bloomscale=1` (`bloomfull`), or every row taken before today becomes unrepeatable. **And the ladder says where the chain ends**: 90–91 % of frames already sit on one interval, so further bloom work on this device buys nothing — the next millisecond comes from somewhere else (the probe's 1.6 ms and the sky LUT's 1.0 are the named candidates). |
| **THE TWO BLOOM LEVERS, FLOWN — and one of them reaches 60 fps** | **2026-09-05** ([the row](../../benchmarks/opensa-engine/2026-09-05-mobile-bloom-levers.json)), against a `field` baseline re-flown BETWEEN the arms at **21.52 ms**: **`bloomrg11` 19.16 (−2.4 ms, rung 1 80 %)** and **`bloomhalf` 17.16 (−4.4 ms, rung 1 **91 %**, p90 22 against 36)** — the first change measured on this device to put nine frames in ten on ONE display interval. **Neither buys it with resolution, sampling or anti-aliasing**: the world is still full size, 4× MSAA, `rgba16float`; what moves is what the BLOOM chain stores and the resolution its glow is computed at. **They do NOT stack** — `bloomboth` reads 17.38, the same as half alone — so they are ALTERNATIVES, and 90–91 % of frames already sit on one display interval in both, which is the display's FLOOR: **further bloom work on this device buys nothing**, and the next millisecond has to come from somewhere else. Target memory 32.35 → 29.42 → 27.95 → 27.22 MB. **And the baseline itself is the session's other finding**: the same `field` read 23.44, 23.66 and 21.52 across one day with nothing changed — a 2.1 ms spread — which is why every delta here is against a baseline flown in its own thermal window. **What is NOT settled is the LOOK, and it decides whether either ships**: the daylight A/B is indistinguishable and proves nothing, because the cost `bloomhalf` carries is sub-pixel EMITTERS (at half resolution the bright-pass threshold runs on a 2×2 average and a one-pixel street light is diluted below it). So the night pair was BUILT and SHOT the same day (`night` / `nighthalf`, hour 22, differing by the arm alone — `links.test.mjs` pins it, because a pair differing by anything else is an A/B about something else) and is with the operator. **Which arm ships is a LOOK decision, not a performance one**: if half-res passes, it is the arm and the format adds nothing on top; if it fails, `bloomrg11` is the fallback that costs no resolution at all. **Owed: the operator's verdict, then one line in `console-budget.ts`.** |
| **[9/06](9-the-mobile-frame/readme.md) BUILT AND VERIFIED, and the post chain became a BUDGET** | **2026-09-05.** The cumulus bake ran every frame for a field that scrolls **one texel every ~4.7 s**; it now runs on travel, at a rate DERIVED from the texel rather than picked (half a texel — the bound is not *it looks the same* but *the difference cannot be stored*), with the clump scale in the key rather than the timer so a weather change invalidates at once. **Verified as a PAIR, and the pair is the finding**: `field` − `nocloud` is **0.34 ms** where it was **1.83**, so the pass is gone — while the ABSOLUTE mean did not move (23.44 → 23.66), because those two readings are forty minutes apart in one session and the device drifted by about the size of the win. That is now a rule in [the benchmarks readme](../../benchmarks/readme.md): **an arm is subtracted from a baseline flown in the same thermal window, and no fix is judged by comparing today's absolute with an earlier one** — no row in this repo records battery, charging state or die temperature, which stays an open debt. **Beside it the post chain grew a budget** rather than a constant: `bloomFormat` (the chain's own storage, apart from the scene's — `rg11b10ufloat` is half the bytes with the float range kept, which is `rgb10a2`'s win without UNORM's clipping), `bloomPrefilterScale` (where the pyramid starts; halving it quarters the three passes that are 90 % of the chain) and `bloomMinLevelPx`. **The adaptivity is a budget, not a branch and not a platform test**: a surface asks for the option with the best value on ANY device, `resolveRenderBudget` narrows it to what the adapter granted before a single pipeline is compiled, and the capture states the EFFECTIVE set — nothing reads a user-agent string, a screen size or a vendor name. **The default did not move**, on purpose: every row in `docs/benchmarks/` is a measurement of it and the sweep's baseline is what every arm subtracts from, so the two candidates ship as arms (`?bloomformat=`, `?bloomscale=`, panel links `bloomrg11` / `bloomhalf`) until they are flown and the look is judged on the device. `apps/dispatch/src/world/console-budget.ts` is the one named place that verdict lands in. tsc clean, eslint clean, 970 tests across the app and the engine. **Owed: those two flights, then the operator's eye.** |
| **THE ABLATION SWEEP, FLOWN — and it settles [9/05](9-the-mobile-frame/readme.md) by REFUTING it** | **TAKEN IN FULL 2026-09-05, seven arms** ([the row](../../benchmarks/opensa-engine/2026-09-05-mobile-map-ablation-sweep.json)), same rect, same ten-leg route and same `?surface=720x640` pin as the 09-04 ladder, `surface.ablated` read back before any number was kept. **The baseline is also a CONTROL, and it had to be**: the black-screen defect above means a window can be flown through a NaN eye, and a black frame is a CHEAP frame, so nothing in a capture would have said the 09-04 ladder was one. `field` re-flown on the fixed app reads **23.4 ms** against that ladder's **24.5**, p50 20 vs 20, 29 cell creates vs 29 — **the ladder stands and nothing of it is withdrawn.** **THE BLOOM CHAIN IS 7.7 ms OF A 23.4 ms FRAME**: `nobloom` runs a moving mean of **15.75 ms** with **607 of 614 frames on ONE display interval** (p95 18 against 38) — the only change measured on this device that reaches the declared 60 fps, and it costs no resolution, no sampling and no anti-aliasing, so it is not refused by the standing call. **AND THE CHAIN'S TAIL IS FREE**: `bloom4` — 8 levels cut to 4, every level 9/05 named as waste, the ones that are 12x10, 6x5 and 3x3 pixels here — reads **23.2 ms**, which IS the baseline. So the money is in the FIRST levels, at full and half resolution: the post chain's BANDWIDTH, exactly where 09-04's `rgb10a2` arm pointed, and **9/05 as written (derive the level count from the render size) would buy approximately nothing.** The step is re-aimed rather than built: what has to be priced is the SIZE the chain starts at and how many full-resolution passes read `scene-color`, not how many mips it ends with. **`nocloud` is −1.8 ms** and is the one arm here that converts straight into a fix — [9/06](9-the-mobile-frame/readme.md), a 256² bake whose input changes on a scale of minutes, so amortizing it changes no pixel a person can see. **AND THE WORLD IS CHEAPER THAN THE FRAME AROUND IT**: `nocells` removes every resident cell's bundles — all 96 draws and 242 k triangles, the whole reason this surface exists — for **3.8 ms**, half the bloom chain. Read with `nobloom` that is the chain's headline in one line: **the per-pixel work of the post chain outweighs the per-triangle work of the city two to one at this pose**, so what is spent making the map show MORE world is spent against a frame whose largest item is not the world. **And two bakes that are already amortized still cost 2.6 ms together** — `noprobe` 1.6, `noskylut` 1.0, of passes meant to be nearly free (one spread over `PROBE_FRAME_INTERVAL`, one short-circuited by its own input key): more than the cumulus bake this chain was opened on, and neither is a lever yet — both are a second look at what actually runs on a frame that should skip them. The five removals sum to 15.9 of 23.4 ms; the rest is the clear, the resolve, the post pass, the composite and the vsync quantization, which no arm can remove. **The first four arms cost two attempts** — switching arms opened a second console tab twice, caught by `surface.ablated`, discarded rather than filed, and fixed in the opener before the last three were taken. |
| [9/00 The ablation instrument](9-the-mobile-frame/readme.md) | **BUILT 2026-09-04; the sweep is owed by the device.** There is no `timestamp-query` on the 2/03 phone and **no browser flag brings it** — both WebGPU flags enabled in Yandex Browser 26.6.2.117, browser cold-restarted, adapter feature list byte-identical, because the feature rests on Vulkan queue-family timestamp support this Bifrost driver does not offer ([edge-cases](../../edge-cases/browser-runtime.md)). So GPU time is not measurable at all, every per-pass claim in this chain has been an INFERENCE, and the instrument that replaces it is ablation: `?ablate=cells,cloud,bloom,probe,skylut` and `?bloomlevels=` remove one group from the frame, the same ten-leg route is re-flown, and the difference in the window's MEAN over ~450 moving frames prices that group to about half a millisecond. A `FrameAblation` is a CONSTRUCTOR input beside the `RenderBudget`, for the same reason: the bloom level count decides which textures exist, so an arm is a page load. Six panel links (`nocells`, `nocloud`, `nobloom`, `bloom4`, `noprobe`, `noskylut`), each `field` plus exactly one parameter and a test that fails if it is not; `surface.ablated` in the report says what actually ran, because an ablated run is otherwise indistinguishable from a fast one. **`bloomlevels` is not only a measurement** — it is 9/05's own lever, bounded by what the chain can build. tsc clean, eslint 0 errors, 1 134 tests across the console, the engine and the panel. |
| [9/04 The frame's attachment set](9-the-mobile-frame/readme.md) | **DONE 2026-09-04 — the ladder is flown, and it REFUTES the hypothesis it was built on.** [The row](../../benchmarks/opensa-engine/2026-09-04-mobile-map-attachment-ladder.json): five arms, app `60e290f`, one ten-leg route inside the loaded rect, `?surface=720x640` pinned, each window the delta of two histogram readings. Mean frame **24.5 ms `field` · 21.6 `msaa1` · 20.2 `rgb10a2` · 21.0 `scale75` · 16.5 `scale50`**, and only `scale50` moves a whole vsync rung (**95 % of frames on ONE display interval**, p90 18 ms against 38). **`msaa1` at 12 B/px — ONE tile budget, no resolve — won the LEAST of the three byte-moving arms, and `rgb10a2unorm` at 32 B/px beat it while keeping 4x MSAA**, so the tile-size hypothesis is NOT confirmed and **the row it owed `restrictions/gpu-and-shaders.md` is deliberately NOT written**. What the pair names instead: one sample re-tiles the scene pass and changes nothing downstream, the format halves the bytes of every full-screen pass that READS `scene-color` — **the frame is the POST CHAIN's bandwidth**, which promotes 9/05 from an argument to a measurement, and reopens `render-scale-tier.md` (refused on an M3 Pro, where the same knob bought 0.4-1.4 ms). NOT adopted here: `rgb10a2unorm` is UNORM and cannot hold a value above 1.0, so the honest next arm is **`rg11b10ufloat`** — same 4 bytes, float range kept, reported renderable by this adapter. The look verdict on `msaa1` came back *worse* but is **not takeable through a pinned page** (the pin upscales to a CSS box that moved 320-609 px), so a look arm flies UNPINNED and a number arm flies pinned — two flights. The old build note follows. The scene pass's per-pixel working set is **48 bytes** (`rgba16float` x 4 = 32, `depth32float` x 4 = 16) against the **16** Arm budgets for a 16x16 tile on the Bifrost/Valhall family the 2/03 device runs, and past that the driver shrinks the tile and every per-tile fixed cost multiplies — the largest unexplained number in a frame that measures **31-33 fps against a declared 60 with nothing over the map**. The sample count and the scene format left `render/pipelines.ts` and became a **`RenderBudget`** the engine is CONSTRUCTED with, because every pipeline is compiled against them, every cell's bundle recorded against them and the env probe allocated against them: an arm is a page load, not a key press. **`?msaa=1` and `?scene=rgb10a2unorm`; the third arm needed nothing built** — `?scale=` is `Engine.renderScale` and has existed since 2026-08-12. **Three things the build settled that the step had left implicit**: one sample removes a whole TEXTURE rather than just a resolve (`ensureTargets` skips `msaa-color` and the world pass writes `scene-color` directly, through the one `sceneColorAttachment` the probe's faces read too — a `resolveTarget` on a one-sample view is a validation error); the residency accounting multiplied a literal **8 bytes per pixel**, so the format arm would have looked FREE on the memory axis while halving it (`sceneBytesPerPixel` now feeds the scene targets, the bloom chain and the probe alike); and **`msaa=1` loses alpha-to-coverage** — WebGPU has none at one sample — which is the third leg of the 074 alpha-edge fix on every cutout pipeline, and why the arm owes a LOOK verdict as well as a number. A refused value falls back to the DEFAULT **per half**, and the report's `surface` block carries `sampleCount`, `sceneFormat` and `workingSetBytes` so a row cannot claim an arm it did not run. The panel carries four new links (`msaa1`, `rgb10a2`, `scale75`, `scale50`), each `field` plus exactly ONE parameter, held there by a test that strips the parameter and compares the whole URL. tsc clean, eslint 0 errors, 683 tests. **Owed: the ladder itself, over LOADED ground** — the 08-31 circuit flew four cells at 450-900 m where the frustum reaches far past them, so a camera kept inside the rect or the 16-cell `los-santos-wide` pak; then the look verdict, then — only if the tile-size hypothesis is confirmed — the row in `restrictions/gpu-and-shaders.md`. |
| the phone channel, checked against the plan | **TWO DEFECTS FOUND 2026-08-31 while taking 9/01's circuit, and both are SILENT.** **(1) The panel serves its MCP surface out of the process that is running, not out of the checkout.** The phone's tree was `main`, clean, and `git pull` said *already up to date* — while the MCP server beside it still advertised the SIX links of the day before `302bb2d`, so `map_open`'s `view` offered no `cleared`, no `engine` and no `board`, and its `field` was still the 150-unit board THE FIELD RUN had stopped being. An agent that trusted the tool would have measured the thing the user's call of that morning removed, and nothing would have said so. The way through is the `open` JOB (`LINK=…`), which spawns `open.mjs` fresh and therefore reads the CURRENT `app/links.mjs` — that is how all three arms were opened today — but the fix is a restart of `npm run panel`, which only the person holding the phone can do. **(2) `map_release` cannot reach that person.** `phone_state` reports `termux-vibrate` and `termux-notification` as available because the BINARIES are installed, and both failed at the end of this run (the Termux:API app is what actually answers them) — so the run ended with the console still saying *keep this tab in front* and the operator with no way to learn it was over except by being told in chat, which is the one thing the release call exists to replace. |
| [9/02 The mock publishes at the protocol's rate](9-the-mobile-frame/readme.md) | **DONE 2026-08-31 — one constant, and it retires the cadence half of seven filed rows.** `TICK_MS` was 50 with the comment *"the render loop is independent"*, and the render loop is not: `stepOperations` returns a freshly spread board every tick and `RenderGate` compares it by IDENTITY, so **the board forced a draw 20 times a second** on a console whose feed publishes every **4 s** — the mock running at **80×** the interface it stands in for. The tick is `PUBLISH_INTERVAL_MS` now (`ops/feed-rate.ts`, 4000, read out of PCAD's `cadui.lua` rather than chosen), with `?tick=` to put the old churn back for a comparison. **Replay keeps its own cadence and that is not a compromise**: playback is the operator dragging time, so while `clock.mode` is `replay` the loop runs at 50 ms and the board underneath still steps at the feed's rate — without the split, `×2` and `×4` would have become a four-second slideshow, which is 8/03 broken by a performance fix. In `live` the clock loses nothing, because `advance` pins it to the wall clock and the timeline reads its span off the history window. **The two allocations the step priced beside it need no code**: `trails()` (one `Float32Array` per unit) and `fixAges()` (a `Map` of 150) are `useMemo`d on `ops`, so ~3 000 typed arrays a second became ~750 by arithmetic. **Seven benchmark rows now say they were taken at the mock's rate** rather than being retired — their per-frame numbers stand, their `framesSkipped`, drawn-frame rate and battery figures are the stand-in's. tsc clean, eslint 0 errors, 445 dispatch tests, 6 of them new. **Owed: the 150-unit circuit re-taken at the feed's rate** — `framesSkipped`, the vsync ladder, and the battery half [4/01](4-a-console-is-not-a-game/readme.md) still owes |
| [9/03 Board work on the board's clock](9-the-mobile-frame/readme.md) | **DONE 2026-08-31, and the flag it needed already existed.** `beacons.update` and `unitModels.update` ran inside every DRAWN frame — twelve line buffers refilled and twelve `updateDebugLines` issued, 150 root matrices written, a `Set` and an array allocated over the roster, `engine.updateVehicles()` called — and **neither reads a camera**: both depend on `ops` and `selection` alone. So panning an unchanged roster repeated the whole board layer at the display's rate. The gate compares exactly those two values one line above to decide whether to draw at all, so `RenderGate.boardChanged` is that comparison KEPT rather than a second one written beside it. **A forced frame counts as changed, and that is the load-bearing half**: `wake()` is what a host calls for a change the signals do not carry, and a unit model finishing its load is one — the type arrives between frames and only the board layer's next pass claims an instance for it, so without that clause a car would appear whenever the board next ticked, which after [9/02](9-the-mobile-frame/readme.md) is **up to four seconds later**. Plan mode needs none of this: it draws tiles and symbology and has no board layer. 5 tests, both directions and the woken case; 450 dispatch tests green, tsc and eslint clean. **Owed: `board` → ~0 on a frame that only moved the camera**, in the same capture as 9/02's |
| [9/07 The per-frame allocations](9-the-mobile-frame/readme.md) | **ALL FOUR DONE 2026-08-31, and none of them changes a pixel.** Taken out of the plan's own order because the three ahead of it (9/04–9/06) are arms that need a device this session did not have, and the plan puts this group last only so it cannot absorb one that does. **(1) The opaque submesh order is cached on the instance.** It is a function of `submesh.array` — a constant of the model — and `submeshVisible`, while the translucent order is a function of the EYE and is rebuilt because it has to be; at 150 units the opaque half was ~150 arrays of objects, ~150 sorts and ~150 `map()`s a frame for an answer that was the same every time. `setSubmeshVisible` drops the cache, and it now returns early on a write that changes nothing — a caller setting visibility submesh by submesh, which is how `createVehicle`'s caller does it, would otherwise invalidate once per submesh. **The invalidation is what carries the risk, so the guard is at the DRAW level** (`engine.submesh-order.test.ts`, 4 tests over the fake device, proven red on the exact defect): a stale order draws a car's wreck through itself or hides a body panel, and nothing throws. **(2) Three scratch allocations hoisted to fields** — the frame uniform's `Float32Array(104)` and the visible-cell bundle/blend arrays. The uniform needed one honest fix to be safe: `scheduleProbe` left index 91 unwritten when it skipped and a fresh array supplied the zero, so it writes it now — relying on `new` for a value is the coupling that survives exactly until somebody hoists the allocation. **(3) `unitModels.update` stops building a `Set` and an id array per call.** **(4) `CellStore.picking` is TWO capabilities now**, because it had two prices and only one host paid for both: `pick()` resolves against the placement mapper's world-space bounds and reads no index, while the retained index bytes exist for `hidePlacement`, which only the map inspector calls. The console picks constantly and hides nothing, and was holding a full copy of every resident cell's index data — **1.18 MB of the 1.42 the pinned district reported**, growing with the district. `placementEdits` is the second half; the viewer arms both, the console arms one, and `hidePlacement` returns 0 rather than pretending to work when the bytes were never kept. 1499 tests green across the engine and every app, tsc and eslint clean. **Owed: `engine-frame`'s body before and after**, in the same capture as the rest of the chain |
| 9/01 … 9/07 | **not started, and they are the NEXT WORK.** Order inside the chain is its own — 9/01's third arm and 9/02 first, because both are one constant and both can rewrite what every later number means; then 9/03; then the GPU side one arm at a time; 9/07 last, because it is the only group whose win is certain and small and it must not absorb the session the uncertain, large ones need. **Nothing here is tuned before its arm is run** — the chain's own rule, and the repo's. |
| 1/03 … 8/04 (less 1/05, 1/06, 2/02, 2/03, 3/03, 4/01, 4/03, 5/01–5/04, 6/01–6/03, 7/01–7/10, 8/01–8/04) | not started. Execution order confirmed with the user: **the engine first** — the map profile and the phone, then everything above them. 1/03's first look is redirected by the ASTC capture's second answer: `target` is FIXED cost, so the pak — not the render targets — is where scale lives. **7/01, 7/02, 7/03, 7/06 and 7/07 were taken out of turn on 2026-08-22, with the reason recorded**: everything chain 1 and chain 2 still owe is gated on a field run that only the user can take (a pak, a phone, `build/<game>/opensa`), and chain 7's first two steps are what is above them that needs neither — 7/01 is what [6/02](6-display-modes/readme.md)'s tile bake stands on, and 7/02 turned out to be a defect fix as much as a feature (the camera could frame ground the streamer never loads) |

**Owed, and not yet paid:** the evidence table above is still somebody else's measurement, but this chain has
now taken one of its own — [the first real-world mobile row on a streamed SA
district](../../benchmarks/opensa-engine/2026-08-07-mobile-district-inventory.json) (31 fps, 239 MB resident
over 4 cells, Mali-G51). Two things it did NOT settle, and both gate work above: it was taken on the wrong
ground (Ganton, not the pinned district), and **94 % of its frame has no owner** — no `timestamp-query` on
this adapter and empty `spans`. The headline claim — a real district, on a real phone, in an operator's hands
— stays unproven until [2/03](2-real-device-truth/readme.md) records its row on the pinned district.

**The next action is a field run, and since 2026-08-28 it is the AGENT'S** — the phone answers as an MCP
server ([phone-console plan 002](../../../tools-debug/phone-console/docs/plans/002-mcp.md)), so convert,
serve, open the console, fly the camera, read the report and file the capture are tool calls rather than
taps. It still reads `build/<game>/opensa` and no container has one; what changed is who drives the device
that does.

**The first session on that channel took no measurement, and the reason is worth the row** (2026-08-28).
Three things sat between the agent and the world, none of them visible from any screen:
**`npm run phone` had refused every pak since 2026-08-27** — `--lod-only` became a recipe field with
[6/01](6-display-modes/readme.md) and `phone.sh` began expecting it on every reuse, while `pak-recipe.ts`'s
comparison table never grew the row, so the answer was always *"lodOnly: not recorded in this pak"* and
nothing was served (the fix derives the key, as `textures` already did); **the tunnel died mid-session**
(`tunnel inactivity timeout`) after a guard that had already settled swallowed the exit, so it neither said
so nor reconnected; and **opening the console was still a person**, which is now the `open` job. All three
are fixed. **The numbers below are unchanged and still owed** — read the panel plan's *"What the next agent
does, in order"* before re-deriving any of this.

**The second session on that channel took no measurement either, and this time the phone was three days
stale** (2026-08-30). `phone_state` reported `ready` on nineteen checks and `npm run phone` then refused the
pak with *"lodOnly: not recorded in this pak"* — the 08-27 defect the row above records as fixed on 08-28,
because the phone was sitting on a branch that had been merged and deleted under the main-only rule, so its
`pull` had no ref to fetch and every job was running the code that checkout froze at. **A stale checkout does
not only mismeasure; it argues in the voice of a bug that no longer exists.** The panel's preflight now
separates a branch that was never pushed from one whose upstream is GONE and **fails** on the second, and
`main` is a job, so the recovery is two tool calls ([the run log](../../../tools-debug/phone-console/docs/plans/002-mcp.md),
[the restriction](../../restrictions/architecture.md)). **The numbers below are still owed**, and returning
the device to `main` is the one step of it a person has to take.

**THE RUN ITSELF WAS TAKEN 2026-08-30, and it found a blocker that no camera work can get past:
[THE FIELD RUN's own link measures 150 SYMBOLS, not 150 units](../../benchmarks/opensa-engine/2026-08-30-mobile-pinned-district-symbol-only.json).**
The loop ran end to end with nobody touching the phone — `phone_state`, the pak accepted and served,
`map_open` raising the page itself, eight `map_goto` flights the streamer followed, `map_snapshot` reading
the report. What came back says `unitsAsModels: 0` and `unitsAsSymbolOnly: 150`, and the console names the
cause three times in its own `errors`: the board's kinds are `copcarls`, `ambulan` and `firetruk`
(`ops/seed.ts`) while `phone.sh` converts `admiral,infernus,comet`. **So [5/02](5-symbology-and-picking-as-product/readme.md)'s
budget — 150 units each drawn as a MODEL with a symbol over it — has never once been on the table on this
pak**, and every mobile row in the evidence table above was measuring the symbol-only path without saying
so. The next field run is gated on a convert carrying those models, not on a flight.
**AND THE READING ABOVE WAS RIGHT ABOUT TWO OF THE THREE NAMES AND WRONG ABOUT THE ONE THAT MATTERED**
(2026-08-31, fixed the same day). `ambulan` and `firetruk` are stock models the convert had simply left out.
**`copcarls` is not a stock model at all** — the LS police car is `copcarla`, the committed roster
(`scripts/cars-server/data/original.json`, 212 cars) has no `copcarls` in it, and `patrol` is five of every
seven generated units plus four of the named nine. So most of the declared 150 resolved to nothing on EVERY
pak, `--vehicles all` included, and no convert would ever have fixed it. **The reason it read as a thin
convert is that the fallback is not a diagnosis**: a name THIS build cannot draw and a name NO build can
draw leave the same line in the log, and here a real second cause was sitting beside it. Both halves are
closed — the board asks for `copcarla` now, `phone.sh` converts the board's own three rather than
`admiral,infernus,comet` (a default from before this console drew models at all), and `DEMO_MODELS` is the
one owner of that list with `seed.test.ts` failing if the script drifts from it, proven red on the old
value. **What is still owed is unchanged and is now purely a flight**: 5/02's milliseconds at 150 units
drawn as MODELS, which no capture in this repository has ever measured.
**The frame question is still unanswered, and for the old reason plus a new one.** The best window reached
**125 frames against the collector's own 300 floor**: each raise of the console answered for 40–80 s and
then stopped polling — Android suspending a tab that is not in front — and a re-raise is a fresh boot, so
the flight that DID run with the world resident was lost before it could be read. Three raises, three
freezes. **That condition is a person's to hold**, and it is the one part of this loop no tool surface
closes. What the window does establish, filed as a capture that is explicitly NOT a row: 4/4 cells,
resident **71.0 MB** (target 32.3, texture 25.8, cellVertex 10.3), 112 draws, 278 k triangles, 21.7 MB in 23
requests with 19.0 from cache, boot open 553 ms — and **147 of 190 chips dropped** at 360x320 CSS, which is
[3/03](3-the-operator-surface-on-a-phone/readme.md)'s budget measured in the field.

**It is two buttons now, and that is deliberate** (2026-08-27, on the user's instruction that the panel
should be made convenient rather than the work deferred): the phone panel's **THE FIELD RUN** link opens the
console at the declared count (`units=150&calls=40&inventory=1`) with no query to type, and the console's
readout files its own capture into the panel — district, mode, unit count and its own query included, with
the pak's recipe and the device stamped on by the panel. The copy-JSON-paste-and-name round trip that used
to sit between a measurement and its file is gone; `Commit & push` is the second button. The panel also
builds and opens the one-file shareable console ([2/02](2-real-device-truth/readme.md)'s device half), and
`prebuilt/opensa-webapp.tar.gz` was refreshed in the same change, because a panel with new buttons serving
an old app is worse than neither.

**The blocker that used to sit here is GONE, and the row said otherwise for eleven days.** On 2026-08-09 the
phone's `game-src/original` and its `build/phone*` output were symlinks into ONE folder, so the converter
rewrote the archives it was reading (`gta3.img` 1073 `.osm` bundles, `gta_int.img` 155 — measured with
[`img-census.ts`](../../debug/README.md)), and this table said 1/03 waits until they are restored. **They were
restored the next day**: both archives re-extracted from `Download/GTA CORP.rar` and verified `clean`, which is
recorded in [the 08-10 texture budget](../../benchmarks/opensa-engine/2026-08-10-district-texture-budget-los-santos-centre-astc.json)
— the first pak built from a restored source, 281 map objects converted where the damaged one gave 0 — and the
[08-12 ASTC baseline](../../benchmarks/opensa-engine/2026-08-12-mobile-pinned-district-astc.json) was taken on
it. `guardOut` refuses that configuration now and `npm run phone` checks it before deleting anything. So
**1/03 is not blocked on the game files; it is waiting its turn**, and the row is corrected here rather than
left to cost a session (`CLAUDE.md`: if the reading and the code disagree, the code is the truth and the doc
is the bug).

**Two variables moved between that capture and the 08-07 row, and the row must say so.** The ground changed
(Ganton → the pinned district) and, since [200/2-02](../200-platform-reach/2-universal-textures/readme.md)
landed, the phone workflow writes **ASTC** rather than RGBA8 by default — a quarter of the texture memory on
the same texels. So the pinned-district capture is the chain's **new baseline**, not a comparison against
239 MB / 31 fps: the old row was already off-series. What it must not do is claim a delta against it. The pak
records its own format now (`report.json` → `build.textures`), so the row can state which. `TEXTURES=rgba8`
takes the other side when the format itself is the question.
