# Debugging toolbox

The standing convention (2026-07-22): **a debug script that proved useful is kept in the repo** —
one-off inspectors live in `scripts/debug/`, and every kept script gets a row here (what it answers +
how to run it). Throwaway experiments run as `scripts/debug/.tmp-*.ts` (inside the repo so `@opensa/*`
path aliases resolve) and are deleted after; the moment one earns its keep, it is renamed, linted and
documented here in the same change.

## The triage method

Field bugs are traced to data BEFORE any code is touched (plans 084/085 proved the order):

1. **Symptom** — the user's in-game report, with a position and a model name if possible (F2 helps).
2. **Source asset** — is the DFF/TXD/IDE itself what we think it is? (`dump-texture`, `dump-chunks`,
   `find-instances`, `model-bbox`, IDE flags.) **Ask the MATERIALS, not the dictionary**: a TXD serves many
   models, so "this txd has a glass texture" does not mean this model draws it — plan 092 picked a field
   control that way and its windows turned out to be opaque (`dump-dff-materials.ts` is the answer).
3. **Pipeline stage** — which converter stage owns the transformation; its report/ledger first
   (`report.json` → `textures.missing` / `textures.crossTxd`, the pack log's ⚠/ℹ lines).
4. **Pak bytes** — what actually shipped (`dump-osm`, `dump-osm-meta`, `dump-texel-avg`). Byte-faithful
   output means the LOOK is the data's — see row G of plan 085: a "missing" radar texture was the mod's
   own near-black texture, proven by matching opaque texel averages source↔pak.
5. **Shader** — only after 1–4 are clean. The test suite cannot catch shader defects (the fake GPUDevice
   records, it does not validate); patch the shader to output its own terms as colour channels and shoot
   the game headless instead (that is how "ambient = 1.0 under the car" was found in 084).

The field verdict decides. Every measured number lands in the owning plan doc in the same change.

## Inspectors — source-asset side (`scripts/debug/`)

Run any of them as `npx tsx scripts/debug/<name>.ts …`; `--game <id>` picks the variant under
`game-src/` (default `original`).

| Script | Answers |
| --- | --- |
| `find-instances.ts <model\|id>…` | every placement of a model across ALL map IPLs (text + binary streams), with source file — "ghost text placement vs real streamed placement" |
| `world-ambient-hours.ts [--floor 0.13]` | the world ambient term hour by hour, with OUR deliberate floor (093) printed beside the timecyc's own value and a column saying which of the two wins. Boots nothing — it drives the environment adapter directly. Answers "is the day-shaped floor really retired after dark, and does it retire smoothly": at whole hours `dn` is binary, so the floor is 0.13 from 07:00 to 19:00 and **exactly 0 from 20:00 to 06:00**, and probing fractional hours shows the hand-over is a continuous one-hour ramp with no step. Read the floor column before judging a night look: where it is 0 the frame is strict SA parity and anything dark on screen is AUTHORED |
| `ipl-row-census.ts [<game-dir>]` | **what a game costs against SA's permanent-placement ceilings**: every text-IPL `inst` row and every gta.dat slot that carries one, including what modloader adds or overrides. The two numbers that decide whether a map-content plan can ship, and the ones plan 07 spent a fortnight guessing at. Points at a BUILT tree (`build/original/opensa` by default) or at a real modded install — both were needed to settle the question: our build reads 20 146 rows / 38 slots, the reference install 72 914 / 36 with six files over the stock 4 096-row buffer. **Only files carrying `inst` rows take a slot** — counting an inst-less IPL as one is how a hand count reported 37 where the truth was 36. The ceilings it prints are STOCK; a real install may lift them (`docs/gta-sa-original/reference-install.md`), so read that before acting on a red line |
| `inspect-area.ts <x> <y> [radius]` | every instance near a point and WHY it would (not) render: def, LOD class, interior, DFF/TXD presence, parse result |
| `lod-link-check.ts [<game-dir>] [<max-lines>]` | **does every LOD link still point at its own LOD?** Resolves every `lod` field of a tree — text IPLs AND the binary streams inside `models/*.img` — and prints the ones whose target does not stand where its owner stands. A `lod` cell is a ROW INDEX, so any stage that drops or inserts an `inst` row re-points every link past it at another VALID row: no error, no missing file, just a building with no LOD. Stock is the baseline and it is clean (`game-src/original`: 6 103 links, 0 findings), so a finding on a built tree is OURS. It found the 11 links `0. Map Fixes Pack`'s stream merges shipped one row off ([ipl-row-removal-breaks-lod-links.md](../open-issues/fixed/ipl-row-removal-breaks-lod-links.md)). Defaults to `build/original/sa`; the same check is a hard build guard on the finished `sa` tree. **What it cannot see**: a shift that lands within 20 u of the owner — the radius is the floor of what it guarantees, not the whole class |
| `dat-order-check.ts [<game-dir>…]` | **does any `inst` row place a model whose IDE `gta.dat` lists LATER?** The game reads `gta.dat` top to bottom: an `IPL` line places its rows THERE, so a definition further down does not exist yet and the row is refused — and only with `modloader.asi` OFF, because modloader supplies the same mod IDEs itself, early, which is why this shipped for months unseen. Reports one finding per (id, IPL) with both gta.dat line numbers and the row count. Text IPLs only, deliberately: a `<area>_streamN.ipl` streams by distance long after every IDE line is read. Stock is clean (`game-src/original`: 9 268 rows, 0), so a finding is OURS — the built `sa` tree of 2026-08-21 reported **137 rows / 31 ids** across `lae`, `las`, `lahills` and `vegase` from six mod IDEs. Defaults to `build/original/sa` plus stock; the same check is a hard build guard (`assertDefinitionOrder`) on the finished `sa` tree |
| `mod-id-collisions.ts [game = original]` | **which model IDs two sources claim with DIFFERENT models** — across stock and every mod of `mods-src/<game>/mods` (layered or flat). The duplicate is not the finding: two claims with the SAME name are a deliberate redefinition, two with different names mean mod-installer's supersede rule deletes one and every placement of that id draws the other mod's model. Static (`.ide` / `.ide.merge`, no install), so it runs before a build rather than after. On `original` it found a train mod's masts sitting on Map Fixes Pack's neon ids 18631/18632; what is left after that mod was dropped is a BASELINE of 7 stock duplicates (16700–16708) between the loaded `countn2.ide` and `leveldes.ide`, which `gta.dat` never loads at all ([unloaded-map-data.md](../gta-sa-original/unloaded-map-data.md)) — the scan reads the folder, not the load list, so those seven are what "clean" looks like |
| `mod-layer-conflicts.ts <base> <install A> <install B> [mods root]` | **what the target layer of a LAYERED mods folder writes over the `common` layer** (mod-installer plan 011). Empirical — it diffs three states of the same install (base → A → B) rather than re-deriving the overlay/bake rules, and it names the mods on both sides. Reports loose FILES and ARCHIVE ENTRIES apart, because a file diff cannot see inside `models/*.img`. The header carries the two install commands to produce A and B. **Read the install LOG beside it**: an id superseded by a baked IDE deletes a row from a stock `.ide` with no mod shipping that file, which shows up here only as "the `.ide` changed" — on `original` (2026-08-15) that is how the train mod's `18631/18632` were found sitting on Map Fixes Pack's neon objects |
| `cutscene-converter-drive.ts <game> <cars> <out> [shots]` | **does the standalone app still work end to end** — the three pickers, a real run, the status line, Exit. Drives the BUILT app (`npm run --workspace=@opensa/cutscene-converter build` first) with the native folder dialogs stubbed in the main process, and prints each step's verdicts, the busy line, the finished line, the buttons and the about line; exit 1 if the run ends in the failure state. The renderer has no test lane, so its defects were reaching Windows instead: the 0.4.0 field run showed a BLACK window with the converted files already on disk — an effect written with a concise body handed React `scrollIntoView`'s return value, React took it for the cleanup function, and the whole tree unmounted at the end of the run. This script reproduces that class in seconds. `out` is written for real (~280 MB for two cars), so point it at a scratch folder, and pick cars that actually cover cutscene slots (`bobcat`, `bravura`, …) if you want the conversion to do work |
| `handling-diff.ts [baseline.cfg] <candidate.cfg> [--rows]` | two `handling.cfg` tables column by column, and **how much of the difference the engine can even see** — per column: how many cars changed it, the mean relative move, and whether it is one of the five columns the adapter maps. Answers "a well-calibrated handling mod is installed and the car still feels wrong": against a 210-car realism table, 58 % of its edits never reach the physics (081/01) |
| `video-routes.ts [<game-dir>] [--seeds 5] [--tries 40] [--to 25] [--worst 5]` | whether the 096 video mode's seeded route builder can find drivable footage in each region, WITHOUT booting the game: accepted routes per region, why the rest stopped (`too-tight` / `dead-end` / `region`), mean length vs target, sharpest junction turn, tightest smoothed corner, straightness histogram — plus the hardest accepted routes, which is how a corner-heavy route is picked deliberately for a field round — paste the coordinates it prints into `?video=1&at=x,y` to drive that street (096/04). A pin the builder cannot walk out of is logged and falls back to a seeded route: downtown LS is one such place (`docs/edge-cases/route-graph.md`). Dir defaults to `build/original/opensa`; only the `original` variant ships a path graph at all (096/01) |
| `video-shiver.ts <harness.log>` | **"the camera shivers" → which channel is actually moving.** Reads the `[diag]` lines a `?video=1&diag=1` run prints (one row per RENDERED frame — the `[video]` series is 10 Hz and cannot see a per-frame defect) and reports, per shot and BETWEEN declared cuts, the high-frequency energy of each channel: the drawn car, the eye, the aim, and where the car lands on screen (in px of the field window). The `[mount]` line says whether a car-mounted shot is riding the car rigidly or dragging behind it, and splits the eye's jitter into along-travel and lateral. This is what separated a noisy subject from a noisy camera in 096's first field round: the car was smooth (0.006 m), the planted tripod eye did not move at all, and the tracking eye jittered 0.023 m ALONG the travel axis — which named the damper, not the physics. **Read the `same-frame` / `frame-late` pair first**: they are the same geometry judged against the frame the pose lands in, and the gap between them is the wiring rather than the director. Round 1's fix moved the intent 0.311° → 0.032° while what rendered stayed at 0.305°, so the bug survived a measurement that called it fixed — `frame-late` is the tripwire that would have said so (096 ledger, field rounds 1 and 1b) |
| `slow-frame-census.ts <console-export.log> [--worst 8]` | **what the slow frames of a HUMAN DRIVE actually were.** Input is a browser console export, not a harness log — the game prints a `[slow]` line for every frame over `SLOW_FRAME_MS` in any dev run (`perfLogs = IS_DEV`), so a drive measures itself with nothing switched on. Reports the frame-ms spread, which block owned each frame, the named spans that appeared, and the GPU pass beside the CPU render — that last pair is the point: `other`/`unattributed` covers BOTH the GC tail and the CPU waiting on the GPU, and only `gpu` on the same line separates them. It settled 091 on 2026-08-02 — 223 slow frames, **zero** carrying a vehicle span (the per-type car cost never lands on one), gpu pass mean 13.73 ms against a CPU render of 0.33 ms (GPU-bound, not GC). It also flags a **negative `unattributed`**, which means a span is being subtracted twice and is the only signal that exists for that class of defect (`docs/restrictions/architecture.md`) |
| `video-accept.ts <harness.log>…` | video mode's acceptance exam, rolled up off a harness log — the numbers every 096 phase re-sits, so a change is judged against the same question the last one was: scenes, directed frames, the share inside the safe frame, cuts and what ended them, how often the pan cap bit, `[cam] jump` lines, the realised mod-car share, and the regions IN THE ORDER THEY PLAYED (the cycle is an acceptance question a log can answer). Dedupes the report lines the harness echoes twice, and weights each scene's `safe` share by its own frame count rather than averaging shares. **Reads all three scene kinds (096/07)**: a `drive` and a `walk` carry the director's ledger, a `fly` has no subject and so contributes no directed frames — it gets its own line (passes flown/planned, metres of staging lift, live guard hits over probes) rather than being averaged into a safe-frame share it has no opinion about |
| `video-chrome.ts <appUrl> [timeoutMs] [--control] [--shot <png>]` | **is the CHROME out of the shot?** Boots the real app, waits until a fragment is actually on screen (the video overlay carries `is-clear`) and reads the four pieces of UI out of the DOM: the "Click to play" prompt, the HUD clock, the Fullscreen button, the dev perf readout. Exit 1 if any of them is in the frame. **Run `--control` against a URL with no `?video=` first** — it samples as soon as the game is playable and expects all four; without it, an all-false video run is equally consistent with a probe looking for the wrong selectors. This is the only check for the 096/08 class of bug: video mode asks for the hide from inside `boot()`, where React is not listening yet, and nothing in the console notices — `[video]` reports know nothing about the DOM and the bench harness screenshots the black end card (`docs/restrictions/architecture.md`) |
| `touch-controls-check.ts <appUrl> [timeoutMs] [--shot <png>]` | **do the on-screen controls reach the game?** Boots the real game in a TOUCH-emulating context (`hasTouch`, which is what `isTouchDevice()` reads) and drives the overlay by its own pixels: the two joysticks and the Jump button, with the answers read off the dev perf HUD — walk distance, how far the next walk turned after a held look, and the jump's lift. Exit 1 if a control does not reach the game. Needs `npm run dev` (a prod build has no `#engine-hud`) and an OPEN spawn — the walk checks read where the player actually travelled, and a wall in front of them fails a healthy build. It exists because the overlay was lost for a whole release: the three-host that mounted it was deleted in the engine migration and nothing re-mounted it in the engine host, which no unit test in this repo can see (`docs/restrictions/architecture.md`). The turn it prints is a connected/not-connected signal, NOT a sensitivity measurement — auto-centering swings the camera back the moment the player walks |
| `road-straights.ts [minLength]` | REAL flat straights + crests from the game's own vehicle path graph (`NODES*.DAT`): each run's length, its Δz, its heading, and crests as rise/drop within a launch length — where a scripted drive scene can be put without guessing coordinates (081/01) |
| `scm-disasm.ts <file.cs \| dir> [--census] [--strings] [--json] [--out <dir>]` | what a compiled CLEO `.cs` actually does: the Sanny-like listing (plan 097/01's `disassemble()` behind a CLI — the exact format committed at `fixtures/custom/cleo-listings/`), the per-file + global opcode frequency table, or just the string operands (model names, GXT keys) at a glance. Decodes to the `__SBFTR` footer boundary; an unknown opcode is a located error naming the offset, never a walk into the metadata |
| `exe-field-access-scan.ts --offset 0x26,0x28 [--width 16] [--context 0x8e3fb0,$0x34,…] [--window 150] [--exe <gta_sa.exe>] [--dis <listing>] [--all] [--json]` | **the completeness scan behind an engine patch** — every instruction in the shipping exe that touches a struct field at the given offset(s) with the given width (`i686-w64-mingw32-objdump`, ~3 s; `--dis` reuses a listing), and which sit in a CONTEXT (a pool/global address or a `$0x<sizeof>` stride within ±window lines) that marks the base register as the struct you mean; `(%esp)/(%ebp)` tagged `stack`. Answers "did I hook EVERY reader of `IplDef.+0x26/+0x28`?" — perfect-map 004 nearly shipped with one `RemoveIpl` back-edge re-read unpatched, 011 ran this to the end: 192 → 34 in context → 3 readers by hand (`asi/perfect-map/docs/plans/011-ipldef-dummy-range.md` step 2 is what a finished classification looks like). The survivors are still a reading job; pair it with a gta-reversed grep, and believe the filter only once it retains the sites you already know |
| `cleo-census.ts [paths…] [--json]` | which opcodes a CLEO corpus uses, sorted by frequency — the table plans 097/03-05 implement against. Defaults to the fixtured corpus (`fixtures/original/cleo`, regenerate via `npm run test:fixtures`); the `status` column is `-` until the VM handler registry lands (plan 03), then it becomes used/implemented/unimplemented — the same code path plan 07 promotes to CI. Baseline: 115 unique opcodes, per-script counts in the 097/01 ledger |
| `cleo-run.ts <file.cs> [--ticks 60] [--fps 60] [--calls 60]` | run a compiled CLEO script HEADLESS on the plan 097/03 VM against the recording mock host — the host-call trace reads as a story ("request 14645 · create at x,y,z · rotate 4.8°"), plus peak instr/tick, ms/tick and any thread faults. The fastest "what would this mod DO" answer, no browser; `--atlas` (native-call resolution) lands with plan 097/05 |
| `cleo-trace-fixtures.ts` | regenerate the corpus trace snapshots at `fixtures-src/cleo-traces/` (the source; the `fixtures/custom/` mirror is written too) (plan 097/07 decision 6): each supported script's deterministic host-call story (seeded random, canned host — run configs live in `packages/cleo/src/vm/corpus-traces.ts`, shared with the regression test). Run after a DELIBERATE handler/atlas behaviour change and review the diff — the diff is the change |
| `dump-texture.ts <txd> <name> [out.png] [alpha]` | one TXD texture as PNG (software DXT decode). **Gotcha:** transparent texels take the viewer's background colour — always check the `alpha` dump too; for DXT1a ground truth decode blocks (3-colour mode ⇒ index 3 is transparent black) |
| `impostor-density.ts <model…> [--cards 4,2] [--px 64,32] [--azimuths 16] [--blend] [--windings 2]` | **is the impostor as dense as the tree it replaces** (plan 013 step 03): renders the HD mesh and the card cage from the same poses with the same software rasteriser, at the size a tree really has on screen (`--px` is its HEIGHT in pixels — ~64 at the 150 u switch, ~32 at twice it), and reports covered share, alpha MASS and luminance per candidate against the HD, with the per-azimuth spread. `--blend` composites each card back-to-front with no depth write instead of the cutout union, and `--windings 2` draws each card twice — together they reproduce the pre-step-02 class, which is how the ×1.59 the field saw was priced against the ×0.97 it ships as. Needs no build |
| `impostor-atlas-census.ts <model…> [--tex 512] [--cards 4] [--ss 2] [--png <dir>]` | **what a tree impostor's atlas IS, baked in process in ~2 s instead of a ~10 min pmb `trees` stage** (plan 013): canopy fill, canopy MASS (mean alpha — the only coverage figure that survives antialiasing), the speckle share, mean/alpha-weighted RGB and median luminance, per card and per tree, plus the bake time. It bakes from `mods-src/original/vegetation` with the stage's `--prelight`, resolving each material's texture across the WHOLE folder — a tree's leaves routinely live in another mod's TXD, and baking with only `<model>.txd` silently produces an untextured grey atlas that measures like a solid canopy. `--png` writes the atlas and a `.grey.png` composited over mid-grey: **look at that one** — a viewer paints transparent texels white, and a sparse dark canopy on white reads as a white-leaved tree |
| `dump-chunks.ts <file> [filterHex]` | a RenderWare file's chunk tree — WHERE a plugin chunk lives |
| `img-census.ts [<path/to/gta3.img>]` | **is this game archive still the game's, or has a converter already rewritten it?** Entry count and a histogram by extension, read off the VER2 directory alone (8 bytes + 32 per entry — cheap enough for a 1.2 GB archive on the phone). The verdict line is the `.osm` count: `opensa-pack` deletes the model entries it converted and inserts `<model>.osm` bundles in their place, and a stock archive cannot contain one. It exists because that rewrite lands on the SOURCE when `--out` and `--game` resolve to the same directory (two symlinks into one folder, 2026-08-09) — the tree still boots, the next convert simply has less to convert, and nothing else says so. `guardOut` refuses that configuration now; this answers whether a copy was already run in it |
| `model-bbox.ts <model>…` | render extents (DFF) vs collision extents (COL) — partial mesh vs transform/culling bug |
| `dff-reflection.ts <car.dff> [more.dff…] [--diff]` | **the three numbers a car's shine is made of, and — with two files — exactly which of them a `vehicle-optimizer` run moved.** Per texture and as a histogram: the MatFX env-map `coefficient` (the mirror strength AND the author's marking of which surfaces reflect — body, glass, chrome), the SA reflection `intensity` (`0x253f2fc`, on nearly every material, ×8 in the install's SkyGfx pipe, so anything ≥ 0.125 is saturated and looks identical) and the specular `level` (`0x253f2f6`, ×3 there — **the term a "too shiny" report usually means**: the reported car sat at 0.26–0.56 against a tasteful 0.05). A 0 is counted apart from a missing chunk: it means the author called that part matte. `--diff` prints only the materials whose values changed, which is how three field arms of "nothing applied" were settled — plan 003's ledger and `docs/gta-sa-original/skygfx-fork-vehicle-pipe.md` carry the measurements |
| `dump-dff-materials.ts <model>…` | per-material DFF breakdown: texture, tris/verts, DAY vs NIGHT prelit RGBA averages + per-material bbox — "which submesh is this and how does the artist light it at night" (closed 085 rows G/H) |
| `txd-retune.ts <mod.txd> [--match <txd\|entry>] [--halve n] [--add <txd>#<name>] [--max n] [--no-mips] [--write]` | **rewrites** a mod's dictionary: every texture resized to the dimensions a REFERENCE (normally the stock) dictionary carries, plus textures an earlier mod ADDED that this one would silently drop. Dry-run by default; prints the plan and the bytes saved. Three real cases it exists for: an HD retexture that cost 5.6 MB against a stock 106 KB (mod 56 → −95 %); mod 4 overwriting mod 3's `cj_commercial.txd` so `cj_frame_glass2` vanished and mod 3's fridge lost its glass (the pak already reports these in `report.json` → `textures.missing` — `--add` is how you put one back); and simply halving an oversized mod (58 → −60/−75 %). **`--match` is a FLOOR as well as a target**, so `--halve` on a mod that already carries some textures at stock size shrinks only what is genuinely oversized. **Gotchas:** the author's DXT format is kept (forcing stock `dxt1` onto a `dxt5` glass would throw away its alpha), and a reference with a different ASPECT cannot be reached by halving — the tool stops at the nearest step above it and says so rather than squashing what the author drew |
| `plate-census.ts [model…]` | which models wear a license plate and how: per model the `carplate` (text strip) / `carpback` (city background) quads, their geometries and tri/vert counts, plus the both-vs-one-face split. The stock sweep: 14 865 DFFs → 143 plated, 139 with both faces (082 phase 0) |
| `plate-render.ts <vehicle.txd> [out.png]` | composed plates from a real `generic/vehicle.txd` → a zoomed PNG, plus the derived cell grid and compose timing. **Point it at a BUILT pak**: a mod may replace `platecharset`/`plateback*` wholesale (the shipped pack carries 512×256 backgrounds against the stock 64×32), and this is what proves the grid still lines up with the atlas the game actually reads |
| `alpha-class-census.ts [--img <path>] [--flips] [--json <out>] [--below/--above/--near <share>] [--txd <substring>]` | the alpha CLASS of every texture in an archive — which ones the pack sends to the depth-writing cutout pass and which land in the blend pass, **where the background paints over the foreground** (plan 092). Reports both readings of the histogram: the current absolute one (`mid ≤ 2 %` ⇒ cutout) and the mask one relative to the A2C reference 128 (`below` / `near` / `above`), plus the flip list. Defaults to `build/<game>/sa/models/gta3.img` — the merged, mods-installed tree — and **silently falls back to `game-src/` when that target was not built** (an `opensa`-only build leaves no `sa/`), which is a DIFFERENT, unmodded population: it prints the archive it read, so check that line before comparing two runs. The thresholds are CLI flags on purpose: the class is BAKED, so the rule is fitted on the census instead of on a re-pack |
| `txd-alpha.ts <txd\|path>…` | per-texture format/hasAlpha — vanilla only puts a model through blended render states on its ALPHA pass, so a DXT1 no-alpha texture draws opaque even on an ADDITIVE-flagged def (085 row H) |
| `find-2dfx.ts [--img <path>]` | 2d Effect entries across the map: type histogram + decoded roadsigns; diff archives to expose re-export damage. It byte-steps for the chunk id, so it also reports FALSE POSITIVES (particle names full of binary garbage) — good for dumping, not for counting |
| `two-dfx-census.ts [--game <name>\|--img <path>] [--frames]` | entries **and models** per 2dfx type + the payload sizes seen, walked through the real chunk tree (`extract2dfxEntries`) — the trustworthy denominator for "we carried N of M" when a LOD step starts keeping more than lights. Stock `original`, 14 865 models: light 2203/327, particle 64/43, ped-attractor 916/266, enter-exit 78/71, roadsign 489/207, trigger 33/7, cover-point 15 007/1210, escalator 5/4; every type has ONE payload size map-wide. `--frames` adds the frames the fx geometries hang off (**stock: 0 rotating, 0 scaled, 4 translating — all four are vehicles carrying lights only**), which is what says whether a payload-aware carry can move any output at all. **`build/<game>/opensa/models/*.img` is NOT the shipped corpus** — the map lives in the pak, those archives hold ~540 models |
| `two-dfx-space.ts [--game <name>]` | whether each 2dfx type's authored position is MODEL-LOCAL or WORLD — the fact that picks the transform a LOD carry applies, and the one whose absence would have thrown every roadsign plate a kilometre (plan 100/00). Scores each entry twice, against its model's origin and against the nearest placement of that model, so the verdict is a comparison and not a threshold; a model no IPL places is reported `unplaced` rather than guessed. Stock `original`: **type 7 is world (489/489), every other type is model-local**, unanimously, and the per-type totals reconcile with `two-dfx-census.ts` exactly. This is the measurement behind `space` in `@opensa/lod-common`'s carry policy — re-run it before adding a row |
| `ide-flag-histogram.ts` | which IDE object-flag bits the map actually uses, with example models per bit (flag semantics: `packages/renderware/src/parsers/text/ide-flags.ts` — verify bits against a real asset before acting on them) |
| `audit-rw-coverage.ts` | what the archive's DFF/TXD data contains vs what our parsers handle (chunk histograms, parse failures, dropped textures) |
| `check-cell-signs.ts <x> <y>` | the cell build's roadsign path offline — where a missing sign drops out |
| `procobj-stats.ts <x> <y>` | procobj scatter counts for one cell, per model/category |
| `procobj-species-floor.ts [--limit 150] [--density 1] [--floor 0] [--stride 7] [--cells 200] [--rules <procobj.dat>] [--build-time]` | **does a clutter cap drop a SPECIES to zero, and does the floor stop it** ([plan 012](../../tools/sa-procobj-placement/docs/plans/012-species-representation-floor.md)), through the shipping functions rather than a model of them — it calls `procObjCellBudget`, the same decision the engine spends. Default mode is the RUNTIME cell cap: on stock rules it binds in 58.3 % of clutter cells and zeroes ≥1 species in **17.7 %** of them (worst `8,-3` at 16 of 23 placed). `--floor N` turns the fix on and prints its price: **N=1 → 0 % losing a species for 0.32 % of the drawn placements traded**. Pointed at the BUILT `opensa` tree's `procobj.dat` it reads the SAME 17.7 % — since 2026-08-10 that tree carries all 95 rules, so the "latent on what we ship" verdict is dead. **Counts a species per MODEL, not per batch** (batches are model×surface; a bush drawn from the neighbouring surface has not gone missing) — a per-batch count reads 19.8 % and is not comparable. `--build-time` runs `convertProcObj`'s whole-map pass per species (candidates → `lottery < 1` → MINDIST → `procObjMax`): nothing is zeroed at any stage, and MINDIST provably cannot (empty grid ⇒ the first placement of a batch always survives), though it is where the population goes (`sjmcacti2` 152 → 2 map-wide). Self-checks the cap identity — binding ⇒ exactly `limit` drawn — which caught the rig, not the engine, on the first run. **Read its corpus line**: geometry comes from `game-src`, so `--rules` is how a shipping-set question gets asked |
| `procobj-biome-vs-surface.ts` | **does BIOME tell the scatter anything SURFACE does not already say** — the go/no-go that re-scoped [plan 011](../../tools/sa-procobj-placement/docs/plans/011-biome-zone-density.md). Buckets every rule-bearing collision face by area, region (`map.zon` level) and district (`info.zon` label), and prints it both ways round: per surface, where its area lives; per region, what it is made of. **2026-08-11: 12 of the 14 rule-bearing surfaces sit ≥ 90 % in one region type** (`p_sand_arid` 91 % desert and carrying the Joshua trees; `p_woodland`/`p_foreststumps` 100 % countryside and carrying the forest species), so a biome multiplier would scale a set the authored data already got right. Also prices the SLOPE half separately — the steep share per surface, which is the one signal no existing axis carries (`p_mountain` **48.8 %**, everything else under 20 %). And the number that makes the city half moot: Los Santos holds 64 409 m² of rule-bearing ground against countryside's 6 504 722. One full collision build (~1 min) |
| `procobj-spacing-census.ts [--density 1] [--nn 0] [--models <lod_procobj.models>] [--json <path>]` | **what SPACING and MINDIST are worth, both readings side by side** — the check that settled plan 07/02 decision 5. Prices the map-wide expectation under our `area / spacing` against the game's recovered `area / spacing²` ([`gta-sa-original/procedural-objects.md`](../gta-sa-original/procedural-objects.md)), then runs the converter's own whole-map pass and reports its nearest-neighbour signature. Stock 2026-08-09: **1 947 713 vs 103 007** expected objects (90 906 for the 43 converted species) — the reading was fixed the same day, so the `old` column now prices the DEFECT and the run measures the corrected pipeline: 43 converted species give **272 559 candidates → 91 067 placed** (drift 0.06 % against `area/spacing²×3`, the self-check that says a rig is modelling the pipeline that runs) with same-species nearest neighbour **min 0.0 / p05 3.4 / med 8.9 m and 90 180 of 91 012 pairs closer than their MINDIST**. That last number is the REGRESSION check: the shipped-before build read min 50.0 m with **0 of 20 246** below, the signature of MINDIST spent as an exclusion radius. Also prints **group-or-single per surface and per rule** — the share of a species' objects landing on a face that expects ≥2 of the same species vs ≥2 of any — which is where the field report about clumping becomes a number (`sand_combush02` 59 % same, `sand_josh1` 2 % on the SAME surface: the species' `spacing` decides it, not the biome). One full collision build (~1 min) plus the NN pass; `--json` dumps the per-rule table so a later slice needs neither |
| `procobj-layer-census.ts [<game-dir>]` | **what the BUILT tree's procobj layer actually carries**, split into the three counts that get confused with each other: placed objects (HD), permanent text LOD rows (tall species only), and binary stream records (HD + the unlinked LOD of every short species). `build/original/opensa` reads **15 286 objects / 6 487 rows in 8 slots / 24 552 stream records in 51 tiles / 43 species**, and plan 07 was costed for a fortnight against that 24 552 as if it were the object count. Two traps it exists to avoid: **scope by FILE, never by model id** (the stock map hand-places 816 instances of the same species), and `plotr*` areas are **shared with the tree impostors** (467 of them), so a row count per file is not a procobj count. Self-checks both identities — linked HD == permanent rows, unlinked HD == stream LOD records. Point it at a BUILT tree; `game-src` has no layer yet, and its `procobj.dat` still has the rules the build strips |
| `dump-fx-system.ts <system>` | one effects.fxp system: emitters, blend modes, textures, keyframed tracks |
| `fx-anchor-census.ts [--pak <dir>] [--level hd\|lod] [--system <name>] [--near <x,y>] [--limit <n>] [--worst]` | the 2dfx PARTICLE anchors a BUILT pak carries — per-system counts and, the half a count cannot give you, **where to stand to see one**. Plan 100's field check burned two rounds on emitters it had no coordinate for. Canonical pak (`13:19 08-08-2026`): **943 anchors in 569 hd cells**, `insects` 336, `vent` 209, `vent2` 162, `cigarette_smoke` 87, `fire` 53, `smoke30lit` 49; the lod level carries 888 of them, so **do not sum the levels**. **`--worst` answers the budget question** plan `sa-lod-generator/008` closed on: the point in the whole map where the most emitters are simultaneously LIVE, each inside its OWN shipped cull distance — **46 of 943** at `2582.1,2081.4,10.8`, and **28 of the 77** long-range smokes at `2726.2,2683.0,55.0`. Both are upper bounds (a frustum sees less than a sphere), it is recomputed from the pak so a pipeline change moves the viewpoint with it, and the positive control for a frame A/B there is `?fx=0.001`. These are NOT the 878/`insects` 402/`cigarette_smoke` 0 the 100 ledgers quote — those came from an in-process bake count, this is read off the shipping bytes. Prints anchors in GTA coords, and **self-checks that each one lands in the cell it was read from**: the pak stores them cell-LOCAL, and reading them as world put 934 of 943 up to 3 km away while printing perfectly plausible numbers |
| `wind-coverage.ts` | how each wind-listed model will sway; folder↔constant drift |
| `solve-roadsign.ts` | brute-forces the roadsign plate transform (kept as the method record) |

## Inspectors — pak side (what actually shipped)

| Script | Answers |
| --- | --- |
| `dump-osm.ts <model> [--pak dir]` | a built pak `.osm`'s sections + DESC fixture: parts, submeshes, texture-array refs, own-TEXS vs world-sourced, and the model's UV animations (keyframes, distinct u-offsets, cadence, loop) with the submesh each one drives |
| `dump-osm-meta.ts <model> [--pak dir]` | per-submesh texture-LAYER histograms (vertex meta) + each TEXS layer's size/format/mips/name-hash — the layer-mismatch finder |
| `dump-texel-avg.ts <model> [pakDir]` | average colour of each own-TEXS layer (BC endpoint scan) — tells a black/greyed bake from a faithful one in seconds |
| `dump-vehicle-ao.ts` | per-part night-alpha (vehicle AO channel) stats for the mods-src admiral/comet — bakes from the DFF, so it judges a sky-occlusion change offline (run · stash · run · diff) |
| `dump-vehicle-materials.ts <game> <model>` | what every submesh of a BUILT car ended up as (reads `vehicles.img`/`vehicles2.img`, then `gta3.img` — the split archives, 2026-08-17) — material class (matte/paint/chrome/glass), lamp tag, night-twin layer, min vertex alpha, reflection slots and mean SKY occlusion — read out of `build/<game>/opensa`, the tree a field run actually loads. The answer to "why does this panel shine / glow / turn red?": it found the previon's dash trim coming out CHROME at coefficient 0.5 with sky 0.63, mirroring the sun through the windscreen |
| `vehicle-submesh-census.ts [game] [model]` | what the BUILT fleet costs in draws and what a build-time same-state merge would leave (opaque submeshes shown by default vs distinct runtime states — part/kind/damage/extra/variant/array/uv-anim/lamp/plate/tyre), the translucent count that cannot merge, and the `_vlo` census; a model name adds its per-part breakdown. 2026-08-18: 200 cars, opaque 16 954 → 10 932 (−36 %), `_vlo` on 192/200, mean 96 k body tris vs 4 k LOD | `npx tsx scripts/debug/vehicle-submesh-census.ts original feltzer` |
| `dump-vehicle-rig.ts <model\|path.dff> [--game <game>]` | what the vehicle BUILDER sees in a car: frame tree, emitted parts, doors + their hinges, wheels, and the retractable-headlight pod with its derived open angle. Two traps are called out by name: a `<part>_ok`/`_dam` frame carrying its OWN transform (**SA destroys that frame** — a mod parked 1.518 m there and its doors hung off the car) and a ROTATED hinge/pivot (which is how a scissor door is built). Also prints the VehFuncs `f_extras`/`f_class` variant tree the runtime picks from per spawn (`[min,max]` per node, tagged submesh counts). A bare name is read from the game archives, a path from a mod folder |
| `dump-cell.ts <x> <y> [pakDir]` | a WELDED cell's tables at a world point: objectTable rows (kind, timed window, per-group class/array/sphere) + placement boxes near the point — the pak-bytes step for bugs in the welded look (built for 085 row H) |
| `dump-cell-collision.ts [x y] [pakDir]` | whether a pak carries BAKED collision (200/3-01) and on which grid — then a cell's regions with their triangle counts, placements, surface ids and breakable placements. First line is the grid, because the silent failure is a bake keyed on the RENDER grid (250) instead of the GAME grid (256): the world renders and the player falls through parts of it. With no position it summarises the whole bake, which is how a `--bake-collision` run is confirmed to have written anything; a pak without one says so and names the flag |
| `pak-recipe.ts <pakDir> [--expect key=value …]` | what a pak WAS BUILT FROM — rect, the texture format, `--max-texture`, `--bake-collision`, `--no-ao`, the model subsets and the claimed platforms — read back out of `report.json`'s `build` block, with the build time and commit. Answers "which pak am I actually looking at?" before a benchmark row cites one. With `--expect` it compares instead of printing and exits 1 naming both sides, which is the gate `npm run phone` uses to refuse serving a pak built for a different district, a different texture format or the other side of the collision A/B. A pak built before recipes were recorded says so and does NOT fail. A mismatch also prints the env overrides that would ASK for the pak that is actually there (`TEXTURES=rgba8 RECT=9,-7,10,-6 npm run phone`) — naming the difference without naming the knob cost a field round-trip. **A key the pak does not record is DERIVED, never reported as missing** (`textures` from `rgba8`, `lodOnly` as `false`): `phone.sh` sends every key on every reuse, so a key the table cannot answer refuses EVERY pak rather than none — `lodOnly` did exactly that from 2026-08-27 to 2026-08-28 and nothing was served in between |
| `texture-budget.ts [pakDir]` | what a pak costs the GPU (the decoded pyramid, computed with the runtime's own `ostexLayerBytes`) versus what it costs on disk, split by format, plus the same content priced in BC1 / ASTC 4x4 / ETC2 / RGBA8 and a histogram of layers by longest edge (what `--max-texture` moves). The number chain 2 is argued on: RGBA8 is 4x a BC3 payload and 8x BC1, so a phone that had only RGBA8 could buy a district and not the map. Since 2026-08-07 `opensa-pack --textures astc` writes the 1 B/texel column instead of hypothesizing it — run this against both paks of that A/B and the `by format as built` block is the answer. Prints the CEILING (every array resident at once); a drive holds the subset its cells draw |
| `cutscene-anim-channels.ts <entry.ifp> [object-regex]` | which BONES a cutscene anim drives, per object, with the keyframe kind (KRT0 = rot+trans, KR00 = rot only) and frame count — reads the old ANPK format in `anim/cuts.img` that the engine's ANP3 parser can't. THE ground-truth instrument of vehicle-cutscene's gate 7: proved every bone gets a NAME-bound channel (so converted rigs must keep vanilla locals as bind pose) and found the taxi's opening door channel (`door_rr_hi_ok`) binding to nothing because the mod misnames that door under its dummy |
| `cutscene-anim-replay.ts <img>::<entry.dff> <scene.ifp> <object>` | the model's cutscene pose as the GAME composes it, under the runtime law (plan 004 round 15, gta-reversed `FrameUpdateCallBackNonSkinned`): NAME-bound frames take their anim frame-0 value, every other frame is forced to identity rotation + stored position — the rewrite the engine does each tick. Prints per-atomic world y/z bboxes. Calibrate on the vanilla model first (must reproduce bind), then run the converted one; this is what reproduced HEIST8A's securica-on-its-tail when bind pose, worlds and a naive replay all looked upright |
| `cutscene-seated-actors.ts <cuts.img> <car,car,...>` | which cutscenes seat an ACTOR inside a converted CAR, the car-local offset they ride at, and for what share of the scene — the input to plan 005 (seat retarget) and the guard on its patch pass. A cutscene actor's position is ABSOLUTE out of `anim/cuts.img` while `ped_frontseat` is read by GAMEPLAY only, so a donor whose cabin rides higher than R\*'s seats its occupants low (0.281 m, measured on SMOKE2B) and no model change can fix it. Two traps paid for: a track SHORTER than the scene is a static pose and must hold its last value (a parked car gets 5 keyframes — comparing only the overlap made the first census miss RIOT_4B's greenwood entirely), and props ride vehicles too (an ammo box on the bobcat's bed), so the caller decides what is a person. The `seated %` column is what says whether a pair may be patched at all: an actor who walks up and gets in is seated for only part of the scene |
| `cutscene-scm-sites.ts [scene-regex]` | every cutscene main.scm plays (135 `02E4 LOAD_CUTSCENE` sites) with the interior AREA its adjacent `04BB` sets, the ONMISSION global (`$409`, read from `0180`), and — given a regex — the opcode window around a site. Settled cutscene-override's step 0: main.scm waits on `06B9 HAS_CUTSCENE_LOADED` between load and start (starting unloaded = degraded, no camera/widescreen), the area comes from the SCRIPT not the `.dat`, and every wait is condition-driven. `area=?` means the adjacent-byte heuristic missed (54/135 decode), not that the scene has no area |
| `cutscene-override-ini.ts [--out <path>]` | generates `cutscene-override.ini` for the CLEO cutscene-override script (cleo/scripts plan 003): 35 vehicle scenes in the comment head (ANPK `NAME` chunks × the vehicle-cutscene census), empty `scene =`, `[areas]` rows for ALL 148 scenes (a MISSING row is dangerous — a failed CLEO ini read corrupts the var, `docs/edge-cases/cleo-vm.md`), and `[sitex]/[sitey]/[sitez]` world sites from each `.cut`'s own `offset` row (the override warps the player there). Found that `csdinghy` is driven by NO cutscene (22/23 census models appear somewhere) and that the `.dat` beside each scene's `.ifp` is camera data, not the object list |
| `sa-name-key.ts <name>…` | the game's own name hash (`CKeyGen::GetUppercaseKey`, CRC-32-IEEE over the UPPERCASED name) for a list of names — the only way to read a RUNTIME log back as model names, because SA keeps no model-name string at runtime: `CBaseModelInfo` carries just `m_nKey`. Prints the signed form too, since a log that formats the key as int32 shows it negative. This is what identified the cars in perfect-cutscene's field census (plan 001 step 2): `csplay` −249921641 skinned, `cstaxi92` 1793024146 and `csgreenwood` −1591174577 NOT skinned |
| `cutscene-fleet-verify.ts <built-game-dir>` | structural verification of a vehicle-cutscene build vs the vanilla cutscene.img (plan 002 step 10's gate, exit 1 on failure): every archive DFF parses, skeletons consistent, per slot the FIRST frame with a vanilla name carries the vanilla bone id (anims bind by name to the first match; later duplicates are adopted mod meshes — counted, not failed), ids unique, indexes contiguous. Deliberately does NOT check vanilla-id order (order is free) + prints the per-slot DFF/TXD size table |
| `uv-anim-measure.ts [model] [--pak dir] [--hz 120]` | what a built model's UV animation DOES when the engine plays it, and what one advance COSTS: steps the `.osm`'s own keyframes through the engine's `stepUvAnimation` and reports the observed cadence against the authored keyframe gaps, then times 2 M calls (ns/call — the whole CPU cost of one animated model, beside one 16-byte write). Closed plan 099's two owed numbers: the ferris ring steps **0.225 s exactly**, 130 times per 29.25 s loop, at **~130 ns/call** with its real 261 keyframes. **The gap spread is the sampler, not drift** — raise `--hz` and it tightens around the authored value (0.2167–0.2333 at 120 Hz, 0.2208–0.2292 at 240 Hz), which is also the script's own self-check |
| `stream-ring-bounds.ts [pakDir]` | per-cell TRUE geometry XZ AABB vs the slot's 250-grid rect: overhang stats/worst offenders, hd-only↔lod-only slot pairing, ring-skip check from spawn — the plan-087 streaming-holes verifier (default pak `build/gostown/opensa/pak`) |
| `pak-placement-parity.ts [gameDir] [pakDir] [--streams <prefix>] [--slack <u>]` | does the PAK carry every instance the GAME DIR placed, at the same coordinates — the convert-is-lossless half of "`sa/` and `opensa/` are the same world" (the other half, one shared stage build, is pinned by pmb's target-split test). Reads a layer from BOTH the binary IPL streams and the text IPLs (default prefix `plobj` = procobj), then tests each position against the pak's placement AABBs. 2026-08-10: 182 184 / 182 184 at 0.05 u. **Re-scoped 2026-08-10 by plan 014:** the procobj clutter is baked for the `sa` target ONLY (OpenSA scatters it at runtime), so the default `--streams plobj` finds nothing in an `opensa` tree — and used to print `0 / 0` with `NOT covered: 0.000 %`, a perfect score for having measured nothing. An empty layer now says `NOTHING MEASURED`, names the cause and exits 1. The layer both targets share is the trees: `--streams plotr` reads **934 / 934 covered at 0.05 u** on the 2026-08-10 pak. Use `plobj` only against a `sa/` tree. **Always run its positive control** — the same game dir against another content's pak (`NO_COMMIT/old_map/pak`) must report ~98 % uncovered, or the 100 % means nothing |
| `grid-extent.ts [gameDir]` | the true occupied 250-grid extent of a game dir (default `build/gostown/sa`) — measures a game's `PACK_RECTS.full` rect BEFORE any pak exists (gostown `[-8,-16,37,5]`, original `[-12,-12,11,11]` measured 2026-07-23) — AND names the stragglers, the cells sitting far from the median, since one exiled placement stretches the extent without adding any map (a merged original build: `[-12,-81,11,11]`, one lamppost at cell `(9,-81)`) |
| `water-depth-map.ts [pakDir] [x0 y0 x1 y1]` | ASCII heatmap of the baked water DEPTH field (water.bin) over a GTA region — the pak-bytes step for water-look bugs (found 087 row C: T-junction false shorelines striped every gostown lake; default region = the bridge/dam lakes) |

Default pak: `build/original/opensa/pak` (086 phase 8 — the game dir is self-contained; older builds are
probed at `opensa-pack/` and the nested `opensa/opensa`). The world-welded side of a model lives in
cell bundles, not its `.osm` — `dump-cell.ts` covers that path.

## Mutators — write to the source IMG (`scripts/debug/`)

Unlike the inspectors, these EDIT `game-src/<id>` in place. `game-src/*` is git-ignored, so the edit is
local and reversible by re-extraction; each is report-by-default, `--write` to apply, and drops a one-time
`.bak` of the target IMG first.

| Script | Does |
| --- | --- |
| `teleport-spot.ts <model> [--near x,y] [--distance N] [--eye 1.6] [--top 3] [--src <builtDir>]` | **inspector, does not write** — where to STAND to look at a map object: a ring of candidate spots around its placement, each one ray-cast onto the collision of every instance nearby (the z you would actually land on), rejected if a player-sized box there is inside anything, and annotated when the view to the target crosses another model. Instance rotation is honoured (rays go into each model's own space), and the world AABB comes from the eight ROTATED corners — swelling to the bounding radius made one long fence veto every direction. Prints ready-to-paste `SA_TELEPORTS` rows. Reads `build/<game>/opensa`, the tree a field run loads. Written for plan 092's field controls after a hand-picked spot put the camera inside a building |
| `model-repack.ts <model…> [--dff f.dff [--txd f.txd]] [--no-lod] [--strip-normals] [--crease <deg>] [--prelit-floor <luma>] [--margin <cells>] [--raw] [--no-mods] [--mod-only a,b] [--no-ao] [--game g] [--out <dir>]` (+ `LAB_NO_WATER=1`) | **the OpenSA-target one-model swap** (opensa-lod-generator plan 007 — the counterpart of `model-lab.ts` for `sa`): swap a version of a world model — an optimizer variant, or a mod's LOOSE `--dff/--txd` — into a servable lab copy of the built game WITHOUT a full rebuild, HD **and far view**: the rect's cell LODs are re-baked from the swapped HD the way `opensa-lod-generator` does (`--no-lod` keeps the built ones), then the rect is re-welded. gostown one cell: 1.9 s (LOD 1.2 s); original `burger01_law` (88 models): 17.4 s (LOD 12.1 s). Originally plan 024 phase 0 — the static world renders from `pak/world.ospak` welded cells, so a DFF edit in the built tree changes nothing. Resolves each affected model's source the way the build does (last mod wins, PNG texture-folders replayed), re-runs the map-optimizer geometry chain in-memory (targets get the experimental flags), re-welds ONLY the cells the targets touch via `convertDistrict` rect, and writes `build/<game>/opensa-lab` — a per-file-symlink mirror of the built game with the lab pak. ~10 s for a one-cell rect (111 models). Serve with `npm run serve:static`, app `?src=/build/<game>/opensa-lab`, spots via `teleport-spot.ts`. Bisection levers (blue-strip hunt, `docs/open-issues/fixed/mod-dff-winding-and-atomic-frame.md`): `--raw` welds SOURCE bytes (no optimizer chain), `--no-mods` resolves vanilla-only, `--mod-only a,b` re-mods just those DFFs, `LAB_NO_WATER=1` drops the water pass. Caveats in the header: world-context prelight not replayed, generated LODs thinner, `.osm` props may lose textures inside the rect |
| `scan-model-defects.ts [--game g] [--top N] [--dz 0.5] [--json out]` | ranks every PLACED world model by broken-authored-vertex-data criteria (plan 024 phase 1), scanning the SOURCE assets the build consumes (last mod wins, vanilla otherwise) so each hit NAMES the mod that shipped it. Family A: faces referencing an authored vertex normal facing the GROUND ≥ `--dz` more than the face itself, ranked by summed flagged AREA — the test is asymmetric and area-weighted because field round 1 falsified the naive angle version twice (`standard01_lawn`: up-rotated grass-card normals are a legal authoring trick that only brightens; 686 tiny faces ≠ 22 road slabs). Family B: day-prelit black holes (verts < 10 luma, whole-black triangles) on models with a healthy day median (≥ 40; `tobj` excluded). ~1 min for the 7 177-model original map. Family C (`--aniso` 8 + `--discont` 4, plan 025): faces whose UV→world map draws a texel N× longer than wide, up to a collapsed axis, ranked by the SHARE of the model's own surface they cover and printed with the map-wide POPULATION. First run's headline: 65 Family A models; 2 243 Family B (186 with ≥100 black tris — 125 of those VANILLA: SA authoring baked shadow to 0 relying on the renderer's ambient floor); **Family C 4 708 models at raw 8×, 3 464 once the model-baseline test is added — but the criterion is NOT settled: three formulations mis-rank in turn (raw puts cables first, edge-neighbour disagreement misses contiguous bands, model-baseline catches bands and puts wires back on top). Use it to SIZE the population, never to conclude a model is broken; plan 025 has the detail** |
| `scan-geometry-parity.ts [<gameDir>] [--all]` | the two ways a LEGAL DFF can be rendered wrong by us (plan 095), sibling of the row above — that one ranks defects in the authored data, this one in our READ of it, and both families are silent because the model still renders. **Family C:** the Struct face array disagrees with the BinMeshPLG index data RenderWare actually draws — an inside-out mesh, which for a single-sided road slab means it vanishes (`roads32_law2`, the Santa Maria "blue strip"; 4 models in the merged original map). **Family D:** a non-identity transform on the frame an ATOMIC hangs from, judged against the **COL** — collision is authored in the space SA renders, so whichever version matches its bounds is the truth (`land_42_sfw` 8.15 → 0.00 with/without; vanilla `aw_streettree1` was sunk 3.1 m for 165 instances). Two traps baked into the code as comments: Family C must compare FACING HISTOGRAMS (sorted-triple keys report 770 phantom flips on vanilla `laenwblk2`) and Family D must ignore the `Omni*` 2dfx light frames (189 models → 41). Takes an SA-FORMAT tree, never `build/<game>/opensa` |
| `verify-cell-normals.ts <cellKey> <model> [labPakDir] [mainPakDir]` | **inspector, does not write** — reads a MODEL's welded vertex normals straight out of a pak cell (oswire→oscell decode, placement table → exactly the vertices its triangles reference) and prints the n.up histogram — the offline proof that an experiment reached the shipping cells without booting the game. Defaults to comparing the main pak against `model-repack.ts`'s lab pak. Cell key = `<cx>,<cy>,hd\|lod` from the instance pivot (`position/250` floored). Proved plan 024's Family A end-to-end: main pak roads17 = 96/165 verts sideways/down, lab (strip-normals) = clean up |
| `zero-vehicle-dummy.ts <file.dff> <dummyName> [--write]` | moves one vehicle DUMMY to the model origin — how an author declares "this model has no lamp of that kind" (`docs/contracts/vehicles.md` §3: SA reads a missing lamp dummy back as (0,0,0) and `DoHeadLightBeam` tests exactly that; OpenSA honours it since 098/11). A 12-byte edit to that frame's position, nothing else touched. **It checks itself**: the offset it computed must decode to the position the PARSER reports for that frame, and after writing it re-parses and fails unless exactly the named dummy moved. Dry run by default. Writes into `mods-src/` — keep the author's archive, there is no undo |
| `anim-census.ts [--img anim.img] [--groups bikes,bmx,quad\|all] [--ped ped.ifp] [--match regex]` | what the animation archives actually CARRY for a vehicle anim group — per IFP every clip with bone count, keyframe count and root motion, plus the `ped.ifp` clips matching a regex. Answers "does a clip for this exist at all" before a resolution table or a pose plan is written. Plan 098/12's reading: the nine ride groups share one 12-slot table, **no wheelie or stoppie clip exists anywhere** (`Left/Right/Back/Fwd` are 2–7-frame partial poses), bicycles add `bunnyhop`/`pedal`/`sprint`; for cars (098/13) `ped.ifp` carries 80 car-class clips of which the engine plays 7, and the per-group IFPs (`truck/van/coach/bus/kart/tank/dozer/BF_injection`) hold the boarding sets `handling.cfg` col 34 selects. Re-run after a mod overrides a ride IFP by bare name |
| `lamp-census.ts [img]` | which vehicles actually AUTHOR lamps — per model the `headlights`/`taillights` dummies (real / at the model ORIGIN, which is SA's own "no lamp here" / absent), the count of head/tail lamp-material submeshes, and the ImVehFt `ivflights` cross-tab. Answers "why is this car dark at night" and "what would a lamp rule cost" without booting the game. Reads a BUILT archive (`.osm`, default) or a stock one (`.dff`), so mod and stock are directly comparable. Found plan 098/11: 12 stock models — every trailer and aeroplane — carry no lamp dummy at all and were being given headlights anyway. **Re-run it after installing custom cars**: a model whose lamps live only in `ivflights` reads as lampless to us |
| `txd-from-pngs.ts <folder> [out.txd] [--version-from <t.txd>] [--replace]` | builds a `.txd` FROM a folder of PNGs — the missing half of the texture-folder convention (`docs/contracts/mods.md`), which only ever PATCHES a dictionary that already exists. Use it when the installer says `texture folder … — no entry <name>.txd`: a mod that ships the PNGs of a whole dictionary has nothing to patch, so every texture is dropped and the models naming it render untextured. Writes `<folder>.txd` beside the folder; `--replace` then deletes the folder (the PNGs were its unpacked form). The RW version is READ from a sibling dictionary, never assumed, and each texture's format follows its own image (DXT5 with real alpha, DXT1 without) — the same rule the merge applies. Real case: `52. Abandoned Cars`'s `gta3_img/philss/`, 22 PNGs that were exactly `cuntwjunk04`'s 22 textures. **Writes into `mods-src/`**, unlike the row below |
| `strip-polygons-from-dff.ts --img <path> --tex <name[,name…]> [--models a,b,…] [--write]` | drops every submesh whose material references a given texture (its triangles) from DFF models inside an IMG — for cards that render as flat missing-texture quads because the texture is absent from the mod. Vertices + the material list are left untouched (the material just goes unused — no re-indexing), so the other cards on the same model survive; each edited DFF is re-parsed and verified before write. Default scans all DFFs; `--models` restricts. Fixed gostown's `LODEnsemble*` `Gp_feuillu1` magenta (`lodveg.txd` ships 6 of the 7 LOD-veg card textures; `Gp_feuillu1` is absent from the mod — not a recovery miss) |
| `bundle-inventory.ts [--entry dispatch.html] [--dist dist] [--top 25] [--json out.json]` | what a built HTML entry SHIPS: every chunk it reaches by STATIC import (an `import()` chunk is downloaded later and is deliberately excluded), raw and gzipped, plus every source module inside them attributed by a sourcemap walk. Needs `npm run build -- --base=./ --sourcemap` first. Written for 201/1-06 because the 08-09 before-table was measured by a one-off script and **lost 107 kB of it silently** — `shaders.ts` went unattributed and the chain drew a wrong conclusion from the rest |
| `residency-census.ts [--cells 41] [--json out.json]` | what the streaming residency rule ASKS FOR per zoom — ring policy vs view policy (201/1-05), counted out of the shipped `StreamingDriver` over a fake engine and a fake worker, on a synthetic grid. Answers *did the view gate actually cut the request set, and at which zoom does HD stop being fetched* without a pak, a GPU or a device. It measures the RULE: seconds and megabytes need a real district and belong to a field run |
| `overlay-census.ts [--units 150] [--calls 40] [--shapes 10] [--json out.json]` | what the console's label decluttering (201/3-03 — placed vs dropped on a phone and on a desk, against the viewport's own ceiling), its radar (201/7-04) and its sketches (201/7-05) ASK OF THE 2D CANVAS per frame, through a stub context that records calls — and whether the radar's dirty check actually skips a frame where nothing moved. Answers *how much work an inset requests* without a browser, a GPU or a device; milliseconds still need a field run |
| `multi-atomic-census.ts --game <game dir> [--tree <built tree>]` | every DFF behind an IDE `objs`/`tobj`/`anim` row with MORE THAN ONE atomic (`_dam` twins excluded): section, id, each atomic's frame + offset + triangles; with `--tree`, the LOD each such HD links to in a built tree and how many atomics THAT file carries. SA's atomic model info keeps ONE atomic of a clump it reads (`docs/gta-sa-original/atomic-model-one-atomic.md`), so `lodAtomics > 1` on an `objs` LOD is a LOD that draws one part of itself at the origin — the census that found the Burger Shot's absent LOD (`sa-lod-visibility-budget.md` round 15): stock 34 models, all `anim`; 16 verbatim multi-atomic clones in `build/original/sa` |
| `txd-dimension-census.ts --game <game dir> [--filter '^salod'] [--npot]` | every DXT texture in `models/gta3.img` whose top level is NOT a multiple of 4 on both sides — the raster the real game refuses, taking its whole dictionary and every model on it down (`docs/restrictions/dxt-raster-dimensions.md`); plus a count of non-power-of-two textures (4-aligned ones load, `--npot` lists them). Stock: 0 of either. Turned "~6 LODs missing all over the city" (`sa-lod-visibility-budget.md` round 16) into five named dictionaries + one mod TXD; 0 fatal on any tree built after 2026-08-17 |
| `png-folder-census.ts [--game original]` | what every PNG **texture folder** in a game's mods does to the dictionary it patches: per PNG the image size, the stock texture it replaces (size, format, levels) or "adds", and — for a replacement of an UNCOMPRESSED raster — what both encodings cost over the whole mip chain. The census behind `docs/contracts/mods.md`'s inherit rule and [mod-installer plan 015](../../tools/mod-installer/docs/plans/015-replacement-png-follows-its-raster.md); run it before touching that policy. `original`: 33 folders, 56 additions, 24 replacements, 18 of an uncompressed raster (2 673 KB as DXT, 18 264 KB as they ship) |
| `carmods-check.ts <game dir>` | does the tree's `carmods.dat` name a model no IDE row defines? The real game crashes at boot on one (`docs/gta-sa-original/carmods-unknown-part-crash.md`, `0x4C4576`); the installer refuses to write one since plan 009, so this is for a tree the installer did NOT write — the bottle, an older build. Exit 1 with the lines. Found the blade's two `tuning_new_parts.txt` parts (2026-08-17) |
| `col-splice.ts --base <prev.col> --donor <mod.col> --models a,b --out <merged.col>` / `--list <file.col>` | byte-for-byte splice of named collision models from one `.col` LIBRARY into another (COL2/3 blocks copied whole, nothing re-encoded), and a block listing. For the quiet `.col` conflict `docs/contracts/mods.md` describes: a mod shipping a whole library cut from STOCK reverts every fix an earlier mod's copy carried, and the installer's LOST warning stays silent (mod 65's `lae2_5.col` reverted three Map Fixes Pack models, 2026-08-17 — repaired with this) |
| `dump-binmesh.ts <file.dff…>` | a DFF's `BinMeshPLG` per geometry — strip/list, split count, and every split in FILE ORDER with its material, index count and vertex range (+ the extension's chunk types). **The split order is the draw order inside the atomic** and RenderWare authors blended materials LAST; this dump is what found the map-optimizer rebuild sorting `cehollyhil06`'s vertex-alpha detail split (material 8 of 15) into the middle — the whole "normals × repeat textures" smear (`sa-lod-visibility-budget.md` round 14). Extract the file from an IMG with `img-patch.ts get` first |
| `img-patch.ts <set <name> <file> \| restore <name> \| get <name> <out> \| status> (--game <built tree> \| --img <archive>)` | swaps ONE entry of a built game's IMG in place — the field A/B instrument that costs seconds instead of a ~10 min rebuild (open issue `sa-lod-visibility-budget.md`, round 8). `set` APPENDS the bytes past the end of the archive and repoints the directory; the original bytes stay put and the pristine record goes to `<img>.patches.json`, so `restore` is a directory write. Never shrinks or overwrites in place — that is how `lodcuntw65` became a hole in `build/bisect-nomods/sa`. `--game` resolves and patches EVERY archive of the tree carrying the name (`gta3.img`, `gta_int.img`, the `gta.dat` IMG lines). **A name the archive carries TWICE resolves to the FIRST record — the one SA reads** (2026-08-19: the index was last-wins, so a `set` patched the copy the game never opens and reported success; an IMG editor that ADDS where it should replace is how the duplicate gets there). The duplicate is announced when it is met. Library half in `scripts/lib/img-patch.ts` for other scripts |
| `model-optimize.ts <model…> --src <game dir> [--patch <built tree>] [--out build/model-lab] [--no-add-normals \| --strip-normals-after \| --list-only \| --restrip \| --raw] [--crease <deg>]` | runs ONE model through the map-optimizer GEOMETRY chain (weld → degenerate → dedupe → prune → smooth-normals, the build's addNormals + curated crease wiring) in memory, prints per geometry `flags STRIP/LIST N P L, tris, verts, struct bytes` before → after, writes `<model>.<variant>.dff` and with `--patch` swaps it into the tree via `img-patch` — one variant per run so a field A/B is one flag apart. `--strip-normals-after` is the chunk-level strip round 10 left untested (NORMALS flag + morph normals dropped, trilist re-encode and split vertices KEPT); `--list-only` forces the SOURCE through `rebuildGeometry` alone (trilist BinMesh, re-emitted Struct triangles, bounds — no chain, no normals, no split); `--restrip` is the chain's output with its trilist BinMesh converted BACK to a tristrip (degenerate-joined, parity kept, TRISTRIP flag set — a probe encoder at ~2× the index count, not a stripifier); `--crease 180` is the free fourth variant (no split → overlay path → strip kept, normals ON). NOT replayed: the world-context prelight passes (they moved 378 B of `cehollyhil06` without touching structure). `--src` is any tree with the PRE-optimizer bytes (`game-src/original`, `build/bisect-nomods-noopt/sa`). Reproduces round 9 exactly: `cehollyhil06` `0x6f STRIP -PL 1320 v / 39 744 B → 0x7e LIST NPL 1322 v / 55 656 B` |
| `model-lab.ts <model> --tree <built tree> (--src <game dir> \| --dff <file> [--txd <file>]) [--no-add-normals \| --strip-normals-after \| --list-only \| --restrip \| --raw] [--crease <deg>] [--dry]` | ONE model through the whole `sa` pipeline the field sees, in ~1 s: HD source (any game dir's archives, or a loose DFF/TXD out of a mod) → the map-optimizer geometry chain in a named variant (`scripts/lib/optimize-model.ts`) → the clone LOD cut from the RESULT the way `sa-lod-generator` does (`cloneLodDff`, run-config decimate budget + 2dfx policy) → patched in place into the tree (HD `.dff`, LOD `.dff`; with `--txd` also the HD `.txd` and a regenerated `texScale` clone TXD under the LOD's current `salodNNNN` name). The HD↔LOD link is read from the built tree's own IDE/IPL, so an HD whose LOD the build left stock (shared / dual-role / tiny) gets HD-only + a note. Exists because clones are cut FROM the HD — an HD-side variant that does not reach the LOD leaves the far view with the old defect. Verified: the tree's `lodcehollyhil06` is byte-equal to its `cehollyhil06`, and the script's clone of the same input is too. Undo per entry with `img-patch.ts restore` |

## Driving the map viewer — the folder-to-folder A/B (094)

`sa-map-viewer` renders the map straight from a folder of **SA-format** files (`game-src/<game>`,
`build/<game>/sa`, `build/salod`) — no repack in the loop, and a pose fully specified by query params. For an
OpenSA-converted dir (`build/<game>/opensa`, geometry is `.osm`) use the in-game debugger's map viewer
instead; this one says so and welds nothing.

Interactively it is the debugger's map panel: the cell grid, whole map, LOD mode, and **click-to-pick** —
a click reports the placement's model/txd/GTA position and can hide it (`Restore all` brings it back). That
is the fastest way to put a NAME on something odd in a screenshot before any bisecting starts.

The **sea** is drawn too (the flat `water.dat` build — no bake exists for a source folder), with a
**Show water** checkbox: an inspector has to be able to look UNDER the sheet at a sunken road or a
mis-levelled pool. It is the one thing in the viewer that moves on its own, so `map-viewer-shot.ts` shoots
with `water=0` unless the URL says `water=1` — with the sea in frame two runs are no longer byte-identical.

The other direction — you have the NAME and want the place — is **FIND MODEL**: type a substring, the rows
list every PLACED name with its placement count, Enter (or the row) centres the view on the nearest
placement and makes its cell resident. Pressing Enter again walks the same name's other placements outwards,
so a model placed 30 times is reachable past the first one. The pose (height, pitch, yaw) is deliberately
NOT changed by a jump — a search from a whole-map height stays there, so it cannot silently alter what a
capture is showing; dolly in afterwards.

| Script | Answers |
| --- | --- |
| `map-viewer-shot.ts <appUrl> <outPng> [timeoutMs]` | one scripted pose of a source, captured headless. Adds `panel=0` unless the URL sets it (the panel carries a live fps line, and a pixel diff must not compare a frame counter) and echoes the viewer's own `[sa-map-viewer]` load lines, which NAME the source. **Two runs of one URL are byte-identical PNGs** (wind is off by default — it was the only thing animating a noon frame), so two sources at one pose diff directly: `magick a.png b.png -compose difference -composite -colorspace Gray -format '%[fx:maxima*255]' info:` |

| `game-shot.ts '<query>' <outPng> [settleMs] [srcUrl]` | one standing pose of the GAME host, captured headless through the real load path — the sibling of `map-viewer-shot.ts` for what only the game can answer: a look A/B of a DATA change (weather, timecyc, lighting) with the ped grounded and the world streamed around him (built for 104/04, 12 captures in one session). `query` is the host's own knobs without the `?`; the loader and `src` are supplied. It prints the **HUD** (so `grounded` and the live position sit beside the picture) and the boot lines that say what the run READ — `[timecyc]` names the timecyc file that won, which is the only way to know which table a capture photographed. Three traps it exists to get right: **a guessed `spawn` z drops the player into the void** (`?spawn` does not wait for collision — read a real height out of `dump-cell.ts` first); **`weather` and the player's CITY must be chosen together** (the zone remap turns a `FOGGY_SF` forced in LS into `SUNNY_LA` before frame 1); and **aim is not sight** — `look` points the camera, it does not promise an open sightline |
| `console-surface-check.ts [--dir dist] [--browser <chrome>] [--width 360]` | **does the console's chrome FIT a phone, and can a finger hit it** — the check [`docs/restrictions/cross-platform-surface.md`](../restrictions/cross-platform-surface.md) has been asking for since it was written, where it says the failure is silent in every way this repo can be silent and that *nothing in this repository measures a touch target*. Serves the build at 360 CSS px with a COARSE pointer (`hasTouch`, which is what makes the styles switch) and fails on two things: a control past the viewport edge with no scrollable ancestor to reach it, and any control under 44 px in either axis. It found both on its first run, 2026-09-05, and neither was new — the skin picker sat at 398..492 and the hour dial had collapsed to **0x44**, present and focusable and impossible to use. Run it after adding any control to the console's chrome. |
| `webapp-smoke.ts [--dir dist] [--browser <chrome>]` | **does the built app BOOT when served the way the phone serves it** — from `/build/webapp/`, not from the server root. Fails on a page still reading `starting…` after the boot window and on any `/assets/` 404. Written 2026-08-31 after `prebuilt/opensa-webapp.tar.gz` was rebuilt without `--base=./`: absolute asset paths 404 under a subdirectory, the module never runs, and the console sits on the boot shell with no error anywhere. The first check written missed it by serving `dist/` at the ROOT, where an absolute path happens to resolve |
| `canvas-symbol-arms.mjs [--browser <chromium>]` | **is a blit actually cheaper than rebuilding a symbol's path, and by how much** — the desk-side control for 201/9-01, on a real Canvas2D in headless Chromium. The console's phone has no `timestamp-query`, so on the DEVICE that change is priced by flying `nosprites` against `board`, which needs a settled window and a thermal bracket; this answers the narrower question in ten seconds. 150 marks a frame drawn both ways, arms INTERLEAVED and run twice per invocation. **Read the clamp note in its header before trusting a small delta**: `performance.now()` is clamped to ~100 us and one frame of 150 marks is ~0.3 ms, so the first version quantized every sample onto three values and read as a clean 15 % win it had no resolution to see — a sample is a BLOCK of 40 frames now. Its second reading is the useful one: 150 marks cost ~0.3 ms at a desk while the phone reports `overlay:symbols` at **7.37 ms**, which says the DRAWING is a minority of that span on the device and the rest is what the layer does per unit around it It also prices the SCALE BAR and the font churn inside it, which refuted the guess that `sym:scale`'s ~250 µs a frame is its two `ctx.font` assignments: alternating two shorthands is 0.6 µs an assignment and the whole bar is 3 µs ([the row](../benchmarks/opensa-engine/2026-09-05-desk-canvas-symbol-arms.json)) |

```bash
npm run serve:static && npm run dev   # /build + /game-src on :3001, the app on :5173
npx tsx scripts/debug/map-viewer-shot.ts \
  'http://localhost:5173/sa-map-viewer.html?src=http://localhost:3001/game-src/original&at=150,-1700' a.png
npx tsx scripts/debug/game-shot.ts 'weather=9&hour=12&spawn=-1960,505,36&look=-2400,520,30' fog.png
```

## Driving the game itself — the scripted physics lap (081/01)

The most useful tool this repo has for a gameplay bug: **the real game, driven by a script, printing
numbers**. Not a simulation of the game and not a unit test — the timeline goes through the same
`InputState` and the same `drive()` a player's keyboard does, so what it measures is what ships.

```bash
npm run serve:static && npm run dev              # the build on :3001, the app on :5173
SRC=http://localhost:3001/build/original/opensa
# one scene, or `phys=all` for the seven; `car=` picks the model
TAG='[phys]' NODE_PATH=$PWD/node_modules node tools-debug/bench-harness/drive.js \
  "http://localhost:5173/?loader=http-dir&src=$SRC&phys=all&car=infernus" phys 1200000 7
npx tsx scripts/phys-compare.ts before.log after.log [--determinism]   # diff two capture sets
npx tsx scripts/phys-regression.ts sweep-*.log                        # gate a sweep on the shipped pack
```

A lap teleports beside a real road spot, waits for streaming AND the collision behind it, spawns the car,
seats the player, settles the springs, then plays a keyframe timeline while the telemetry ring records every
fixed step — and prints one `[phys] {json}` line (summary + a 20 Hz series). Scenes:
`apps/web/src/phys-scenes.ts`. Captures belong in
[`docs/benchmarks/vehicle-physics/`](../benchmarks/vehicle-physics/readme.md).

**What it is good for beyond physics tuning:** it is a repeatable driver for anything that only happens while
a car moves. Two engine bugs were found in its first day of use — telemetry reading every orientation-derived
rate as zero (an aliased array), and a car unloaded after you left it killing the fixed step permanently —
and both were invisible to the unit suites.

**How to read a failed lap.** The runner names its own failures (`[phys] scene 'x' failed: …`), and the
harness screenshots on exit — the on-screen HUD carries `FIXED-STEP ERROR: …`, which is how the despawn
crash was identified in one look. A capture that came back absurd (a car "airborne" for its whole lap) is
usually the lap seating into a WRECK a previous scene left at a shared spot.

Companion scanner: `road-straights.ts` (above) — where to put a new scene without guessing coordinates.

**Full guide: [`docs/development/physics-laps.md`](../development/physics-laps.md)** — the scene format, how
to add one, the capture schema, how to read a failed lap, and the gotchas five sweeps produced.

## Approaches beyond scripts

- **`report.json` ledgers first** — `textures.missing` (name → models that asked) and
  `textures.crossTxd` (donor txd per rescued name) at `<pak>/opensa/report.json`; one ⚠/ℹ pack-log
  line per event. A clean ledger rules out the resolver in one grep.
- **F2 debugger in game** — Position teleports (feature spots are pre-listed in
  `apps/web/src/game-config.tsx`), time-of-day presets, "Missing Textures: magenta" toggle.
- **Headless field check** — boot+screenshot+bench the real game without a window:
  `?loader=http-dir` + `npm run serve:static` (`tools-debug/bench-harness`); one-liners in
  [`docs/commands.md`](../commands.md). The served dir must be an opensa-pack `--out`.
- **The `[slow]` frame breakdown** — the dev-mode console line (Perf screen toggle) for any frame over
  20 ms: `render (submit) · stream (blob N worst M upload U) · camera · fixed (steps: controller + physics ·
  cars) · collision · vehicles · ped · anim · other`, plus draws / cells / bodies / colliders. **Read
  `other` first** — it is what the loop did NOT account for, and a 2026-07-27 field report of 20-250 ms
  frames was 90-98 % `other` until `blob` (the pak worker's texture upload, which ran BETWEEN frames) was
  given its own timer. A stall outside the loop cannot be found by reading the loop. `blob`/`worst` are the
  handler's residue (decode + createTexture) and `upload` is the budgeted in-frame drain, after the fix
  moved the writes into `StreamingDriver.update` (`docs/performance/applied/texture-upload-budget.md`).
- **The `[cam] jump` watchdog** — same Perf toggle: one line when the camera's look target jumps > 1.5 m,
  or the yaw jumps > 20° on an idle mouse, outside every legitimate discontinuity (teleport, mode switch,
  scripted seat sequence, fly, bench) — with the step state (mode, dt, distance, collision shown). Quiet on
  a healthy session. Distance-channel moves are deliberately not watched: the designed occlusion snap-ins
  live there (plan 080/09 §4.1; the seat-entry slam it hunted turned out to be a distance-channel glide,
  found by reading `resolveCollision` against the report).
- **Shader-term probe** — temporarily output a shader term as the fragment colour, shoot headless,
  compare against expectation. Reading the code had pointed at the wrong cause twice in 084.
- **Spot rebake (no full pmb run)** — APFS-clone the build (`cp -Rc build/original build/.x`), rebake
  one model, drop it in with `rewriteModelArchives` (inserts ONLY — a delete of the same name removes
  what you just inserted), serve, shoot, delete the clone.
- **Offline algorithm replica** — when a baked value looks wrong, rebuild the model through the SHARED
  builder in a `.tmp` script and instrument the exact function (per-azimuth traces pinned the 084 AO
  smudges to a scrap-ratio division in minutes; eyeballing histograms had only narrowed the part).
- **Real fixtures over synthetic** — a real GTA asset fixture is ONE manifest line
  (`npm run test:fixtures`); real files falsify what synthetic fakes confirm.
